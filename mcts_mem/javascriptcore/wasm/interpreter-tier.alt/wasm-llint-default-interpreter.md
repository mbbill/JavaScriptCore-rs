- Wasm uses the LLInt generator by default unless IPInt is explicitly requested.
- IPInt validation and generated entries exist only as non-default implementation support.

## Moves

- 2025-01-17 (6ee9b349) replaced by [[interpreter-tier]]: After fixing the known bugs, IPInt should be stable enough to re-enable again. (sourced)

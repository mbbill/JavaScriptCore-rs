# Sparse Rewrite Progress

Record only major completed checkpoints. Do not add entries for small fixes or
one-off test adjustments.

## Completed

- 2026-05-17: Single-crate Rust project with module-level JSC subsystem layout is in place.
- 2026-05-17: Parser -> bytecompiler -> interpreter path executes a broad JavaScript subset.
- 2026-05-17: Core VM tests cover objects, properties, functions, classes, control flow, exceptions, arrays, strings, primitives, JSON, Math, Date, errors, Map/Set, Symbol, Promise, and RegExp basics.
- 2026-05-17: Reflect and Proxy runtime slices are implemented and VM-tested.
- 2026-05-17: ArrayBuffer, Uint8Array, and DataView basic runtime slices are implemented and VM-tested.
- 2026-05-17: WeakMap and WeakSet API slices are implemented and VM-tested; full GC ephemeron semantics remain open.
- 2026-05-17: Array mutation and combination slices are implemented and VM-tested, including reverse, sort, splice, shift, unshift, concat, and fill.
- 2026-05-17: BigInt storage moved from i128-backed values to internal arbitrary-precision signed values, with VM tests above the i128 range.
- 2026-05-17: Current code gates passed after the BigInt update: `cargo test --lib` 700 passed, `cargo check`, `cargo clippy --all-targets --no-deps`, and `cargo fmt --all -- --check`.
- 2026-05-17: Rewrite scheduler updated to require one reference interpreter plus one honest baseline JIT tier as the first executable architecture proof.
- 2026-05-17: Parallel audit completed for runtime, modules/jobs, memory/API, and execution tiers; next serial chain is execution-mode boundary, cell/root identity, baseline frame/root ABI, baseline install, then narrow opcode execution.
- 2026-05-17: Execution-mode boundary implemented: default interpreter-only mode, explicit baseline-allowed mode, baseline fallback records, interpreter execution-path diagnostics, and differential source test; `cargo test --lib` 702 passed.
- 2026-05-17: Current rewrite position is P3 memory ownership integration; P4-P6 remain the serial path to real baseline JIT execution.
- 2026-05-17: P3 prerequisites landed for target-carrying precise roots and Symbol/BigInt static cell metadata; `cargo test --lib` 709 passed.
- 2026-05-17: P3 heap-owned cell-payload bridge landed with one-to-one payload/CellId binding checks; `cargo test --lib` 715 passed.
- 2026-05-17: P3 interpreter dispatch now carries explicit heap access for future heap-backed allocation/root/barrier integration; `cargo test --lib` 716 passed.
- 2026-05-17: P3 heap-owned targeted-root registry landed, including target validation and marking-plan target diagnostics; `cargo test --lib` 728 passed.
- 2026-05-17: P3 string interning now binds existing untracked string payloads when heap-aware execution requests them, preserving pointer identity while assigning checked `CellId`s; `cargo test --lib` 728 passed.
- 2026-05-17: P3 object identity bridge added for lazily binding interpreter-owned object payloads to heap `CellId`s without broad allocation call-site migration; `cargo test --lib` 731 passed.
- 2026-05-17: P3 interpreter register-root snapshots can now resolve cell values into targeted roots, lazily binding untracked object/string payloads without installing live root lifecycles; `cargo test --lib` 734 passed.
- 2026-05-17: P3 interpreter dispatch now synchronizes live targeted VMRegister roots for CoreOpcode execution and unregisters them on completion; `cargo test --lib` 738 passed.
- 2026-05-17: P3 staged API protection root planning and owner-aware object value-store barrier contracts landed; full gates passed, including `cargo test --lib` 749 passed.
- 2026-05-17: P3 live API protection root mutation, compiler safepoint targeted-root plans, and VM heap-owned top-level CodeBlock/GlobalObject identities landed; full gates passed, including `cargo test --lib` 761 passed.
- 2026-05-17: P3 heap-known frame roots and VM-owned global roots now sync live targeted roots through the heap; full gates passed, including `cargo test --lib` 770 passed.
- 2026-05-17: P3 heap-known exception values now sync live targeted roots for pending/last/unwind exception state without disturbing global roots; full gates passed, including `cargo test --lib` 776 passed.
- 2026-05-17: P3 production value-store barriers, VM-owned API protection hooks, and weak/ephemeron policy contracts landed; full gates passed, including `cargo test --lib` 793 passed.
- 2026-05-17: P3 frame roots now distinguish known, no-target, and unknown targets so synthetic roots stay diagnostic while invalid known targets still fail heap validation; full gates passed, including `cargo test --lib` 795 passed.
- 2026-05-17: Final P3 memory-boundary audit completed; lazy intrinsic constructor/prototype installation now records owner-aware barriers and no-heap TypeError paths were removed; full gates passed, including `cargo test --lib` 796 passed.
- 2026-05-17: P4 first baseline frame/root ABI batch landed with data-only baseline ABI metadata, safepoint root-map lowering, interpreter fallback resume, and disabled generated-entry gating; full gates passed, including `cargo test --lib` 815 passed.
- 2026-05-17: P4 baseline frame/root ABI completed after ABI-proof audit; baseline entry eligibility now requires stable frame-header ordinals and a validated baseline ABI proof while generated-code entry remains disabled; full gates passed, including `cargo test --lib` 818 passed.
- 2026-05-17: P5 VM-owned baseline install transaction landed with CodeBlock registry/liveness, controlled baseline slot mutation, install/reject/defer/invalidate records, and interpreter-only execution after install; full gates passed, including `cargo test --lib` 829 passed.
- 2026-05-17: P5 opaque baseline bytecode eligibility proof integrated into VM install decisions; same-owner ABI and eligibility proofs are now required before baseline slot mutation, with full gates passed, including `cargo test --lib` 835 passed.
- 2026-05-17: P5 CodeBlock-derived baseline eligibility landed; VM install now derives proofs from registered CodeBlock bytecode/root-map/handler metadata and rejects unsupported bytecode before slot mutation; full gates passed, including `cargo test --lib` 841 passed.
- 2026-05-17: P6 foundation landed with a non-native typed baseline generated-code artifact contract and interpreter frame/register helper boundary; VM generated entry remains next, with full gates passed, including `cargo test --lib` 848 passed.
- 2026-05-17: P6 first generated-entry proof landed; VM tiering can enter a typed baseline body for constants/moves/int32 arithmetic and resume the interpreter on overflow fallback; full gates passed, including `cargo test --lib` 862 passed.
- 2026-05-17: P6 generated-entry hardening added VM-boundary differential coverage and CodeBlock snapshot fingerprints so typed baseline artifacts reject same-owner stale bytecode before execution; full gates passed, including `cargo test --lib` 868 passed.
- 2026-05-17: P6 generated fallback diagnostics now travel as typed executor results and VM-owned fallback records, and generated fallback throws sync exception roots after no-GC exit; full gates passed, including `cargo test --lib` 870 passed.
- 2026-05-17: P6 generated-effect contract now records no-heap-allocation/no-runtime-call/no-root-mutation eligibility for the current opcode slice, including the explicit register barrier-handoff caveat; full gates passed, including `cargo test --lib` 874 passed.
- 2026-05-17: P6 bitwise widening added typed generated int32 fast paths for bit-not, bitwise or/xor/and, and signed shifts under the generated-effect contract; full gates passed, including `cargo test --lib` 878 passed.
- 2026-05-17: P6 relational widening added typed generated int32 fast paths for less-than, less-equal, greater-than, and greater-equal comparisons under the generated-effect contract; full gates passed, including `cargo test --lib` 882 passed.
- 2026-05-17: P6 generated PC/jump handling landed for unconditional jump and nullish conditional jump, with taken-target validation and source-level nullish-coalescing coverage; full gates passed, including `cargo test --lib` 891 passed.
- 2026-05-17: P6 primitive-truthiness widening added typed generated `JumpIfFalse` for primitive local values with cell/unknown truthiness fallback diagnostics; full gates passed, including `cargo test --lib` 900 passed.
- 2026-05-17: P6 unsigned-shift result widening added typed generated `UnsignedRightShiftInt32` with Int32-or-Double primitive writes and existing non-int32 fallback; full gates passed, including `cargo test --lib` 901 passed.
- 2026-05-17: P6 primitive-boolean widening added typed generated `LogicalNot`, `StrictEqual`, and `StrictNotEqual` for primitive values with cell/unknown fallback diagnostics; full gates passed, including `cargo test --lib` 908 passed.
- 2026-05-17: P6 primitive-number widening added typed generated `LoadDouble`, numeric-only `NegateNumber`, `DivNumber`, and `ModNumber` with `NonNumberOperand` fallback; full gates passed, including `cargo test --lib` 918 passed.
- 2026-05-17: P6 primitive-coercion widening added generated `ToNumber`, `Void`, and primitive-coercing `NegateNumber`/`BitNotInt32` with typed cell/unknown coercion fallback; full gates passed, including `cargo test --lib` 927 passed.
- 2026-05-17: P6 pure-number binary widening added generated Int32/Double `AddInt32`/`SubInt32`/`MulInt32`, numeric relational, and binary bitwise `ToInt32` behavior while preserving old-subset int32 fallbacks; full gates passed, including `cargo test --lib` 936 passed.
- 2026-05-18: P6 generated runtime/heap boundary scaffolding landed with a typed planner helper/allocation contract and VM heap/root/no-GC boundary snapshots; full gates passed, including `cargo test --lib` 942 passed.
- 2026-05-18: P6 generated runtime/heap boundary consumption scaffold landed with executor helper-handoff resumability metadata and VM no-GC suspend/resume mechanics; full gates passed, including `cargo test --lib` 953 passed.
- 2026-05-18: P6 explicit VM-owned generated helper execution path landed for a planner-approved `NewObject` helper handoff, including no-GC suspend/re-entry, single-instruction interpreter dispatch, stale-plan rejection, and full gates with `cargo test --lib` 966 passed.
- 2026-05-18: P6 artifact-owned helper metadata now flows through VM-owned generated install and normal tiering entry for `NewObject`, with helper-aware eligibility, malformed-proof rejection, no-GC helper dispatch, and full gates with `cargo test --lib` 976 passed.
- 2026-05-18: P6 helper metadata is now planner-derived from registered `CodeBlock` root maps for `NewObject`; explicit helper plans must match the derived metadata, with full gates passing including `cargo test --lib` 985 passed.
- 2026-05-18: P6 destination-only allocation helper derivation now covers `NewObject` and `NewArray`, with VM-owned install/dispatch proof and full gates passing including `cargo test --lib` 994 passed.
- 2026-05-18: P6 operand-role helper metadata now covers runtime-helper `TypeOf` with destination/source root-slot derivation, VM-owned install/dispatch proof, and full gates passing including `cargo test --lib` 1014 passed.
- 2026-05-18: P6 `LoadString` foundation now stores bytecompiler-needed string literals on `UnlinkedCodeBlock`/`UnlinkedConstantPool`, prefers CodeBlock-owned literals during interpreter dispatch, and fingerprints the literal table, with full gates passing including `cargo test --lib` 1019 passed.
- 2026-05-18: P6 runtime-helper metadata now covers `LoadString` with CodeBlock-owned literal derivation, destination-root safepoint proofing, VM-owned generated helper dispatch, and stale literal-table rejection, with full gates passing including `cargo test --lib` 1033 passed.
- 2026-05-18: P6 runtime-helper metadata now covers `LoadBigInt` with CodeBlock-owned source-text literal derivation, destination-root safepoint proofing, VM-owned generated helper dispatch, and stale same-instruction/different-literal rejection, with full gates passing including `cargo test --lib` 1050 passed.
- 2026-05-18: P6 bytecompiler/generator output now produces exact helper root maps for `NewObject`, `NewArray`, `TypeOf`, `LoadString`, and `LoadBigInt`, with owner stamping at VM registration and full gates passing including `cargo test --lib` 1063 passed.
- 2026-05-18: P6 mixed CodeBlock install now allows generated/runtime-helper prefixes to hand off once to interpreter fallback sites while rejecting all-fallback, non-core, handler-metadata, and helper-without-proof blocks; full gates passed, including `cargo test --lib` 1067 passed.
- 2026-05-18: P6 may-throw runtime-helper handoff now carries explicit throw metadata through proof and generated handoff, rejects stale throw metadata, roots heap-thrown helper exceptions, and cleans helper roots; full gates passed, including `cargo test --lib` 1070 passed.
- 2026-05-18: P6 generated JS `Call` handoff now has a distinct non-fallback executor/VM path for `CoreOpcode::Call`, with no-GC suspension, return continuation, throw rooting/unwind, and callee/argument root cleanup proven; full gates passed, including `cargo test --lib` 1077 passed.
- 2026-05-18: P6 generated property handoff now has a distinct non-fallback executor/VM path for `GetByName`, with no-GC suspension, result continuation, getter-throw rooting/unwind, base-root cleanup, and other property opcodes unpromoted; full gates passed, including `cargo test --lib` 1082 passed.
- 2026-05-18: P6 `GetByName` property handoff sites now bind to CodeBlock-owned `PropertyLoad`/`GetById` IC metadata and reject stale, mismatched, or missing side-table cache entries; full gates passed, including `cargo test --lib` 1090 passed.
- 2026-05-18: P6 `GetByName` property handoff now carries a metadata-only cold `PropertyLoad` IC miss descriptor, rejecting duplicate/warmed bytecode IC state and malformed miss descriptors before VM dispatch; full gates passed, including `cargo test --lib` 1095 passed.
- 2026-05-18: P6 slow `GetByName` lookup now records local data-only lookup facts for property handoff observations without `DispatchHost`/VM persistence or IC mutation; full gates passed, including `cargo test --lib` 1102 passed.
- 2026-05-18: P6 property handoff observations now drain through a root-safe `DispatchHost` bridge into VM-owned data-only IC observation records, still blocked from attachment and without CodeBlock IC mutation; full gates passed, including `cargo test --lib` 1117 passed.
- 2026-05-18: P6 interpreter-local shape facts now give own-data `GetByName` observations valid `StructureId`/`PropertyOffset` metadata while prototype/miss/getter/indexed/proxy cases remain blocked; full gates passed, including `cargo test --lib` 1121 passed.
- 2026-05-18: P6 ready own-data `GetByName` observations now derive VM-owned data-only access-case plans with defensive base/holder validation and no CodeBlock IC attachment; full gates passed, including `cargo test --lib` 1125 passed.
- 2026-05-18: P6 generated property-load probe ABI landed for read-only own-data plan checks with explicit returned-cell root-sync metadata, still unwired from generated execution; full gates passed, including `cargo test --lib` 1129 passed.
- 2026-05-18: P6 VM-owned property-load plan tables now validate, dedupe repeated observations, and order data-only own-load candidates for future generated probes without IC attachment or generated execution; full gates passed, including `cargo test --lib` 1138 passed.
- 2026-05-18: P6 generated `GetByName` now has an executor-local opt-in property-load sidecar that probes VM-owned own-data plan candidates, continues on hits, preserves default handoff on misses/no sidecar, and records destination root-sync intent without VM root mutation; full gates passed, including `cargo test --lib` 1142 passed.
- 2026-05-18: P6 VM now wires generated `GetByName` own-data sidecar hits through validated plan tables with destination roots synced from current register state before fallback/handoff boundaries and cleaned afterward; full gates passed, including `cargo test --lib` 1146 passed.
- 2026-05-18: P6 VM-owned `GetByName` property-load observations and plans are now scoped by bytecode snapshot so generated sidecar tables ignore stale same-owner plans after CodeBlock/artifact replacement while preserving same-snapshot hits; full gates passed, including `cargo test --lib` 1148 passed.
- 2026-05-18: P6 generated `GetByName` property-load sidecars now report probe misses to VM-owned plan lifecycle state, preserving non-terminal polymorphic/transient misses while filtering terminal-invalid plans from future sidecar tables; full gates passed, including `cargo test --lib` 1151 passed.
- 2026-05-18: P6 prototype-data and own-missing `GetByName` observations now produce separate VM-owned metadata-only guard plans for future watchpoint integration while remaining blocked from access-case tables and generated sidecar execution; full gates passed, including `cargo test --lib` 1156 passed.
- 2026-05-18: P6 metadata-only `GetByName` guard plans now carry root-safe prototype/negative lookup chain certificates with per-entry structure/prototype proofs, reject index/symbol/private synthetic plan keys, and remain excluded from access-case tables and generated sidecar execution; full gates passed, including `cargo test --lib` 1169 passed.
- 2026-05-18: P6 `GetByName` guard certificates now derive VM-owned structure watchpoint dependency records for future install-time rechecks while keeping live watchpoint sets, code artifacts, access-case tables, and generated sidecars unchanged; full gates passed, including `cargo test --lib` 1176 passed.
- 2026-05-18: P6 VM-owned `GetByName` guard dependency metadata now has install-time recheck records that accept only coherent structure-transition dependency descriptors while leaving live watchpoints, IC install barriers, code artifacts, access-case tables, and generated sidecars untouched; full gates passed, including `cargo test --lib` 1184 passed.
- 2026-05-18: P6 VM-owned `GetByName` guard watchpoint materialization now records explicit live structure-watchpoint snapshots and `WouldStartWatching`/`AlreadyWatching` binding intent after accepted rechecks while leaving live mutation, invalidation dispatch, IC barriers, code artifacts, access-case tables, and generated sidecars untouched; full gates passed, including `cargo test --lib` 1196 passed.
- 2026-05-18: P6 VM-owned `GetByName` guard watchpoint invalidation now consumes data-only `WatchpointFireEvent`s, retires matching materializations and guard plans on advanced structure-transition generations, and rejects stale/reused guard metadata while leaving live watchpoint mutation, IC barriers, code artifacts, access-case tables, and generated sidecars untouched; full gates passed, including `cargo test --lib` 1203 passed.
- 2026-05-18: P6 VM-owned `GetByName` guarded property-load projection now derives non-executable prototype-data and negative-lookup candidate metadata from active accepted guard materializations while keeping own-data access-case tables, generated sidecars, IC barriers, code artifacts, bytecode ICs, and live watchpoints untouched; full gates passed, including `cargo test --lib` 1211 passed.
- 2026-05-18: P6 guarded `GetByName` property-load probe contract now has separate non-executable request/result/hit/miss metadata for future prototype-data and negative-lookup sidecars while leaving own-data plan tables, `DispatchHost`, baseline execution, VM wiring, IC mutation, watchpoints, barriers, and artifacts untouched; full gates passed, including `cargo test --lib` 1215 passed.
- 2026-05-18: P6 guarded `GetByName` host probe boundary now lets `DispatchHost`/`CoreOpcodeDispatchHost` validate prototype-data and terminal negative-lookup guard plans against live interpreter object state while leaving baseline sidecar execution, VM wiring, IC mutation, watchpoints, barriers, access-case tables, and artifacts untouched; full gates passed, including `cargo test --lib` 1219 passed.
- 2026-05-18: P6 guarded `GetByName` candidate tables now expose fresh VM-built, JIT-facing prototype-data and negative-lookup guard plan snapshots with provenance while leaving baseline execution, VM generated wiring, own-data access-case tables, IC mutation, watchpoints, barriers, and artifacts untouched; full gates passed, including `cargo test --lib` 1230 passed.
- 2026-05-18: P6 guarded `GetByName` miss telemetry now records generated guarded property-load probe misses with guard/materialization/dependency provenance and separate terminal classification while leaving guard retirement, materialization invalidation, own-data miss invalidation, baseline execution, IC mutation, watchpoints, barriers, and artifacts untouched; full gates passed, including `cargo test --lib` 1234 passed.
- 2026-05-18: P6 guarded `GetByName` sidecar execution now uses the existing baseline property-load sidecar with separate own-data and guarded tables, routes guarded hits through the guarded host probe and root-sync path, and records guarded misses into VM telemetry while leaving guard retirement, watchpoint invalidation, own-data invalidation policy, IC mutation, barriers, and artifacts untouched; full gates passed, including `cargo test --lib` 1242 passed.
- 2026-05-18: P6 guarded `GetByName` terminal miss policy now retires matching VM-owned guard plans with a distinct terminal-miss lifecycle and suppresses future guarded candidates while leaving watchpoint invalidation records, materialization lifecycle, own-data miss invalidation, baseline sidecar code, IC mutation, barriers, and artifacts untouched; full gates passed, including `cargo test --lib` 1245 passed.
- 2026-05-18: P6 generated single-dispatch exit now uses one VM-owned no-GC suspend/dispatch/exception-root-sync/resume transaction for JS-call, property, and runtime-helper handoffs, with `CallWithThis` admitted as the first widened non-construct call proof while keeping direct calls, constructors, property stores, IC attachment, live watchpoint mutation, and helper categories untouched; full gates passed, including `cargo test --lib` 1249 passed.
- 2026-05-18: P6 `PutByName` generated property handoff now derives store IC metadata, dispatches mutation through the shared interpreter-owned single-dispatch exit, and records VM-owned store observations with barrier-log evidence while keeping generated stores, store sidecars/probes, store access-case plans, cacheable store readiness, IC attachment mutation, live watchpoint mutation, and real structure-transition claims untouched; full gates passed, including `cargo test --lib` 1256 passed.
- 2026-05-18: P6 generated JS call handoff now records VM-owned metadata-only call observations for `Call`/`CallWithThis` after the shared interpreter-owned single-dispatch exit, including root-safe callee/this identity, target class, outcome, argument count, and bytecode snapshot while keeping direct generated calls, call-link attachment, constructor calls, polymorphic call caches, IC/code mutation, live watchpoint mutation, and generated artifact mutation untouched; full gates passed, including `cargo test --lib` 1260 passed.
- 2026-05-18: P6 generated baseline artifacts now have a VM-owned data-only invalidation lifecycle that records explicit, watchpoint, and terminal property-probe invalidations, filters invalidated artifacts from entry selection by artifact id and bytecode snapshot, and lets replacement artifacts become eligible again while keeping generated artifacts inspectable and leaving CodeBlock ICs, live watchpoints, code patching, generated stores, and call links untouched; full gates passed, including `cargo test --lib` 1266 passed.
- 2026-05-18: P6 property-store IC metadata now has JIT-owned data-only replace/transition access-case plan tables with explicit future barrier requirements, owner/snapshot-shape duplicate validation, and rejection of unsupported keys, calls, dependencies, malformed structures, generated store sidecars, VM plan emission, cacheable store claims, IC mutation, live watchpoints, and object/storage mutation; full gates passed, including `cargo test --lib` 1276 passed.
- 2026-05-18: P6 call-link readiness now records VM-owned metadata-only blocker records derived from accepted `Call`/`CallWithThis` observations, with stricter descriptor validation for this-binding, argument-count, root-safety, and blocked/opaque invariants while keeping bytecode-function calls blocked and leaving direct generated calls, `CallLinkInfoDescriptor` emission, call-link attachment, CodeBlock IC mutation, executable entry selection, artifacts, constructors, tail/varargs calls, and ABI correctness claims untouched; full gates passed, including `cargo test --lib` 1282 passed.
- 2026-05-18: P6 property-store VM metadata now derives owner/snapshot-scoped data-only replace/transition access-case plan records and validated plan tables from accepted `PutByName` observations with observed barrier evidence, preserving first repeated candidates while leaving store observations cacheability-disallowed and keeping generated stores, store sidecars/probes, CodeBlock IC mutation, live watchpoints, object/storage mutation beyond interpreter dispatch, artifact invalidation, and baseline entry selection untouched; full gates passed, including `cargo test --lib` 1287 passed.
- 2026-05-18: P6 property-store install recheck metadata now records VM-owned data-only barrier-readiness outcomes for store access-case plans, validating owner/snapshot/slot/shape/observation coherence and observed runtime barrier evidence while leaving generated stores, store sidecars/probes, cacheability policy, CodeBlock IC mutation, watchpoints, object/storage mutation, barrier execution, artifact invalidation, and baseline entry selection untouched; full gates passed, including `cargo test --lib` 1295 passed.
- 2026-05-18: P6 generated property-store probe ABI metadata now has JIT-visible request/hit/miss/result vocabulary for future `PutByName` store sidecars, preserving plan kind, key, stored value, base/new structure intent, offset, effect contract, and barrier effect while leaving VM telemetry, interpreter probing, baseline wiring, generated store execution, heap/storage/structure mutation, barrier execution, CodeBlock IC mutation, cacheability policy, and artifact state untouched; full gates passed, including `cargo test --lib` 1300 passed.
- 2026-05-18: P6 generated property-store probe readiness now has a read-only `DispatchHost`/interpreter host probe boundary and VM-owned record-only store-probe miss telemetry for coherent active replace/transition plans, preserving stored-value kind, owner/snapshot/slot/shape/offset/new-structure provenance, and non-terminal outcomes while leaving generated store sidecars, baseline wiring, heap/storage/structure mutation, barrier execution, root mutation, CodeBlock IC mutation, cacheability policy, plan lifecycle, artifact invalidation, and baseline entry selection untouched; full gates passed, including `cargo test --lib` 1310 passed.
- 2026-05-18: P6 generated property-store mutation authority boundary landed with JIT-visible request/commit/rejection vocabulary, VM-owned readiness telemetry requiring active store plans plus accepted barrier-evidence install rechecks, and an interpreter-owned `DispatchHost` commit hook that revalidates live replace/transition state, applies the existing value-store barrier before mutation, preserves replace shapes, and installs transition stores with the exact planned structure/offset while leaving generated store sidecar execution, baseline wiring, CodeBlock IC mutation, artifact invalidation, live watchpoint mutation, and baseline entry selection untouched; full gates passed, including `cargo test --lib` 1324 passed.
- 2026-05-18: P6 generated property-store mutation candidate projection ownership landed with JIT-owned immutable candidate/table types carrying store-plan, install-recheck, readiness, observation, stored-value-kind, and barrier-evidence provenance, and VM-owned projection from accepted readiness records only, deduping semantic repeats while keeping projection non-executable and leaving store sidecar execution, baseline wiring, CodeBlock IC mutation, live watchpoint mutation, object/storage mutation, artifact invalidation, and baseline entry selection untouched; full gates passed, including `cargo test --lib` 1334 passed.
- 2026-05-18: P6 generated `PutByName` property-store sidecar execution landed with a mixed load/store baseline sidecar sharing one `DispatchHost`, VM-projected mutation candidates, interpreter-owned probe-plus-commit mutation/barrier authority, VM-drained store probe-miss telemetry, and separate record-only mutation-rejection telemetry while leaving CodeBlock IC mutation, live watchpoint mutation, generated artifact invalidation, cache attachment, and baseline entry selection untouched; full gates passed, including `cargo test --lib` 1348 passed.
- 2026-05-18: P6 VM-owned property IC attachment boundary landed with CodeBlock-owned metadata-only own-data load and store replace/transition attachment, tiering-approved owner/snapshot/plan/readiness validation, attached-plan lifecycle records, and explicit rejection of watchpoint-required or unproven store-barrier cases while keeping generated sidecars probe/commit-only and leaving generated artifact invalidation, live watchpoints, native repatching, and baseline entry selection untouched; full gates passed, including `cargo test --lib` 1368 passed.
- 2026-05-18: P6 live structure-transition watchpoint bridge landed with interpreter-owned `WatchpointSetId` to `StructureId` registry, `DispatchHost` snapshot/start/drain APIs, old-structure fire events from shape/prototype/generated-store-transition mutations, and VM/tiering event dispatch through existing guard-materialization invalidation validation while keeping production auto-drain, guarded IC attachment, native patching, generated sidecar behavior, and broad `object::Structure` integration untouched; full gates passed, including `cargo test --lib` 1377 passed.
- 2026-05-18: P6 guarded `GetByName` property IC attachment landed with metadata-only prototype-data and negative-lookup bytecode attachment kinds, optional attachment offsets so negative lookup carries no fake slot, VM/tiering validation from accepted live watchpoint materializations and projected guarded candidates, attached guard-plan lifecycle records that still allow watchpoint invalidation dispatch, and no structure stub allocation, native repatching, generated sidecar behavior, or entry-selection changes; full gates passed, including `cargo test --lib` 1386 passed.
- 2026-05-18: P6 attached guarded property IC clearing landed with a VM-authorized CodeBlock metadata clear transaction, tiering-owned active/cleared attachment lifecycle records, provenance-checked clear requests derived only from accepted watchpoint invalidation dispatch, stale get-by-id guard metadata reset to unlinked/unset state, and no clearing by structure id, production auto-drain, native patching, generated sidecar changes, or unrelated own-data/store attachment mutation; full gates passed, including `cargo test --lib` 1391 passed.
- 2026-05-18: P6 attached property IC consumption landed with read-only CodeBlock metadata validation and VM registry projection of active attached IC records into generated sidecar candidate tables for own-data loads, guarded prototype/negative loads, and store replace/transition, preserving active-plan ordering and provenance while suppressing stale/cleared/mismatched metadata and leaving native stubs, repatching, direct calls, interpreter dispatch, and generated execution semantics untouched; full gates passed, including `cargo test --lib` 1397 passed.
- 2026-05-18: P6 production watchpoint-event drain landed at VM execute-code-block safepoints, dispatching pending structure-transition watchpoint fires before generated entry selection and after execution, clearing only matching active guarded attached IC metadata through the existing VM/tiering clear path, and leaving watchpoint materialization policy, native stubs, direct calls, GC/rooting, and generated execution semantics untouched; full gates passed, including `cargo test --lib` 1399 passed.
- 2026-05-18: P6 production guarded watchpoint materialization and metadata-only guarded IC attachment policy landed at VM execute-code-block safepoints, rechecking active guarded load plans, snapshotting/starting host structure-transition watchpoints, recording accepted materializations, attaching prototype-data and negative-lookup guarded IC metadata through existing CodeBlock/tiering validators, and suppressing duplicate materialization/attachment churn while leaving native stubs, direct calls, machine-code patching, and GC/rooting untouched; full gates passed, including `cargo test --lib` 1399 passed.
- 2026-05-18: P6 production non-guarded metadata-only property IC attachment policy landed at VM execute-code-block safepoints, attaching own-data load plans and deriving store replace/transition install recheck plus mutation-readiness evidence from source store observations before attaching through existing CodeBlock/tiering validators, with duplicate-attempt suppression and no native stubs, machine-code patching, direct calls, new GC/rooting behavior, or broader IC parity claims; full gates passed, including `cargo test --lib` 1399 passed.
- 2026-05-18: P6 own-data `GetByName` structure-stub metadata lifecycle landed with CodeBlock-authorized `StructureStubInfo` allocation, provenance validation, clear-time reset, and registry suppression from existing metadata-only sidecar projection while keeping guarded loads, stores, native code emission, executable memory, repatching, and direct calls untouched; full gates passed, including `cargo test --lib` 1402 passed.
- 2026-05-18: P6 descriptor-only own-data `GetByName` structure-stub projection and repatch transaction metadata landed with registry validation from real `StructureStubInfo`, VM/tiering transaction records, reserved `JitCodeArtifact`/`InlineCacheStub`/`CodePatchPlan` descriptors, and no production execution routing, native code emission, executable memory, applied patching, guarded/store stubs, or direct calls; full gates passed, including `cargo test --lib` 1410 passed.
- 2026-05-18: P6 VM-owned property IC attachment requests now carry explicit requested stub mode, preserving metadata-only production policy while allowing explicit own-data `GetByName` structure-stub attachment through VM/tiering validation and duplicate provenance; full gates passed, including `cargo test --lib` 1415 passed.
- 2026-05-18: P6 descriptor-only structure-stub reservation safepoint landed, projecting explicit own-data `GetByName` structure-stub attachments into one VM-owned reserved repatch transaction with duplicate suppression and tiering-owned descriptor code ids while keeping default production IC attachment metadata-only and leaving native code emission, executable memory, applied patching, generated sidecar routing, and baseline artifacts untouched; full gates passed, including `cargo test --lib` 1421 passed.
- 2026-05-18: P6 CodeBlock-owned structure-stub access-case linkage landed with VM-authorized idempotent `StructureStubInfo.access_cases` mutation from reserved descriptor transactions, tiering-visible link records, rejected-link no-mutation behavior, and clear-time access-case cleanup while keeping production IC attachment metadata-only and leaving native code emission, executable memory, applied patching, generated sidecar routing, and baseline artifacts untouched; full gates passed, including `cargo test --lib` 1422 passed.
- 2026-05-18: P6 descriptor-only structure-stub clear lifecycle landed, allowing only active own-data `GetByName` StructureStub attachments to clear without watchpoint provenance, retiring matching repatch transaction/access-case link/load-plan lifecycle records on accepted clears, preserving metadata-only and guarded clear boundaries, and leaving native code emission, executable memory, applied patching, generated routing, and baseline artifacts untouched; full gates passed, including `cargo test --lib` 1424 passed.
- 2026-05-19: P6 active StructureStub descriptor sidecar projection landed, feeding only current CodeBlock-confirmed, active linked, reserved own-data `GetByName` StructureStub descriptors into the generated property-load sidecar table while suppressing retired, rejected, stale, or missing-access-case metadata, preserving production metadata-only defaults, and leaving native code emission, executable memory, applied patching, generated code bodies, and baseline artifacts untouched; full gates passed, including `cargo test --lib` 1430 passed.
- 2026-05-19: P6 VM-owned baseline executable materialization boundary landed, requiring a prior accepted materialization record before native baseline install can mutate CodeBlock baseline slots or baseline entry artifacts, validating owner/artifact/native symbol/machine range/protection/lifecycle/snapshot/ABI provenance while keeping generated-code artifacts independent and leaving real executable allocation, link-buffer emission, native entry calls, and IC patch application untouched; full gates passed, including `cargo test --lib` 1435 passed.
- 2026-05-19: P6 data-only executable allocation/link-finalization ledger landed, adding a JIT-owned finalization proof layer for frozen assembler buffers, baseline link-buffer plans, allocation/code-ref/range/handle/native-symbol/protection/lifecycle/patch-plan consistency, and making VM baseline materialization require an accepted finalization record while leaving real executable allocation, cache flushing, native entry calls, generated baseline execution, and IC patch application untouched; full gates passed, including `cargo test --lib` 1442 passed.
- 2026-05-19: P6 platform-facing executable-memory residency proof layer landed, recording data-only allocation mapping, RW-to-RX protection transition, exact instruction-cache flush, and accepted residency validation below VM materialization while leaving syscalls, mmap/mprotect, cache flushing, executable pointers, native calls, generated baseline execution, and IC patch application untouched; full gates passed, including `cargo test --lib` 1448 passed.
- 2026-05-19: P6 data-only assembler byte-image copy/link proof landed, requiring accepted frozen source-image and LinkBuffer copy/link metadata before finalization while keeping platform residency and VM materialization as transitive consumers, with no real bytes, syscalls, native calls, generated baseline execution, or IC patch application; full gates passed, including `cargo test --lib` 1456 passed.
- 2026-05-19: P6 native baseline entry authorization metadata landed with opaque descriptor/token validation and VM readiness records proving resident installed artifacts are ABI-callable while native entry remains disabled and interpreter execution continues; full gates passed, including `cargo test --lib` 1460 passed.
- 2026-05-19: P6 VM-entry native baseline launch metadata landed with exact ready-disabled gate/readiness validation, symbolic dispatch token selection, active entry/top-frame/formal-argument metadata, and interpreter fallback recording while keeping native execution disabled; full gates passed, including `cargo test --lib` 1465 passed.
- 2026-05-19: P6 executable entry publication boundary landed with bytecode-owned symbolic baseline native publication records, call/construct separation, arity-check-only executable-slot caching, and validation against VM launch metadata plus installed baseline CodeBlock state while keeping native execution disabled; full gates passed, including `cargo test --lib` 1469 passed.
- 2026-05-19: P6 VM-to-executable publication wiring landed with CodeBlockRegistry-owned provisional executable entry slots, successful publication ledgers, VM launch-descriptor publication after ready-disabled native gates, and top-level unspecialized call-path acceptance while preserving interpreter fallback and arity-check-only executable caching; full gates passed, including `cargo test --lib` 1470 passed.
- 2026-05-19: P6 top-level executable ownership landed with `CellType::Executable`, VM-owned `ExecutableRegistry` script registration/install-code records, executable-first baseline native publication before CodeBlockRegistry fallback, and interpreter fallback preserved; full gates passed, including `cargo test --lib` 1473 passed.
- 2026-05-19: P6 nested function executable ownership landed with VM-allocated function executable/code-block cells, `ExecutableRegistry` function call-code install records, interpreter function dispatch using owned `CodeBlockId`s instead of synthetic IDs, and call semantics unchanged; full gates passed, including `cargo test --lib` 1475 passed.
- 2026-05-19: P6 bytecode call observations now carry known target executable/code-block identities from VM-owned function code blocks and call-link readiness no longer reports missing target metadata for those bytecode callees, while direct calls, call-link attachment, and generated call execution remain disabled/metadata-only; full gates passed, including `cargo test --lib` 1476 passed.
- 2026-05-19: P6 metadata-only call-link descriptor records landed from rooted returned bytecode `Call`/`CallWithThis` observations with known executable/code-block targets, recording `CallLinkInfoDescriptor` evidence while preserving direct-call, call-boundary, IC attachment, and generated call execution blockers; full gates passed, including `cargo test --lib` 1477 passed.
- 2026-05-19: P6 call-link boundary validation records landed with VM registry/liveness checks for executable, target code block, installed function call code, callee object, and symbolic `CallBoundaryMetadata`, producing accepted/rejected metadata-only evidence while preserving immutable descriptor records and direct generated call blockers; full gates passed, including `cargo test --lib` 1481 passed.
- 2026-05-19: P6 metadata-only call-link attachment planning landed with validated `CallLinkAttachmentPlan`/table records from accepted VM call-link boundary validations, preserving immutable descriptor evidence, bytecode IC side tables, direct-call blockers, generated call execution fallback, and rejected-validation no-plan behavior; full gates passed, including `cargo test --lib` 1486 passed.
- 2026-05-19: P6 call-link attachment pre-install recheck landed with VM-safepoint fresh executable/code-block/callee liveness evidence and tiering-owned install-recheck records that revalidate attachment plans, descriptor/readiness/observation provenance, boundary metadata, and direct-call blockers while still avoiding bytecode IC mutation and direct generated calls; full gates passed, including `cargo test --lib` 1492 passed.
- 2026-05-19: P6 metadata-only call-link IC attachment landed with linked CodeBlock call side-table derivation, VM/tiering attachment records from accepted install rechecks, and registry-owned mutation of only `inline_caches.calls`, while preserving direct-call blockers, native-stub absence, duplicate-attachment suppression, and generated handoff/interpreter dispatch; full gates passed, including `cargo test --lib` 1501 passed.
- 2026-05-19: P6 metadata-only call-link IC clear/invalidation landed with VM safepoint scanning of active attached call metadata, CodeBlockRegistry-authorized call IC reset to unlinked state, tiering clear records that retire attachment/plan/descriptor evidence, stale executable/code-block/callee target validation, and no native stubs or direct generated call dispatch; full gates passed, including `cargo test --lib` 1508 passed.
- 2026-05-19: P6 generated call-link candidate projection landed with immutable JIT-facing active call metadata tables from attached call-link ICs, registry/tiering/VM validation, direct-call-disallowed semantics, and build-only VM wiring that preserves generated JS-call handoff/interpreter dispatch; full gates passed, including `cargo test --lib` 1522 passed.
- 2026-05-19: P6 generated call-link sidecar telemetry landed with VM consumption of active call-link candidate tables, shared-host baseline/property sidecar probing, tiering-owned miss/blocked records, and preserved generated JS-call handoff/interpreter dispatch with direct calls still disallowed; full gates passed, including `cargo test --lib` 1537 passed.
- 2026-05-19: P6 guarded native baseline entry smoke path landed with sealed P6 pure baseline callable shim authority, enabled native readiness/gate/decision diagnostics, VM launch revalidation before native shim execution/publication, stale or unsupported fallback to interpreter, native fallback telemetry, and disabled-native metadata behavior preserved while emitted machine code, mmap/mprotect, direct JS calls, property/call sidecars, and native IC patching remain disabled; full gates passed, including `cargo test --lib` 1543 passed.
- 2026-05-19: P6 narrow byte-materialization contract landed with byte-bearing assembler images, linked no-relocation byte images, JIT copy/finalization byte evidence, and platform executable-memory byte-copy residency ordering from writable allocation through byte copy, RW-to-RX metadata, and cache-flush metadata while preserving descriptor-only compatibility and still avoiding mmap/mprotect, raw executable pointers, native calls, direct JS calls, sidecar native routing, and IC patch application; full gates passed, including `cargo test --lib` 1555 passed.
- 2026-05-19: P6 enabled native readiness now requires byte-evidenced link finalization and executable-memory residency at the VM readiness choke point, preserving descriptor-only disabled metadata compatibility while preventing descriptor-only materialization from reaching `NativeEntryReady`, launch publication, or native execution; full gates passed, including `cargo test --lib` 1556 passed.
- 2026-05-19: P6 VM/Heap no-GC authority bridge landed with generated/native no-GC regions mirrored into heap no-GC depth, helper handoff suspend/resume restoring heap authority, and heap semantic rejection of collection-triggering allocation, collection requests, weak processing, and finalizer processing during active no-GC; full gates passed, including `cargo test --lib` 1560 passed.
- 2026-05-19: P6 platform W^X executable-memory compartment landed with a safe allocate-writable/copy/protect-RX/release state machine, private Unix `mmap`/`mprotect`/`munmap` backend, opaque `MachineCodeHandle` production only after RX, and no VM/JIT native-execution wiring; full gates passed, including `cargo test --lib` 1564 passed.
- 2026-05-19: P6 platform-backed byte residency bridge landed, materializing linked bytes through the W^X compartment before recording allocation, byte-copy, RW-to-RX, cache-flush marker, and accepted byte-evidenced residency records while preserving descriptor-only compatibility and avoiding VM/JIT native-execution wiring; full gates passed, including `cargo test --lib` 1570 passed.
- 2026-05-19: P6 VM-owned platform-backed residency consumption landed with accepted baseline materializations retaining owned W^X compartment evidence in the VM, platform bridge errors leaving no VM materialization or evidence, tiering rejections dropping mapped evidence, descriptor-only disabled compatibility preserved, and enabled-native readiness still using sealed shim authority without raw pointer or direct machine-code calls; full gates passed, including `cargo test --lib` 1574 passed.
- 2026-05-19: P6 non-executable baseline machine-byte emission provenance landed with a sealed JIT-side record tying baseline entry metadata to bytecode eligibility proof, x86_64 P6 no-call/no-heap byte-shape, ABI, bytecode snapshot, linked-image digest/size/profile/state, machine-code handle metadata, and entry-offset validation while explicitly avoiding executable-readiness authority, platform calls, VM readiness, callable authority, or mapped-byte execution; full gates passed, including `cargo test --lib` 1583 passed.
- 2026-05-19: P6 VM/tiering-owned baseline emission provenance bridge landed as a record-only lane tying emission records to accepted VM materialization, native-entry metadata, bytecode snapshot, and link/residency byte evidence without changing readiness, entry selection, callable authority, platform calls, or mapped-byte execution; full gates passed, including `cargo test --lib` 1587 passed.
- 2026-05-19: P6 emitter-owned byte construction boundary landed in a separate non-executable `jit::emitter` module with an internally generated x86_64 no-relocation return-stub byte image feeding emission provenance, while explicitly remaining non-semantic, non-callable, and disconnected from VM readiness or platform execution; full gates passed, including `cargo test --lib` 1592 passed.
- 2026-05-19: P6 operand-aware x86_64 baseline lowering plan landed with owner-witness/proof/snapshot binding in `jit::plan`, record-only `jit::emitter` lowering for constants, moves, returns, and int32 arithmetic from real `CodeBlock` operands, explicit roots/handlers/stale-proof/bad-operand rejection, and no byte emission, executable readiness, VM mutation, or callable authority; full gates passed, including `cargo test --lib` 1603 passed.
- 2026-05-19: P6 x86_64 backend/value-frame contract landed as a record-only bridge from operand lowering to future bytes, binding Rust `JsValue` tag layout, logical `RegisterWindow` frame slots, symbolic baseline ABI roles, per-op operand locations, and int32 non-int32/overflow/negative-zero exit policy while keeping byte images, machine handles, JIT artifacts, VM materialization/readiness, and callable authority absent; full gates passed, including `cargo test --lib` 1609 passed.
- 2026-05-20: P6 symbolic x86_64 instruction selection accepted as the record-only bridge from backend contract to symbolic machine operations for constants, moves, returns, and int32 add/sub/mul, with canonical value/frame/ABI revalidation, bytecode order/count checks, operand-location rebinding, side-exit/negative-zero policy validation, tamper tests, and no byte emission, callable authority, artifacts, VM readiness, or platform execution; full gates passed, including `cargo test --lib` 1618 passed.
- 2026-05-20: P6 semantic x86_64 byte emission landed for accepted symbolic selection, deriving actual non-relocatable assembler/link-buffer byte images for immediate constants, local moves, final return, and int32 add/sub/mul with inline UD2 side-exit placeholders, terminal/physical-register/instruction-byte records, patched rel32 evidence, rejection of frame-argument and constant-memory sources, and no emission/finalization/native-readiness/callable authority; full gates passed, including `cargo test --lib` 1625 passed.
- 2026-05-20: P6 VM-owned semantic baseline materialization landed, deriving lowering/contract/selection/semantic bytes from the current CodeBlock proof, requiring linked-image byte evidence through link copy/finalization and platform W^X residency, installing only a disabled native entry with no callable authority, preserving interpreter fallback, and rejecting unsupported bytecode, stale snapshots, descriptor-only links, tampered bytes, and artifact-size drift before side effects; full gates passed, including `cargo test --lib` 1630 passed.
- 2026-05-20: P6 callable-byte/platform substrate landed for P3b, adding non-authoritative C-ABI-shaped semantic x86_64 bytes with prologue/epilogue and sentinel side-exit return stubs plus a sealed platform W^X call helper that keeps executable addresses private and rejects non-RX/released/out-of-range calls; VM readiness/frame/fallback wiring remains open; full gates passed, including `cargo test --lib` 1638 passed.
- 2026-05-20: P3b narrow emitted native entry landed end to end for the accepted P6 no-call/no-heap subset, preserving the disabled-only semantic materialization path while adding exact P6 callable readiness, guarded register frame-base borrowing, platform-compartment execution, sentinel side-exit fallback, descriptor-only non-publication, stale/unsupported fallback, and interpreter-vs-native differential coverage; full gates passed, including `cargo test --lib` 1651 passed.
- 2026-05-20: P4a frame and side-exit alignment landed with active register-window authority, raw native frame-base validation, reserved-tag emitted side-exit payload returns, VM-owned payload tables keyed by readiness/artifact/snapshot identity, precise side-exit-PC interpreter fallback, and overflow side-exit telemetry; full gates passed, including `cargo test --lib` 1657 passed.
- 2026-05-20: P4b reference-interpreter call/return continuation spine landed for ordinary `Call` and `CallWithThis`, adding explicit continuation records, centralized call-frame slot layout, attached callee-frame return metadata, single pop/validate/write return completion, generated-call handoff continuation metadata, and VM-side rejection of forged destination/resume/argument/callee fields; full gates passed, including `cargo test --lib` 1664 passed.
- 2026-05-20: P4c generated runtime-helper exit transaction hardening landed, adding VM-side handoff revalidation against current CodeBlock-derived helper proofs, exact root-map-filtered temporary VM register roots, no-GC suspend/resume across helper return/fail/throw/reject paths, disallowed-throw exception rollback, and stale/forged helper metadata rejection without dispatch; full gates passed, including `cargo test --lib` 1668 passed.
- 2026-05-20: P4d generated property handoff boundary hardening landed, adding VM-side active-frame/registered-snapshot/installed-artifact/derived-site validation, explicit no-GC and may-throw parity checks, exact property-load destination-root target validation, and stale/forged property metadata rejection before dispatch; full gates passed, including `cargo test --lib` 1673 passed.
- 2026-05-20: P4e exception and native side-exit fallback cleanup landed, completing handler-consumed unwind state, preventing generated single-dispatch failures from leaving masked pending exceptions or leaked exception roots, and rejecting unmatched native side-exit payloads without imprecise interpreter replay; full gates passed, including `cargo test --lib` 1677 passed.
- 2026-05-20: P5a VM-owned generated JS direct-call boundary landed for ordinary monomorphic bytecode `Call`/`CallWithThis`, authorizing attached call-link sidecar hits only as VM transaction requests, revalidating caller frame, CodeBlock snapshot, candidate table, target executable/CodeBlock/callee, boundary, `this`, arguments, destination, and resume PC before dispatch, suspending generated no-GC during callee execution, completing return through the continuation spine, propagating throws with exception roots, and rejecting forged direct-call metadata; full gates passed, including `cargo test --lib -- --quiet` 1683 passed.
- 2026-05-20: P6 emitted-native differential matrix landed for the current x86_64 subset, proving interpreter-vs-native equivalence for constants, moves, return, `AddInt32`, `SubInt32`, and `MulInt32`, plus precise VM fallback coverage for overflow, non-int32 operands, negative zero, unsupported-bytecode install rejection, stale snapshot fallback, and launch/readiness side-effect guards before widening; full gates passed, including `cargo test --lib -- --quiet` 1686 passed.
- 2026-05-20: P7a VM-retained runtime-helper native-exit authority landed, separating helper payload sites from arithmetic side exits and routing opaque retained helper payloads through the existing VM runtime-helper transaction with active-dispatch owner/frame authority, root/no-GC/exception validation, stale/forged metadata rejection, and synthetic `NewObject` coverage; full gates passed, including `cargo test --lib -- --quiet` 1689 passed.
- 2026-05-20: P7b real emitted-native `NewObject` helper exit landed, deriving the existing helper plan during callable native materialization, lowering `NewObject` to an opaque helper payload return, retaining helper-site metadata through VM readiness/artifact/snapshot tables, dispatching through the existing runtime-helper transaction, and preserving no-GC/root/exception cleanup without arithmetic fallback telemetry; full gates passed, including `cargo test --lib -- --quiet` 1692 passed.
- 2026-05-20: P7c emitted-native runtime-helper exit matrix landed for `NewArray`, `LoadString`, `LoadBigInt`, and `TypeOf`, preserving helper-only payload returns, literal snapshot binding, exact destination/source root filtering, stale metadata rejection, and no arithmetic fallback telemetry; full gates passed, including `cargo test --lib -- --quiet` 1696 passed.
- 2026-05-20: P8a emitted-native forward control flow landed for `Jump` and `JumpIfNotNullish`, adding proof-owned forward branch validation, separate bytecode-branch rel32 patch records, shared normal-return handling, native/interpreter taken-fallthrough coverage, and stale/tampered target rejection without side-exit/helper authority drift; full gates passed, including `cargo test --lib -- --quiet` 1705 passed.
- 2026-05-20: P8b emitted-native primitive truthiness control flow landed for `JumpIfFalse`, deciding undefined/null/bool/int32 in native, side-exiting double/cell/unknown truthiness at the branch PC with `UnsupportedTruthinessOperand` fallback evidence, and preserving P8a branch validation plus P7 helper boundaries; full gates passed, including `cargo test --lib -- --quiet` 1713 passed.
- 2026-05-20: P9 emitted-native JS-call exits landed for ordinary `Call` and `CallWithThis`, returning a distinct VM-owned `0xfe` payload, revalidating retained call-site metadata against the active CodeBlock/frame/register state, observing/attaching call-link metadata through the precise generated handoff path, and routing authorized monomorphic calls through the existing VM-owned generated direct-call transaction with no-GC/root/exception cleanup; full gates passed, including `cargo test --lib -- --quiet` 1721 passed.
- 2026-05-20: P10 emitted-native `GetByName` property exits landed as a distinct VM-owned `0xfd` payload, retaining exact property handoff metadata plus destination/base operands, revalidating active frame/CodeBlock/snapshot/site metadata before dispatch, and routing through the existing property handoff transaction to record property-load observations without native lookup or IC mutation; full gates passed, including `cargo test --lib -- --quiet` 1726 passed.
- 2026-05-20: P11 emitted-native `GetByName` property-load sidecar probes landed, projecting current VM-owned own-data and guarded candidate tables at native-exit dispatch time, reusing the generated property-load sidecar probe helper, syncing cell destination roots through the VM, recording existing miss lifecycle metadata, and falling back to the P10 handoff path on miss/no candidate; full gates passed, including `cargo test --lib -- --quiet` 1733 passed.
- 2026-05-20: P12 emitted-native `PutByName` property-store exits landed in the existing `0xfd` property-native namespace with opcode-specific retained operands, routing stores through the VM-owned property handoff transaction, recording store observations/plans and barrier/throw evidence, rejecting forged metadata, and avoiding native store sidecar mutation; full gates passed, including `cargo test --lib -- --quiet` 1740 passed.
- 2026-05-20: P13 emitted-native `PutByName` property-store sidecar hits landed, reusing VM-projected store mutation candidates and host-owned probe/commit authority for replace/transition stores, recording miss/rejection telemetry before P12 fallback, rejecting forged metadata before probing, and preserving no native store/IC mutation authority; full gates passed, including `cargo test --lib -- --quiet` 1745 passed.
- 2026-05-20: P14 emitted-native loop backedges landed with exact VM-derived backedge safepoint authority, retained `0xfc` loop payloads, VM validation/accounting/re-entry, native backedge budget enforcement, direction-neutral branch metadata, and P8-P14 regression coverage; full gates passed, including `cargo test --lib -- --quiet` 1751 passed.
- 2026-05-20: P15 tiering-selected baseline auto-materialization landed at interpreter entry, splitting plan selection from decision commit, reusing the VM-owned native install transaction, entering native in the same execution after readiness recomputation, preserving compile-miss fallback for unsupported/stale code, and blocking loop-backedge auto-install; full gates passed, including `cargo test --lib -- --quiet` 1758 passed.
- 2026-05-20: P16a generated/P9 direct-call nested callee entry landed, factoring VM entry execution by entry kind so authorized direct bytecode calls can P15 auto-install and enter the accepted native tier as `Function` entries while preserving continuation cleanup and forged-metadata rejection; full gates passed, including `cargo test --lib -- --quiet` 1759 passed.
- 2026-05-20: P16b ordinary interpreter-call nested callee entry landed with typed interpreter deferral for VM-owned execution, direct public/raw interpreter behavior preserved, validated registered function callees routed through the nested `Function` tiering/P15-native spine, direct fallback for unvalidated callees, continuation cleanup, and no-GC/root cleanup; full gates passed, including `cargo test --lib -- --quiet` 1763 passed.
- 2026-05-20: P16c non-direct generated/P9 JS-call nested callee entry landed with deferring generated single-dispatch for ordinary bytecode calls, observation draining before nested entry, real post-callee call-link outcomes, VM-owned `Function` entry for validated callees, direct fallback for unvalidated callees, and no-GC/root/frame cleanup; full gates passed, including `cargo test --lib -- --quiet` 1766 passed.
- 2026-05-20: P17a function-value tail-call nested callee entry landed with distinct typed function-value call requests, VM-owned nested `Function` entry for validated `Function.prototype.call` and `Reflect.apply` bytecode callees, direct fallback for unregistered callees, throw/no-GC cleanup, and continuation-heavy runtime callers left direct; full gates passed, including `cargo test --lib -- --quiet` 1770 passed.
- 2026-05-20: P17b function-value property-operation continuations landed for accessors, setters, selected proxy traps, Reflect get/set/has/delete composition, proxy no-trap forwarding, generated property observation finalization, and direct fallback for unregistered callees; full gates passed, including `cargo test --lib -- --quiet` 1781 passed.
- 2026-05-20: P18a ordinary bytecode constructor nested entry landed with distinct `Construct` entry authority, construct-specialized CodeBlock validation, constructor return normalization, direct fallback for unsupported/native/proxy/derived cases, and no-GC/frame/root cleanup; full gates passed, including `cargo test --lib -- --quiet` 1788 passed.
- 2026-05-20: Rewrite scheduler retargeted to full JetStream 3 Octane correctness and JavaScriptCore-level performance, starting from an Octane-core BFS dependency plan.
- 2026-05-20: P18b derived and super constructor nested entry accepted; explicit `super()` and default-derived forwarding now route registered bytecode super constructors through VM-owned `Construct` entry, with nested default-derived chain, object-returning `super()` plus derived field initialization, and throwing `super()` cleanup coverage; full gates passed including `cargo test --lib -- --quiet` 1791 passed.
- 2026-05-20: M1 Octane target and runner contract accepted: local `JetStreamDriver.js` Octane group is the benchmark source, the stale `Octane/run.js` path is rejected, and the first Rust runner is a synchronous `DefaultBenchmark`-equivalent path before official browser/async harness work.
- 2026-05-20: M2 prerequisite slice accepted: source sessions share one VM global/root and dispatch host, update and compound expressions plus conditionals and loose-equality subset execute, and full gates passed including `cargo test --lib -- --quiet` 1804 passed.
- 2026-05-20: M2 global-binding slice accepted: source sessions expose top-level `function`/`var` and declared host names through a real shared global object, with full gates passed including `cargo test --lib -- --quiet` 1809 passed.
- 2026-05-20: M2 file-source slice accepted: shell loaded-source records assign `SourceProviderId`/`SourceOriginId`, bytecompiler provenance reaches `SourceProvenance`, incremental source sessions preserve one global/host across appends, and full gates passed including `cargo test --lib -- --quiet` 1813 passed.
- 2026-05-20: M3a canonical-Math global slice accepted: source sessions install benchmark-visible `Math` on the shared global, bytecompiler resolves standard `Math` through global bindings, Math property mutations persist across batch/incremental loads, and full gates passed including `cargo test --lib -- --quiet` 1818 passed.
- 2026-05-20: M3b canonical-Math runtime slice accepted: `Math.floor`, `Math.sqrt`, `Math.log`, `Math.random`, and `Math.LN2` are installed on session-global `Math`, override persistence is tested across loaded sources, and full gates passed including `cargo test --lib -- --quiet` 1819 passed.
- 2026-05-20: M3c String/global runtime slice accepted: `String.prototype.charCodeAt`, `String.prototype.substring`, `String.fromCharCode`, and global `parseInt` are implemented, `parseInt` is a session-global property with cross-load override coverage, and full gates passed including `cargo test --lib -- --quiet` 1824 passed.
- 2026-05-20: M3d1 safe shell-host global slice accepted: opt-in `performance.now`, read-only `readFile`, `print`, `alert`, and `console.log/info/warn/error` are bytecompiler-visible and runtime-installed on source-session globals, host output is captured, and full gates passed including `cargo test --lib -- --quiet` 1828 passed.
- 2026-05-20: Scheduler moved from M3d1 to M4 runner integration: VM-owned `load(path)` remains designed but deferred because the accepted JetStream 3 Octane-core path can use runner-side file loading and read-only `readFile`.
- 2026-05-20: Octane goal plan refreshed: M3 is now marked accepted, M4 runner integration is the active BFS batch, and the durable `/goal` text points at full JetStream 3 Octane correctness and local C++ JSC-level performance.
- 2026-05-20: M4a Octane manifest/scoring scaffold accepted under `shell::octane`; full gates passed including `cargo test --lib -- --quiet` 1833 passed.
- 2026-05-20: M4b non-executing Octane preparation accepted: runner inputs now load through shell provenance, generated prelude/random/runner sources are prepared, and full gates passed including `cargo test --lib -- --quiet` 1840 passed.
- 2026-05-20: M4c prepared Octane execution/classification accepted: source-session runner execution now reports parse/bytecode/runtime/thrown/telemetry phases, with full gates passing including `cargo test --lib -- --quiet` 1845 passed.
- 2026-05-21: Scheduler refined for the full Octane parity goal: finish Octane-core correctness breadth first, then Octane-core JIT performance, full-Octane correctness breadth, official/comparable scoring, and C++ JSC-level performance closure.
- 2026-05-21: M4d source-session global lexical/class declarations accepted: top-level `let`/`const`/`class` now persist across Octane runner sources without leaking to the global object, with TDZ/read-only/duplicate-conflict coverage and full gates passing including `cargo test --lib -- --quiet` 1857 passed.
- 2026-05-21: M4e1 do-while support accepted: AST/parser/bytecompiler lowering now handles `do while` first-iteration semantics plus `break`/`continue`/`finally`, the Octane do-while blocker gets past parse/lowering, and full gates passed including `cargo test --lib -- --quiet` 1864 passed.
- 2026-05-21: M4e2 switch support accepted: AST/parser/bytecompiler lowering now handles strict case matching, default/fallthrough, case-test order, duplicate-default rejection, switch-local break targets, and loop continue from inside switch; full gates passed including `cargo test --lib -- --quiet` 1871 passed.
- 2026-05-21: M4e3 non-tagged template literals accepted: parser/lexer resume template tails, bytecompiler lowers substitutions through `ToString`, object `toString` interpolation is covered, and full gates passed including `cargo test --lib -- --quiet` 1883 passed.
- 2026-05-21: M4f Octane runner scoring accepted: reserved result telemetry now yields typed benchmark success records, per-benchmark and suite scores, oracle-alert failures, and interpreter-vs-baseline comparison records; full gates passed including `cargo test --lib -- --quiet` 1888 passed.
- 2026-05-21: M4g precursor accepted: non-decimal number literals, trailing call/new argument commas, global/host function capture cells, and sloppy top-level global assignment reads clear the known Octane-core parse/bytecode blockers; full gates passed including `cargo test --lib -- --quiet` 1899 passed.
- 2026-05-21: Octane scheduler refreshed: the active proof target is full JetStream 3 Octane at local C++ JSC-level performance, with M4g now focused on shared runtime blockers before benchmark-specific debugging.
- 2026-05-21: A0 JSC fidelity audit/fix accepted for register windows, capture ordering, ordinary fallthrough, and derived-constructor `this`/`super`/return semantics; full gates passed including `cargo test --lib -- --quiet` 1914 passed.
- 2026-05-21: A1/A2 live global-resolution audit/fix accepted: object-backed standard, host, and source globals now resolve through real session-global properties instead of closure snapshots; full gates passed including `cargo test --lib -- --quiet` 1917 passed.
- 2026-05-21: A1/A2 function metadata and call-vs-construct fidelity accepted: source functions now carry JSC parse/constructor/strict metadata, class constructors reject direct calls, methods and nonconstructable natives reject `new`, and sloppy nullish `this` normalizes to the session global object; full gates passed including `cargo test --lib -- --quiet` 1922 passed.
- 2026-05-21: A1 Program/Eval completion fidelity accepted: source execution now uses a JSC-shaped shared completion destination instead of syntactic last-statement selection, preserving completion through declarations, branches, loops, switch, catch, and finally; full gates passed including `cargo test --lib -- --quiet` 1928 passed.
- 2026-05-21: A1/A2 top-level-return and source-session-ordering fidelity accepted: Program/Eval now reject top-level `return`, the Octane runner uses legal final-expression completion, and batch source sessions execute sources sequentially through the incremental shell-shaped path; full gates passed including `cargo test --lib -- --quiet` 1932 passed.
- 2026-05-22: A2 named-function self-binding fidelity accepted: a function body's own name now binds to the active callee object instead of allocating a fresh function, preserving declaration/expression identity and Octane DeltaBlue-style `superConstructor` properties; focused DeltaBlue `chainTest(1); projectionTest(1)` from the real source returns successfully, while full DeltaBlue remains an interpreter-throughput/remaining-core target; full gates passed including `cargo test --lib -- --quiet` 1935 passed.
- 2026-05-22: A2 dynamic unresolved-global read fidelity accepted: unresolved identifier reads now lower to a runtime global-object resolve instead of bytecode-emission rejection, executed misses throw catchable `ReferenceError`, and the prior Octane Crypto `DV` bytecode blocker now runs until the diagnostic timeout; full gates passed including `cargo test --lib -- --quiet` 1936 passed.
- 2026-05-22: A2 JetStream suite topology accepted: prepared Octane suite execution now uses JetStream's reverse case-insensitive benchmark order, one shared VM per suite/mode run, and retained per-benchmark source sessions so fresh globals do not recycle interpreter object payloads inside the suite VM; full gates passed including `cargo test --lib -- --quiet` 1937 passed.
- 2026-05-22: A2 JetStream result transport accepted: benchmark globals now expose `top.currentResolve/currentReject`, generated Octane runners resolve result arrays through an uncapped host result sink instead of print telemetry, and reserved print telemetry remains diagnostic-only; full gates passed including `cargo test --lib -- --quiet` 1938 passed.
- 2026-05-22: A2 indexed-access infrastructure accepted: interpreter object lookup now uses a payload-to-object index instead of a linear scan, and `GetByValue`/`PutByValue` plus indexed op fast paths record dense element lookup/store observations for future ElementLoad/ElementStore handoff work without changing visible JS behavior; full gates passed including `cargo test --lib -- --quiet` 1941 passed, while the short DeltaBlue probe still times out at 30s.
- 2026-05-22: D0 by-value IC metadata prerequisite accepted: linked code blocks now derive `GetByValue`/`PutByValue` property inline-cache side-table entries using JSC-shaped by-val access kinds and runtime key registers, preserving dynamic-key identity for the next metadata-only ElementLoad/ElementStore handoff slice; full gates passed including `cargo test --lib -- --quiet` 1943 passed.
- 2026-05-22: D0 metadata-only by-value property handoff accepted: generated property handoff sites now preserve `PropertyCacheKey::RuntimeValue` for `GetByValue`/`PutByValue`, classify them as `ElementLoad`/`ElementStore` cold slow-path handoffs, and drain only proven indexed runtime observations to concrete index cache keys while named sidecar probing stays named-only; full gates passed including `cargo test --lib -- --quiet` 1945 passed.
- 2026-05-22: C0/JSC dispatch-budget fidelity accepted: normal VM/source-session dispatch now defaults to unbounded execution like C++ JSC's trap/watchdog model, finite step budgets are explicit harness policy, and Octane threads that budget through source-session appends plus nested call/construct re-entry so `DispatchStepLimitExceeded` no longer appears as a benchmark semantic; full gates passed including `cargo test --lib -- --quiet` 1947 passed, while short Navier-Stokes now reaches the external timeout instead of the VM step-limit failure.
- 2026-05-22: C0 rooting diagnostics/progress accepted: Octane probe progress now reports source-session/source/runner boundaries, VM frame/register root snapshots validate only the roots needed for each targeted sync, and dynamic frame/register root IDs moved into reserved high ranges; full gates passed including `cargo test --lib -- --quiet` 1952 passed. The Richards `runRichards(); runRichards();` reproducer now returns, and short Richards/DeltaBlue runners advance to external timeout instead of duplicate-root failure.
- 2026-05-22: C0/C1 Octane tiering diagnostics accepted: benchmark execution reports now carry per-benchmark tiering deltas for entry decisions, fallbacks, baseline install/materialization/readiness, native launches, call-link activity, and property IC activity, and `octane_probe --tiering-summary` prints the same counts for real benchmark runs. C++/Rust audits confirmed `iterations=2,worst=1` means 100 Richards simulations and 40 DeltaBlue runs, so current short-run timeouts are throughput pressure; release probes show simple Function/Construct entries and direct Richards select baseline but fall back with missing artifacts and zero installs/launches. Full gates passed including `cargo test --lib -- --quiet` 1952 passed; next shared boundary is widening baseline/IC coverage for constructor, property, and indexed hot paths.
- 2026-05-22: D1 by-value property native-exit admission accepted: P10 callable property exits now carry `GetByValue`/`PutByValue` operands, lower by-value property sites as payload-return native exits, and validate/resume them through the existing generated property handoff while named sidecars remain by-name-only and indexed observations stay unattachable until element sidecars exist. C++ audit tied this to JSC baseline `JITGetByValGenerator`/`JITPutByValGenerator`; full gates passed including `cargo test --lib -- --quiet` 1956 passed, and the source-level indexed-loop probe still falls back at missing artifact materialization rather than by-value lowering.
- 2026-05-22: D2 ordinary construct JS-call native-exit admission accepted: `Construct` now derives construct-specialized call-link side-table metadata, classifies as a generated JS-call handoff, lowers through the P9 native-exit payload path, validates a construct-specific continuation instead of reusing call return state, and resumes through the existing VM ordinary bytecode construct entry/return-normalization spine while generated direct construct calls and construct call-link observation/attachment remain deferred. C++ audit tied this to JSC's shared call/construct `CallLinkInfo` and LLInt/Baseline call-op structure; full gates passed including `cargo test --lib -- --quiet` 1957 passed and `cargo build --release --example octane_probe`.
- 2026-05-22: D3 P15 generated-artifact source-entry fallback accepted: selected baseline entries now record native-first auto-materialization attempts, preserve pure P6 native entry, and install gate-consumable generated baseline artifacts after native compile misses for mixed source; source-linked CodeBlocks store the owner-stamped root-map snapshot used by the registry. Release probes show `6 * 7` entering native, indexed array source entering generated baseline with zero fallbacks, and global `var` source entering generated baseline before the current `PutGlobalObjectProperty` fallback. Full gates passed including `cargo test --lib -- --quiet` 1959 passed and `cargo build --release --example octane_probe`; element sidecars remain the next shared dependency.
- 2026-05-22: D4 dense ElementLoad sidecar accepted: C++ audits tied the slice to JSC `JITGetByValGenerator`, `tryCacheArrayGetByVal`, and DataIC indexed-load handlers; Rust now projects proven dense-array `GetByValue` observations into `DataOnlyIndexedLoad` plans, generated/native sidecars reread the live by-value key before host probing, hits avoid interpreter `GetByValue`, and returned cells use the existing destination-root sync path. ElementStore remains deferred for barrier/length-growth policy. Full gates passed including `cargo test --lib -- --quiet` 1963 passed and `cargo build --release --example octane_probe`.
- 2026-05-22: D5 dense ElementStore replacement sidecar accepted: C++ audits tied the slice to JSC `JITPutByValGenerator`, `tryCacheArrayPutByVal`, indexed `AccessCase` store handlers, and the owner write-barrier requirement; Rust now projects proven existing dense-array `PutByValue` observations into `DataOnlyIndexedStore` plans, generated/native sidecars reread the live by-value key before host probing, and host-owned probe/commit revalidates array kind, structure, in-bounds non-hole element, and value-store barrier before mutating. Length extension, hole fill, COW, typed arrays, sparse storage, and generated raw stores remain deferred. Full gates passed including `cargo test --lib -- --quiet` 1971 passed and `cargo build --release --example octane_probe`.
- 2026-05-22: D6 interpreter-origin call-link observation accepted: C++ audits tied the slice to JSC `CallLinkInfo`, `JITCall`, `Repatch` Init/monomorphic behavior, and weak/liveness clearing constraints; Rust now drains ordinary `Call`/`CallWithThis` observations from interpreter bytecode-call requests, retargets the outcome after callee completion, feeds the existing metadata-only call-link boundary/attachment path, and suppresses duplicate in-flight plans for the same owner/snapshot/site/slot. Direct generated dispatch remains call-only; construct metadata shape, exact C++ Init two-sighting semantics, polymorphic calls, and source-entry baseline eligibility remain deferred. A reduced Richards eval probe now reports `call_observations=20` with one boundary/plan/recheck instead of zero observations. Full gates passed including `cargo test --lib -- --quiet` 1971 passed and `cargo build --release --example octane_probe`.
- 2026-05-22: D7 global object property-store handoff/sidecar accepted: C++ audits tied the slice to JSC `op_put_to_scope`, `PutGlobalObjectProperty`, and global-property put IC rules where first-property creation is not cached; Rust now derives `PutGlobalObjectProperty` store IC metadata, validates two-operand retained native exits, resolves the active Program/Eval global object as the implicit base, records opaque creation observations without plans, and admits only existing global data-property replacement into `DataOnlyReplace` sidecars. Full gates passed including `cargo test --lib -- --quiet` 1975 passed and `cargo build --release --example octane_probe`; the original global-store loop probe now enters generated baseline with zero `UnsupportedOpcode` fallbacks and records the expected opaque creation observation.
- 2026-05-22: D8 variable property-handoff metadata accepted: C++ audits confirmed JSC has no small CodeBlock-wide property IC site cap, only per-site polymorphic limits, so Rust replaced the fixed 16-entry generated property-handoff table with variable-length CodeBlock-owned site metadata while retaining snapshot, sort, duplicate-site, and per-site cache validation. The reduced Richards `runRichards(); runRichards();` probe now auto-installs and enters a generated baseline artifact with 91 inline-cache sites instead of rejecting with `BaselineGeneratedPropertyHandoffPlanTooManySites`; the full short Richards runner still times out in runner execution, so the next blocker is throughput/call-link fallback behavior rather than property metadata capacity. Full gates passed including `cargo test --lib -- --quiet` 1976 passed and `cargo build --release --example octane_probe`.
- 2026-05-22: D9 generated slow-path continuation accepted: C++/Rust audits confirmed generated JS-call slow paths should return to baseline code, while call-link direct dispatch remains VM-owned rather than native-patched. Rust generated call/property/runtime helper handoffs now resume the caller in generated code when the continuation can proceed, refresh property/call-link IC attachments at the generated handoff boundary, and rebuild the full generated sidecar envelope on resume; same-execution loop proofs cover call-link direct sidecars and property-load sidecars. Full gates passed including `cargo test --lib -- --quiet` 1978 passed and `cargo build --release --example octane_probe`; reduced Richards still completes with one generated fallback, and the short Richards runner still times out in `Generated(Runner)`, leaving direct/native call throughput and remaining cold runner sites as the next D-layer target.
- 2026-05-22: D10 same-execution call-link/property resume slice accepted: C++ audit kept the target aligned with baseline `CallLinkInfo` guarded monomorphic hits rather than DFG direct-call machinery, and Richards probes identified the hot repeated call as owner `CodeBlockId(CellId(43))` bytecode index 212 / packed 848 targeting `CodeBlockId(CellId(45))`. Rust now rebuilds the full call-link/property sidecar envelope after generated runtime-helper resumes, continues selected single-op generated fallbacks (`LoadFunction`, `LoadCallee`, `InitializeGlobalLexical`) back into generated code, and accepts warmed property IC metadata for artifact-backed generated property handoffs plus retained P10 native property exits while unauthorised cold handoffs still require unlinked IC metadata. Full gates passed including `cargo test --lib -- --quiet` 1980 passed and `cargo build --release --example octane_probe`; release probes show the minimal repeated-call loop returning with one call-link attachment recheck, one `runRichards()` returning with property plans and one call-link recheck, two `runRichards()` calls returning, and five `runRichards()` calls hitting the 30s cap, leaving native/direct call throughput as the next D-layer target.
- 2026-05-22: D11 Richards generated-baseline audit accepted: `octane_probe --tiering-summary` can now filter and widen tail records for owner-specific fallback analysis, C++ `op_eq`/`op_neq` evidence justified only the generated int32 loose-equality fast path, and the unsafe broader non-int loose-equality resume was rejected after it exposed recursive generated slow-path re-entry stack growth. Full gates passed including `cargo test --lib -- --quiet` 1981 passed and `cargo fmt -- --check`; the filtered Richards probe now shows owner `CodeBlockId(CellId(69))` first exits at `Core(NotEqual)` with a non-int operand, so the next shared dependency is an iterative generated-resume trampoline before widening non-int equality/property/call slow paths further.
- 2026-05-22: D12 iterative generated-resume trampoline accepted: C++/Rust audits tied baseline slow paths to machine-code continuations rather than recursive VM re-entry, so Rust now drives generated fallback resumes through a VM-owned loop that refreshes IC sidecars between slices. The generated no-call loose-equality subset widened only to nullish/boolean/same-cell cases after the trampoline was in place; full gates passed including `cargo test --lib -- --quiet` 1982 passed and `cargo build --release --example octane_probe`. The filtered Richards x2 owner `CodeBlockId(CellId(69))` probe now returns with no owner-local generated fallback.
- 2026-05-22: D13 global object property-load handoff/sidecar accepted: C++ audits tied the slice to `op_get_from_scope` `GlobalProperty`, where fast paths are guarded cache reads and generic lookup/accessors/lexical/dynamic cases stay on the slow path. Rust now derives `GetGlobalObjectProperty` load IC metadata with an implicit global-object base, admits it to generated property handoff planning/validation, and probes only data-only own-load sidecars. Full gates passed including `cargo test --lib -- --quiet` 1985 passed and `cargo build --release --example octane_probe`; Richards x2 generated fallbacks dropped from 26475 to 63, with the remaining compact tail at `LoadFunction`/`LoadCallee`/`InitializeGlobalLexical`.
- 2026-05-22: D14-D16 Richards entry-helper cleanup accepted: C++ audits kept `LoadFunction` aligned with `op_new_func` runtime allocation/helper behavior, treated Rust `LoadCallee` as the direct active-frame callee slot load corresponding to JSC call-frame metadata, and mapped `InitializeGlobalLexical` to JSC global lexical initialization slow-path semantics. Rust now generates `LoadCallee` directly, routes `LoadFunction` and `InitializeGlobalLexical` through rooted runtime-helper handoffs, and replaces the fixed 16-proof helper metadata table with CodeBlock-owned variable-length metadata. Full gates passed including `cargo test --lib -- --quiet` 1992 passed, `cargo fmt -- --check`, and `cargo build --release --example octane_probe`; Richards x2 now returns with `fallbacks=0`, `baseline_generated_code_artifacts=32`, and 36 call-link attachment rechecks.
- 2026-05-22: D17 generated direct-call nested callee fast path accepted: C++ audit kept the target on Baseline `CallLinkInfo` monomorphic data-IC semantics rather than DFG direct-call patching, and Rust audit identified repeated nested VM entry/tiering as the remaining Richards shared-path cost. Authorized generated `Call`/`CallWithThis` handoffs now reuse validated callee generated artifacts or ready native baseline callables inside the VM-owned direct-call transaction without committing a fresh callee entry decision or publishing duplicate launch descriptors; stale/missing unsupported callees still fall back through the existing nested VM entry. Full gates passed including `cargo test --lib -- --quiet` 1992 passed, `cargo fmt -- --check`, and `cargo build --release --example octane_probe`; Richards x2 remains `fallbacks=0` and drops from the D16 `entry_decisions=40488, launch_descriptors=2324` to `entry_decisions=6626, launch_descriptors=3`, while the smallest legal Richards scoring run still times out at 90s.
- 2026-05-22: D18 metadata-only polymorphic call-link projection accepted: C++ audit confirmed warmed Baseline call-link hits bypass slow observation/update helpers, and the Rust Richards audit identified owner `105` bci `96` as a polymorphic generated call site blocked by monomorphic-only candidate projection. Rust now permits per-target metadata-only call-link attachment attempts at an already monomorphic bytecode site and projects the rejected `Init`-vs-`Monomorphic` second-target attachment as an immutable VM-owned direct-call candidate without mutating the bytecode IC. Full gates passed including `cargo test --lib -- --quiet` 1993 passed, `cargo fmt -- --check`, and `cargo build --release --example octane_probe`; Richards x2 remains `fallbacks=0` and drops from D17 `entry_decisions=6626, call_observations=6604` to `entry_decisions=2384, call_observations=2362`, with the remaining hot observation site now monomorphic owner `78` packed bci `8` targeting owner `99`; the smallest legal Richards scoring run still times out at 90s.
- 2026-05-22: D19 interpreter-origin call-link direct dispatch accepted: C++ audit confirmed LLInt/Baseline monomorphic call-link hits guard the callee function value and argument shape, not receiver identity, and the Rust audit confirmed the owner `78` site had an authorized candidate once metadata existed. Rust ordinary bytecode `Call`/`CallWithThis` requests now probe active call-link candidates before recording another slow observation and, on a single authorized match, drain the interpreter's pending call observation, preserve the live `this` value, and enter the target through the same VM-owned direct-call callee spine used by generated P9 exits. Full gates passed including `cargo test --lib -- --quiet` 1994 passed, `cargo fmt -- --check`, and `cargo build --release --example octane_probe`; Richards x2 remains `fallbacks=0` and drops from D18 `entry_decisions=2384, call_observations=2362` to `entry_decisions=61, call_observations=39`, with remaining generated call-link misses dominated by owner `105` packed bci `96` `CalleeMismatch`; the smallest legal Richards scoring run still times out at 90s.
- 2026-05-22: D20 generated polymorphic call-link local candidate scan accepted: C++ audit confirmed Baseline/LLInt polymorphic call stubs compute the current callee or executable once, scan call slots locally, and call the slow helper only after a full table miss. Rust generated call-link sidecar probing now prefilters known callee-object candidates before dispatch-host probing, so nonmatching polymorphic slots are local branches instead of repeated VM probe/miss work; missing known-callee targets still record one bounded `CalleeMismatch`. Full gates passed including `cargo test --lib -- --quiet` 1995 passed, `cargo fmt -- --check`, and `cargo build --release --example octane_probe`; Richards x2 remains `fallbacks=0` and drops generated call-link probe misses from D19 `9040` to `21`, while the smallest legal Richards scoring run still times out at 90s.
- 2026-05-22: D21 construct call-link attachment/direct dispatch accepted: C++ audits confirmed `op_construct` uses the same Baseline/LLInt `CallLinkInfo` fast-path shape with construct-specialized targets. Rust now admits `Construct` observations into metadata-only call-link descriptors/plans/attachments, adds the static construct IC schema, validates construct targets through installed construct code, enters authorized ordinary construct requests through the generated/native construct callee spine, and suppresses duplicate warmed-site observations once an attachment or pending plan exists. Full gates passed including `cargo test --lib -- --quiet` 1995 passed, `cargo fmt -- --check`, and `cargo build --release --example octane_probe`; Richards x3/x5 remain `fallbacks=0` and hold `entry_decisions=54, call_observations=53` instead of D20 x3 `entry_decisions=82, call_observations=39` and x5 `entry_decisions=124, call_observations=39`.
- 2026-05-22: D22 generated call-link projection cache accepted: C++/Rust audits kept the cache aligned with JSC's cheap CallLinkInfo hit data while preserving Rust's VM-owned liveness/rooting validation. Rust now caches validated generated call-link candidate tables per owner, bytecode snapshot, and conservative call-link metadata epoch for generated sidecars, P9 direct exits, ordinary call/construct direct dispatch, and generated direct-call handoff validation; cache hits still revalidate callee/executable/code-block targets before entry. Construct call-link clear now restores `Construct` call type/specialization instead of hard-coding `Call`. Full gates passed including `cargo test --lib -- --quiet` 1996 passed, `cargo fmt -- --check`, and `cargo build --release --example octane_probe`; focused direct `runRichards()` x3/x5 eval probes remain `fallbacks=0`, while the full Octane scoring harness remains a throughput target.
- 2026-05-22: D23 generated execution/direct-call telemetry accepted: C++/Rust audits identified the remaining Richards bottleneck as VM-owned generated call transactions and Rust-loop generated execution rather than call-link candidate lookup. Rust now records per-generated-executor bytecode counts and per-generated-direct-call routes (`GeneratedEntry`, `NativeEntry`, native interpreter fallback, nested interpreter fallback), exposes the counters through Octane tiering summaries, and proves the direct-call native-entry path in the focused VM regression. Full gates passed including `cargo test --lib -- --quiet` 1996 passed, `cargo fmt -- --check`, and `cargo build --release --example octane_probe`; focused Richards single-site loop x5 remains `fallbacks=0` and reports `baseline_generated_executions=307544`, `baseline_generated_executed_bytecodes=2876849`, and `generated_direct_call_transactions=152531`, mostly `GeneratedEntry`, making per-call transaction overhead the next shared throughput target.
- 2026-05-22: D24 generated-entry validation/telemetry cleanup accepted: C++/Rust audits confirmed warmed direct calls cache target state and avoid full per-call validation in the hit path, while Rust's no-GC/rooting/frame transaction still must stay until a narrower hot slot exists. Rust now threads `Prevalidated` generated-entry execution from a fresh callee artifact/snapshot comparison, skips the duplicate outer direct-call handoff validation for ordinary call opcodes, and compacts hot generated-execution/direct-call telemetry into aggregate counters with retained sample records. Full gates passed including `cargo test --lib -- --quiet` 1996 passed, `cargo fmt -- --check`, and `cargo build --release --example octane_probe`; focused Richards single-site loop x5 remains `fallbacks=0` with `baseline_generated_executions=307544`, `baseline_generated_executed_bytecodes=2876849`, and `generated_direct_call_transactions=152531` in 44.06s, so the next target is a C++-shaped monomorphic generated direct-call hot slot that reduces transaction/frame overhead rather than more validation cleanup.
- 2026-05-22: D25 VM-owned monomorphic generated direct-call hot slot accepted: C++ audit tied the shape to Baseline/LLInt `DataOnlyCallLinkInfo` monomorphic `op_call` hits, not DFG `DirectCallLinkInfo`; Rust audit kept the slot VM-owned, epoch-bound, ID/metadata-only, and outside the no-GC/rooting/frame transaction. Rust now installs a monomorphic hot slot only after a fully validated generated/native direct-call route, reuses it to skip candidate-table/descriptor/boundary proof replay at the VM handoff, preserves continuation validation, target liveness recheck, argument reread, targeted rooting, frame push, and return normalization, and exposes `generated_direct_call_hot_slot_hits` in tiering summaries. Full gates passed including `cargo test --lib -- --quiet` 1996 passed, `cargo fmt -- --check`, and `cargo build --release --example octane_probe`; focused Richards single-site loop x5 remains `fallbacks=0` with `generated_direct_call_transactions=152531`, `generated_direct_call_hot_slot_hits=152402`, and 43.16s elapsed, so the next target is moving the hot-slot probe earlier into the generated sidecar/host path or narrowing the transaction envelope itself.
- 2026-05-22: D26 generated-side direct-call hot-slot probe accepted: C++ audit confirmed Baseline/LLInt monomorphic `op_call` locally compares the cached callee and only calls the default helper on miss; Rust audit kept the VM-owned slot private and passed `baseline.rs` only a cloned immutable projection slice. Generated call-link sidecars now check the active candidate table plus projected monomorphic hot slots before `DispatchHost::probe_generated_call_link`, preserving old `CandidateNotFound` behavior, VM transaction authority, rooting, frame setup, and final validation; tiering summaries expose `generated_direct_call_sidecar_hot_slot_hits`. Full gates passed including `cargo test --lib -- --quiet` 1997 passed, `cargo fmt -- --check`, and `cargo build --release --example octane_probe`; focused Richards single-site loop x5 remains `fallbacks=0` with `generated_direct_call_transactions=152531`, `generated_direct_call_hot_slot_hits=152402`, `generated_direct_call_sidecar_hot_slot_hits=152402`, and 44.95s elapsed, so sidecar probing is no longer the main measured blocker and the next target is reducing the direct-call transaction/frame envelope.
- 2026-05-22: D27 cached direct-call route preference accepted: C++ audit confirmed warmed monomorphic `op_call` trusts cached callee, target `CodeBlock*`, and call destination after the local compare, with no per-hit target clone/revalidation/root sync; Rust audit identified the remaining hot cost as the outer no-GC suspension plus targeted frame/register root sync, and rejected broad root elision without a leaf/no-reentry proof. Rust hot slots now remember the successful `GeneratedEntry` or `NativeEntry` route and try that route first on hot-slot hits, matching C++'s cached call destination shape while preserving the current rooted transaction, callee liveness checks, frame setup, and fallback routes; tiering summaries expose `generated_direct_call_preferred_route_hits`. Full gates passed including `cargo test --lib -- --quiet` 1997 passed, `cargo fmt -- --check`, and `cargo build --release --example octane_probe`; focused Richards single-site loop x5 remains `fallbacks=0` with `generated_direct_call_transactions=152531`, `generated_direct_call_hot_slot_hits=152402`, `generated_direct_call_sidecar_hot_slot_hits=152402`, `generated_direct_call_preferred_route_hits=152402`, route split `GeneratedEntry=143237`/`NativeEntry=9293`, and 46.69s elapsed, confirming route selection is cached but the transaction/rooting envelope is still the measured blocker.
- 2026-05-22: D28 rootless generated-entry proof boundary accepted: C++ audit reaffirmed warmed Baseline/LLInt call-link hits do not publish per-call targeted roots and rely on materialized JS frames plus slow-path stack publication, while Rust audit required any root-sync elision to be backed by Rust artifact/effect proof rather than direct-call metadata. Rust now validates generated direct-call handoffs before the outer suspend, admits only hot-slot `GeneratedEntry` calls whose target artifact is live/snapshot-matched, has no runtime-helper/property handoff plan, proves no heap allocation/runtime/JS call/throw/root touching, and whose bytecode is a strict local leaf (`Load*`, `Move`, `LoadCallee`, `Void`, `Return`); those calls use a generated-entry-only route, skip the outer no-GC suspend plus frame/register targeted-root sync, and fail closed on unexpected throw/re-entry. Native entries, allocating/throwing/nested fallback callees, property/helper/call-capable generated artifacts, stale artifacts, and unproven bodies stay on the existing rooted transaction; tiering summaries expose `generated_direct_call_rootless_generated_entries`. Full gates passed including `cargo test --lib -- --quiet` 1998 passed, `cargo fmt -- --check`, `cargo check --lib`, and `cargo build --release --example octane_probe`; focused coverage proves the rootless generated-entry path skips host targeted-root sync and that native/allocating direct calls do not use it. Richards x5 remains `fallbacks=0` with `generated_direct_call_transactions=152531`, `generated_direct_call_hot_slot_hits=152402`, `generated_direct_call_preferred_route_hits=152402`, `generated_direct_call_rootless_generated_entries=0`, and 44.10s elapsed, so the proof machinery is in place but real Richards hot targets need a broader lazy-exit/rooting design rather than the strict leaf whitelist.
- 2026-05-22: D29 deferred property-exit rooting accepted: C++/Rust audits confirmed warmed generated JS-to-JS calls should skip per-hit caller-root publication but must publish before runtime/helper/property/fallback boundaries. Rust now admits hot-slot `GeneratedEntry` direct calls whose target artifact has a property handoff plan when the artifact has no runtime-helper plan, no heap allocation/runtime/JS call/throw/root-touch effect, and every non-subset opcode is a validated no-GC property-load exit; the rootless direct-call path marks a deferred-rooting chain, and generated runtime/property/fallback exits publish nonlocal caller frame/register roots only if that chain exits. Focused coverage proves property sidecar hits skip entry root sync and forced property misses publish the caller object root before `GetByName` slow-path dispatch. Full gates passed including `cargo test --lib -- --quiet` 2000 passed, `cargo fmt -- --check`, `cargo check --lib`, and `cargo build --release --example octane_probe`; Richards x5 remains `fallbacks=0` and raises `generated_direct_call_rootless_generated_entries` from 0 to 42670 with the same `generated_direct_call_transactions=152531`, leaving remaining rooted/native direct-call routes as the next throughput target.
- 2026-05-26: D30 deferred property-store exit rooting accepted: C++ audit confirmed hot Baseline/LLInt JS-to-JS calls stay unpublished on guarded hits and publish only at runtime/helper/IC/native/throw exits, while the Rust audit found remaining rooted generated entries dominated by property-store handoff artifacts. Rust now admits validated no-GC property-store exits (`PutByName`, `PutGlobalObjectProperty`, `PutByValue`) to the D29 deferred-rooting proof while runtime-helper, JS-call/throw, native, and unproven store paths stay rooted. Focused coverage proves store sidecar hits skip direct-call entry root sync and forced store probe misses publish the caller object root before `PutByName` slow-path dispatch. Full gates passed including `cargo test --lib -- --quiet` 2002 passed, `cargo fmt -- --check`, `cargo check --lib`, and `cargo build --release --example octane_probe`; Richards x5 remains `fallbacks=0` and raises `generated_direct_call_rootless_generated_entries` from 42670 to 64295 with the same `generated_direct_call_transactions=152531`.
- 2026-05-26: D31 rootless rejection telemetry accepted: C++ audit confirmed hot JS-to-JS generated entries remain unpublished at call entry, helper/slow exits publish at the exit, and native/host/internal routes must remain rooted. Rust now records aggregate rootless generated-entry rejection reasons plus unsupported body-opcode counts without retaining per-call records, and `octane_probe --tiering-summary` prints the counters; focused coverage proves native-entry hot slots are classified as `preferred_route_not_generated_entry`. Full gates passed including `cargo test --lib -- --quiet` 2002 passed, `cargo fmt -- --check`, `cargo check --lib`, and `cargo build --release --example octane_probe`. Richards x5 remains `fallbacks=0` and classifies the remaining rootless blockers as `unsupported_body_opcode=78824` (`CallWithThis=78798`, `Construct=26`), `preferred_route_not_generated_entry=9281`, `hot_slot_miss=129`, and `runtime_helper_plan=2`, making deferred JS-call/construct exit rooting the next shared dependency ahead of native-entry work.
- 2026-05-26: D32 deferred JS-call exit rooting accepted for `Call`/`CallWithThis`: C++/Rust audits confirmed warmed generated JS-to-JS calls do not publish at the call boundary, while generated JS-call slow/link exits must publish before nested call handling. Rust now admits only validated P9 JS-call native-exit plan sites for `Call`/`CallWithThis` into the deferred generated-exit rootless proof, still rejects `Construct`, and publishes any active outer deferred roots before rooted nested direct-call or slow call exits; JS-call exit cleanup now removes deferred roots on all observation/error/continuation paths. Focused coverage forces a mismatched nested `CallWithThis` callee under an outer rootless generated direct call and proves the caller's live inner callee root is published before slow nested dispatch with no root/no-GC leak. Full gates passed including `cargo test --lib -- --quiet` 2003 passed, `cargo fmt -- --check`, `cargo check --lib`, and `cargo build --release --example octane_probe`; Richards x5 remains `fallbacks=0` and raises `generated_direct_call_rootless_generated_entries` from 64295 to 143093, leaving only `Construct=26`, native-route `preferred_route_not_generated_entry=9281`, `hot_slot_miss=129`, and `runtime_helper_plan=2` as classified direct-call rootless blockers.
- 2026-05-26: D33 deferred construct exit rooting and native-route split accepted: C++/Rust audit kept pure Rust native-entry shims rooted and required a separate emitted-C-ABI proof before rootless native-entry work, but identified validated generated `Construct` exits as the safe narrow continuation of D32. Rust now admits validated P9 `Construct` native-exit plan sites to the deferred generated-exit rootless proof, records native-entry preferred-route rejections by callable kind, and keeps both pure shim and emitted C-ABI native routes rooted. Focused coverage forces a mismatched nested `Construct` under an outer rootless generated direct call and proves the live constructor root publishes before slow nested dispatch with no root/no-GC leak; native-entry hot-slot coverage proves emitted C-ABI kind classification. Full gates passed including `cargo test --lib -- --quiet` 2004 passed, `cargo fmt -- --check`, `cargo check --lib`, and `cargo build --release --example octane_probe`; Richards x5 remains `fallbacks=0`, raises `generated_direct_call_rootless_generated_entries` from 143093 to 143119, drops `unsupported_body_opcode` to 0, and classifies all 9281 remaining native-route rejections as `P6X86_64EmittedSemanticCAbiEntry`.
- 2026-05-26: D34 side-exit-free emitted native rootless proof accepted: C++/Rust audit required emitted C-ABI native-entry rootless admission to validate gate/readiness/descriptor/residency/frame/snapshot/sealed-callable state, no heap/runtime/throw/root-touch effects, retained return table shape, and either disallow P6 side exits or publish before fallback. Rust now admits only retained-side-exit-free `P6X86_64EmittedSemanticCAbiEntry` hot slots through a native-entry-only rootless route, records `generated_direct_call_rootless_native_entries`, and keeps emitted P6 side-exit callees on the rooted path with root publication before fallback; the broader side-exit-rootless attempt was rejected after local `Benchmark.runIteration()` probes exceeded the practical gate. Full gates passed including `cargo test --lib -- --quiet` 2005 passed, `cargo fmt -- --check`, `cargo check --lib`, and `cargo build --release --example octane_probe`; direct `runRichards()` x5 still returns with `fallbacks=0`, but the comparable D33 Richards transaction window was not reproduced this slice.
- 2026-05-26: D35 emitted-native property-exit rootless proof accepted: the corrected Richards single-site loop x5 command reproduced the D33 comparable window (`generated_direct_call_transactions=152531`, `generated_direct_call_generated_entries=143237`, `generated_direct_call_native_entries=9293`), and Rust now records separate native-entry rootless rejection counters plus unsupported opcode histograms. Emitted C-ABI native-entry rootless admission now accepts validated retained P10 property exits with deferred generated-exit rooting while rejecting retained side/runtime-helper/JS-call/loop-backedge exits; focused coverage proves a P10-only property-load emitted native entry reaches `generated_direct_call_rootless_native_entries`, while retained P6 side exits remain rooted and classified. Full gates passed including `cargo test --lib -- --quiet` 2006 passed, `cargo fmt -- --check`, `cargo check --lib`, and `cargo build --release --example octane_probe`; Richards x5 remains `generated_direct_call_rootless_native_entries=0` because all 9281 native-entry attempts are now precisely classified as `retained_js_call_exit`, making emitted-native JS-call exit/rooting the next shared blocker.
- 2026-05-26: D36 emitted-native JS-call exit rootless proof accepted: C++ audit confirmed Baseline/LLInt hot JS calls materialize normal frames and publish top-frame/root state at slow/runtime boundaries rather than every call entry, while the Rust audit required avoiding host call-link probing before deferred roots are published. Rust now admits retained P9 `Call`/`CallWithThis`/`Construct` exits into emitted C-ABI native-entry rootless only when retained metadata exactly matches the current code-block derived P9 plan, returns deferred generated-exit rooting, and skips P9 direct-call authorization while deferred rooting is active so the existing rooted JS-call exit transaction publishes roots first. Focused coverage proves a retained P9 native-entry direct call reaches `generated_direct_call_rootless_native_entries` and publishes the callee frame's live nested callee before rooted dispatch; existing P9 direct, throwing, forged-metadata, P10 property, P6 side-exit, and P16a native-entry tests remain green. Full gates passed including `cargo test --lib -- --quiet` 2007 passed, `cargo fmt -- --check`, `cargo check --lib`, and `cargo build --release --example octane_probe`; the corrected Richards x5 window remains `fallbacks=0` and moves `generated_direct_call_rootless_native_entries` from 0 to 9281 with `generated_direct_call_rootless_native_entry_rejections=0`, leaving only `hot_slot_miss=129` and `runtime_helper_plan=2` as direct-call rootless blockers in this focused probe.
- 2026-05-26: D37 Octane probe honesty defaults accepted: JetStream/Rust runner audit confirmed the library runner already resolves official Octane iterations and plan overrides, but `octane_probe` defaulted to core smoke `5/1` and collect-all, making proof logs easy to misread. The probe now defaults to full-suite official iteration counts and fail-fast, exposes the prior core `5/1` behavior behind explicit `--smoke`, and prints each benchmark's effective mode/iteration/worst-case config before the result line. Gates passed including `cargo test --lib -- --quiet` 2007 passed, `cargo fmt -- --check`, `cargo check --lib`, `cargo build --release --example octane_probe`, and `octane_probe --help`; a post-D36 honest `richards --iterations 2 --worst-case-count 1` scoring run still times out at 120s in `Generated(Runner)`, so Octane-core performance remains the active D-layer blocker rather than runner-source ambiguity.
- 2026-05-26: D38 rootless generated-entry proof cache accepted: C++ audit confirmed warmed Baseline call-link fast paths guard callee identity and jump through cached call targets instead of re-proving linked bodies, while Rust audit required preserving hot-slot target validation and artifact invalidation. Rust hot slots now cache only successful rootless `GeneratedEntry` proofs under call-link projection, baseline generated-artifact/invalidation, property-IC, and structure-stub epochs; reuse still checks target CodeBlock id, artifact id/owner/validity, no runtime-helper plan, and bytecode snapshot before taking the rootless route. Focused coverage proves the proof is installed on the first rootless hit and reused on the next hit via new cache-hit telemetry, and tiering summaries expose `generated_direct_call_rootless_generated_entry_proof_cache_hits`. Gates passed including `cargo test --lib -- --quiet` 2007 passed, `cargo fmt -- --check`, `cargo check --lib`, and `cargo build --release --example octane_probe`; Richards x5 remains behaviorally stable and reports `generated_direct_call_rootless_generated_entries=143119`, `generated_direct_call_rootless_generated_entry_proof_cache_hits=142140`, `generated_direct_call_rootless_native_entries=9281`, `generated_direct_call_rootless_native_entry_rejections=0`, and 30.46s total, while honest `richards --iterations 2 --worst-case-count 1` still times out at 120s in `Generated(Runner)`.
- 2026-05-26: D39 hot generated-slice telemetry accepted: C++/Rust audits confirmed the main remaining mismatch is C++ Baseline's owner-level native body and linked-call continuity versus Rust's short generated-slice re-entry. Rust now records aggregate generated-execution summaries by owner and direct-call transaction summaries by caller/site/target/route beyond the retained warmup record cap, and `octane_probe --tiering-summary` prints top summaries before sample tails. Gates passed including `cargo test --lib -- --quiet` 2007 passed, `cargo fmt -- --check`, `cargo check --lib`, and `cargo build --release --example octane_probe`; Richards x5 remains behaviorally stable at 30.20s with `baseline_generated_executions=307544`, `baseline_generated_executed_bytecodes=2876847`, `generated_direct_call_transactions=152531`, top owner `CodeBlockId(CellId(69))` at 69,112 executions / 633,800 bytecodes / 68,976 JS-call exits, and top edge `69@32 -> 96` at 42,683 generated-entry transactions. A hot-slot projection cache attempt was measured and rejected after unchanged counters and a slower comparable probe, so the next target remains collapsing generated JS-call resume/re-entry or owner-level native Baseline continuity.
- 2026-05-26: D40 call-continuation audit completed with no runtime patch accepted. C++ evidence from `commonCallOp`, LLInt/Baseline call helpers, `CallLinkInfo`, and `JITCodeMap` shows JS calls return into the same owner native body at the next bytecode label, while Rust still exits to a VM-mediated generated resume after each JS call. Two Rust experiments were rejected and removed: property sidecar projection caching failed the stale CodeBlock metadata invariant test, and a `Prevalidated` VM-loop JS-call resume variant still preserved the per-call VM generated-entry transaction rather than the C++ owner-body continuation. After revert, the direct Richards x5 probe remains at the D39 counter surface (`baseline_generated_executions=307544`, `baseline_generated_executed_bytecodes=2876847`, `generated_direct_call_transactions=152531`), so the next viable dependency is an owner-body continuation design, not another per-call resume validation shortcut.
- 2026-05-26: D41 owner-continuation map metadata accepted. C++ audits tied the next dependency to Baseline's per-bytecode labels, `JITCodeMap`, `CallLinkInfo::doneLocation`, and in-owner post-call fallthrough, so Rust now derives an immutable generated-owner continuation map at generated-artifact install time. The map records snapshot-bound bytecode labels plus `Call`/`CallWithThis`/`Construct` continuation sites with destination, argument count including `this`, resume bytecode, and call-vs-construct kind; artifact validation rejects stale map snapshots and VM install rederives from the current CodeBlock instead of caching projections. This is deliberately metadata-only: no generated execution, direct-call transaction, or handoff-resume path changed. Gates passed including `cargo fmt -- --check`, `cargo check --lib`, `cargo test --lib -- --quiet` 2014 passed, `cargo build --release --example octane_probe`, and direct Richards x5; the probe remains at the D39/D40 counter surface (`baseline_generated_executions=307544`, `baseline_generated_executed_bytecodes=2876847`, `generated_direct_call_transactions=152531`, `generated_direct_call_rootless_generated_entries=143119`, `generated_direct_call_rootless_native_entries=9281`), leaving native binding of these labels/done locations as the next shared dependency.
- 2026-05-26: D42 owner-continuation native label binding accepted. C++ audit confirmed Baseline binds bytecode labels and `CallLinkInfo::doneLocation` only after `LinkBuffer` final layout, and that call `doneLocation` is distinct from next-bytecode resume because post-call SP/result work follows. Rust now records snapshot-bound owner/native binding metadata for emitted x86_64 semantic entries: D41 bytecode labels bind to emitted instruction offsets, P9 `Call`/`CallWithThis`/`Construct` continuations bind to retained native-exit stub offsets, and each call binding stores a separate metadata-only done offset plus resolved resume-instruction offset. The semantic native install record and retained callable return table keep this evidence, while VM dispatch/direct-call/rooting behavior remains unchanged. Gates passed including `cargo fmt -- --check`, `cargo check --lib`, `cargo test --lib -- --quiet` 2017 passed, `cargo build --release --example octane_probe`, and direct Richards x5; the probe remains unchanged (`baseline_generated_executions=307544`, `baseline_generated_executed_bytecodes=2876847`, `generated_direct_call_transactions=152531`, `generated_direct_call_rootless_generated_entries=143119`, `generated_direct_call_rootless_native_entries=9281`), leaving real owner-body call/fallthrough emission as the next shared dependency.
- 2026-05-26: D43 owner-body post-call obligation contract accepted. C++ audit fixed the contract: `CallLinkInfo::doneLocation` is the post-call native PC before `resetSP`, value profiling, result write, and next-bytecode resume; slow call paths also resume at that point, while exceptions route through handler thunks and tail calls stay excluded. Rust now carries an ordered metadata-only post-call obligation list on each D42 P9 owner/native call binding: reset caller stack pointer, pending call-result profile site, call result write or construct normalization/write, then resume at the resolved bytecode label. The retained callable table preserves this contract for installed semantic entries; VM dispatch, direct-call routing, rooting, generated resume, and emitted bytes are unchanged. Gates passed including `cargo fmt -- --check`, `cargo check --lib`, focused owner-binding and retained-table tests, `cargo test --lib -- --quiet` 2017 passed, `cargo build --release --example octane_probe`, and direct Richards x5; the probe remains unchanged (`baseline_generated_executions=307544`, `baseline_generated_executed_bytecodes=2876847`, `generated_direct_call_transactions=152531`, `generated_direct_call_rootless_generated_entries=143119`, `generated_direct_call_rootless_native_entries=9281`), leaving real owner-body call/fallthrough emission as the next shared dependency.
- 2026-05-26: D44 owner post-call native block append accepted. C++ and Rust audits confirmed the next safe slice is non-entered/linkable owner-body continuation bytes, not routing JS-call returns into them yet: C++ Baseline stores `returnValueGPR` through the post-call `doneLocation` path before falling through to the resume label, while Rust still normalizes construct returns outside native owner-body code. Rust now appends validated P9 x86_64 post-call blocks for `Call` and `CallWithThis` only: reset/profile placeholders, `mov [rbp+local], rax` result write, and a relative jump to the resolved owner resume label. Construct remains metadata-only until native construct normalization is implemented, and VM dispatch/direct-call/rooting behavior remains unchanged. Gates passed including `cargo fmt -- --check`, `cargo check --lib`, focused P9 owner-binding and retained-table tests, `cargo test --lib -- --quiet` 2017 passed, `cargo build --release --example octane_probe`, and direct Richards x5; the probe remains unchanged (`baseline_generated_executions=307544`, `baseline_generated_executed_bytecodes=2876847`, `generated_direct_call_transactions=152531`, `generated_direct_call_rootless_generated_entries=143119`, `generated_direct_call_rootless_native_entries=9281`), leaving real link/entry into owner post-call blocks as the next shared dependency.
- 2026-05-26: D45 P9 owner post-call return-target proof accepted. C++ audit confirmed `doneLocation` is the machine return target immediately after the call and must run resetSP, profiling, result store, and fallthrough; Rust audit rejected direct entry while those operations/rooting remain partly VM-owned. Rust now records a payload-keyed owner post-call return-target proof for retained `Call`/`CallWithThis` P9 sites: the proof binds the decoded payload to the validated post-call block start/end, result-store offset, resume jump, resume label, linked size, and linked digest, and VM lookup rejects forged payload/owner/target/range metadata without dispatch. Construct remains proofless. Runtime routing is unchanged. Gates passed including `cargo fmt -- --check`, `cargo check --lib`, focused P9 proof tests, `cargo test --lib -- --quiet` 2018 passed, `cargo build --release --example octane_probe`, and direct Richards x5; the probe remains unchanged (`baseline_generated_executions=307544`, `baseline_generated_executed_bytecodes=2876847`, `generated_direct_call_transactions=152531`, `generated_direct_call_rootless_generated_entries=143119`, `generated_direct_call_rootless_native_entries=9281`), leaving executable reset/profile/rooting and then real return-target entry as the next shared dependency.
- 2026-05-26: D46 P9 post-call reset/proof validation accepted. C++ audit confirmed `resetSP` is real JSC stack-pointer repair before result profiling/store, while Rust's current emitted C ABI has no separate JS stack-pointer register and keeps frame access pinned to `rbp`; Rust therefore now models `ResetCallerStackPointer` as an explicit P6-C-ABI no-op status, validates the emitted reset byte as `0x90`, keeps call-result profiling explicitly `MetadataPending`, and carries both statuses/offsets through the retained post-call block and payload-keyed return-target proof. The P9 VM-mediated call-exit path now consults that proof before building the JS-call handoff, rejecting tampered reset/profile proof metadata before callee dispatch, observations, fallback telemetry, or NoGC imbalance. Runtime owner-body reentry remains disabled until profiling/result rooting and a result-seeded reentry ABI exist. Gates passed including `cargo fmt -- --check`, `cargo check --lib`, focused P9 proof/path tests (15 passed), `cargo test --lib -- --quiet` 2019 passed, `cargo build --release --example octane_probe`, and direct Richards x5; the probe remains unchanged (`baseline_generated_executions=307544`, `baseline_generated_executed_bytecodes=2876847`, `generated_direct_call_transactions=152531`, `generated_direct_call_rootless_generated_entries=143119`, `generated_direct_call_rootless_native_entries=9281`), leaving executable result profiling/rooting and then real return-target entry as the next shared dependency.
- 2026-05-26: D47 VM-mediated call-result value profiling accepted. C++ audits tied the slice to JSC `emitPutCallResult`: after post-call stack repair, Baseline samples `returnValueJSR` into the call bytecode's `ValueProfile` bucket before storing the destination, and the bucket is a raw `EncodedJSValue` sample rather than a strong GC root. Rust now derives linked call-result value-profile buckets for `Call`/`CallWithThis` only, records raw returned value samples on VM-mediated ordinary call completion before caller destination writes, validates owner/opcode/destination against the current CodeBlock, and covers both cold VM fallback and warmed generated direct-call transactions; `Construct`, native post-call profile bytes, result-in-`rax` owner-body reentry, and prediction merge/clear remain deferred. Gates passed including `cargo fmt -- --check`, `cargo check --lib`, focused value-profile tests (5 passed), focused P9 tests (15 passed), `cargo test --lib -- --quiet` 2022 passed, `cargo build --release --example octane_probe`, and direct Richards x5; the probe remains unchanged (`baseline_generated_executions=307544`, `baseline_generated_executed_bytecodes=2876847`, `generated_direct_call_transactions=152531`, `generated_direct_call_rootless_generated_entries=143119`, `generated_direct_call_rootless_native_entries=9281`), leaving native post-call profile emission/result-seeded reentry as the next dependency.
- 2026-05-26: D48 P9 owner post-call result-seeded reentry proof accepted with routing still disabled. C++/Rust audits confirmed Baseline's post-call path consumes the JS call result in `returnValueJSR`, while Rust's current retained P9 exit returns a discriminator payload in `rax`; Rust now appends a validated callable reentry stub for each `Call`/`CallWithThis` post-call block, runs the P6 callable prologue, seeds `rax` from a third C-ABI result-bits argument, and jumps to the existing post-call block. The retained proof now binds the payload, post-call block, reentry range, seed ABI, linked size/digest, and `MetadataPending` profile status, and VM lookup rejects forged seed/offset metadata before JS-call handoff. A platform x86_64 wrapper proves the third argument carrier, but VM execution still uses the existing VM-mediated return path; native profile-store bytes and JIT-addressable profile metadata remain deferred. Gates passed including `cargo fmt -- --check`, `cargo check --lib`, focused P9 tests (16 passed), `cargo test --lib -- --quiet` 2023 passed, and `cargo build --release --example octane_probe`. A small top-level Richards sanity probe returned; the heavier `Benchmark.runIteration()` x5 probe exceeded five minutes in this run and was interrupted, so the comparable Richards performance counter gate needs to be reestablished next rather than recorded as passed.
- 2026-05-26: D49 JIT-addressable call-result value-profile storage accepted. C++/Rust audits tied the next dependency to JSC's negative-indexed `ValueProfile` table before native `emitPutCallResult` bytes: Rust now materializes fixed boxed raw `EncodedJSValue` buckets for linked `Call`/`CallWithThis` result profiles, binds each bucket to a 1-based value-profile offset, negative metadata-table displacement, bytecode index/checkpoint/kind, and stable raw address, and routes VM-mediated D47 sampling through the same raw bucket before updating telemetry samples. Cell-valued samples remain raw bits and not strong roots, stale bucket bindings are rejected, and the P9 post-call block still records `MetadataPending` rather than claiming emitted store bytes. Gates passed including `cargo fmt -- --check`, `cargo check --lib`, focused value-profile tests (7 passed), focused P9 tests (16 passed), `cargo test --lib -- --quiet` 2025 passed, and `cargo build --release --example octane_probe`; the corrected single-site Richards loop (`for (let i = 0; i < 5; ++i) runRichards();`) reestablishes the comparable D39-D48 surface with `fallbacks=0`, `baseline_generated_executions=307544`, `baseline_generated_executed_bytecodes=2876847`, `generated_direct_call_transactions=152531`, `generated_direct_call_rootless_generated_entries=143119`, and `generated_direct_call_rootless_native_entries=9281`. `Benchmark.runIteration()` x5 is 250 Richards simulations and remains a later throughput gate, not the historical single-site counter gate.
- 2026-05-26: D50 call-result value-profile policy/proof binding accepted. C++ audits confirmed `JIT::shouldEmitProfiling()` is driven only by DFG capability (`CannotCompile` disables, `CanCompile`/`CanCompileAndInline` enable) and that `emitPutCallResult` keeps profiling as a separate side effect before the destination store. Rust now models that capability policy, defaults linked call-result profiles to compile-capable emission, rebinds profile storage when tier capability is overridden, uses the C++-shaped metadata displacement formula over 1-based `ValueProfile` records plus `LinkingData`, rejects duplicate/stale JIT bucket bindings, and carries the exact profile slot/checkpoint/kind/value-profile offset/displacement/raw-byte width/policy through the D41-D48 owner-continuation and P9 return-target proof metadata. The emitted profile byte remains the disabled `MetadataPending` placeholder; VM proof lookup revalidates the symbolic binding against the current registered CodeBlock and checks raw addresses are nonzero, but does not require address equality because current CodeBlock clones own distinct boxed bucket storage until native profile bytes bind to registered storage. Gates passed including `cargo fmt -- --check`, `cargo check --lib`, focused value-profile tests (10 passed), focused owner-continuation tests (9 passed), focused P9 tests (16 passed), `cargo test --lib -- --quiet` 2028 passed, and `cargo build --release --example octane_probe`; the comparable Richards loop remains stable with `fallbacks=0`, `baseline_generated_executions=307544`, `baseline_generated_executed_bytecodes=2876847`, `generated_direct_call_transactions=152531`, `generated_direct_call_rootless_generated_entries=143119`, and `generated_direct_call_rootless_native_entries=9281`. Native profile-store bytes and registered-storage raw-address equality remain the next dependency before real owner-body post-call entry.
- 2026-05-26: D51 registered call-result value-profile storage proof accepted. C++ audits confirmed `emitPutCallResult` samples the JS call result from `rax` into `disp(%r12)` through the Baseline metadata-table register before writing the destination, with no fence, barrier, rooting, VM call, or alternate store when profiling is disabled. Rust now derives owner-continuation profile proof from the current registered CodeBlock storage instead of the lowering clone, requires retained P9 proof raw-bucket address equality against the registered `ValueProfileJitStoreTarget`, and carries a monotonic `ValueProfileJitStorageGeneration` through JIT storage bindings, store targets, D41 owner-continuation metadata, D48/P9 proof metadata, and VM proof validation so stale rematerialized storage is rejected before native entry. Forged raw-address and generation proofs are covered. Native profile-store bytes remain `MetadataPending` until Rust models the metadata-table base register relation or a justified Rust ABI bridge, so the placeholder is not misreported as executable profiling. Gates passed including `cargo fmt -- --check`, `cargo check --lib`, focused value-profile tests (10 passed), focused owner-continuation tests (9 passed), focused P9 tests (16 passed), `cargo test --lib -- --quiet` 2028 passed, `cargo build --release --example octane_probe`, and the comparable Richards loop with `fallbacks=0`, `baseline_generated_executions=307544`, `baseline_generated_executed_bytecodes=2876847`, `generated_direct_call_transactions=152531`, `generated_direct_call_rootless_generated_entries=143119`, and `generated_direct_call_rootless_native_entries=9281`. The next dependency is metadata-table/base-register native profile-store emission, not owner-body return-target routing yet.
- 2026-05-26: D52 metadata-table-relative native call-result profile-store bytes accepted. C++ audits confirmed x86_64 Baseline keeps the metadata table in `r12`, returns JS call results in `rax`, and emits fixed-width `movq %rax, disp32(%r12)` bytes (`49 89 84 24 <disp32>`) before the destination write when profiling is enabled; disabled profiling omits only that store. Rust rejected the non-JSC absolute-address bridge and instead reshaped JIT-addressable value-profile storage into a metadata-table-backed layout so one base address plus the C++ displacement formula reaches each registered raw bucket. P9 owner post-call blocks now emit the real `r12`-relative profile store for `Call`/`CallWithThis`, save/restore `r12` in callable entries, seed `r12` from the P9 reentry fourth C-ABI argument while seeding `rax` from the result-bits argument, and validate store status, base/raw/generation/policy, byte span, and metadata-table seed proof in VM return-target checks. Owner-body return-target routing remains disabled until this result/profile path is entered under the rooting/double-sampling policy. Gates passed including `cargo fmt -- --check`, `cargo check --lib`, focused value-profile tests (10 passed), focused owner-continuation tests (9 passed), focused executable-memory tests (25 passed), focused P9 tests (17 passed), `cargo test --lib -- --quiet` 2029 passed, `cargo build --release --example octane_probe`, and the comparable Richards loop with `fallbacks=0`, `baseline_generated_executions=307544`, `baseline_generated_executed_bytecodes=2876847`, `generated_direct_call_transactions=152531`, `generated_direct_call_rootless_generated_entries=143119`, and `generated_direct_call_rootless_native_entries=9281`.
- 2026-05-26: D53 P9 owner post-call normal-return reentry accepted for validated direct `Call`/`CallWithThis`. C++ audits confirmed normal JS-call returns enter `CallLinkInfo::doneLocation`, then run `resetSP`, value profile, destination store, and next-bytecode continuation exactly once; throws, tail calls, `call_ignore_result`, and construct semantics must not use this call-only path. Rust now splits generated direct-call completion so the ordinary VM continuation remains default, while proof-validated P9 owner calls pop the callee frame, validate the caller continuation, skip VM call-result profiling/destination writes, and return a P9 reentry invocation to the existing native loop. The native loop can now call the result-seeded owner post-call reentry stub, feed `rax` and the metadata-table base, and continue handling normal values or retained P6/P9/P10/P14 payloads without a benchmark-shaped shortcut; suspended NoGC regions are resumed before native reentry. Focused tests prove warmed P9 `Call`, polymorphic `Call`, and `CallWithThis` return through owner native post-call code: VM sample telemetry stays at the cold count while raw JIT profile storage is updated by the native store, and the old generic bytecode-2 VM resume dispatch disappears. Construct and throw routes remain on the old VM/construct paths. Gates passed including `cargo fmt -- --check`, `cargo check --lib`, focused P9 tests (17 passed), focused value-profile tests (10 passed), `cargo test --lib -- --quiet` 2029 passed, `cargo build --release --example octane_probe`, and the comparable Richards loop with `fallbacks=0`, unchanged aggregate direct-call counters (`baseline_generated_executions=307544`, `baseline_generated_executed_bytecodes=2876847`, `generated_direct_call_transactions=152531`, `generated_direct_call_rootless_generated_entries=143119`, `generated_direct_call_rootless_native_entries=9281`). The next dependency is widening this owner-continuation entry to the generated-entry/Richards hot surface rather than more proof metadata.
- 2026-05-26: D54 generated-entry owner-continuation handoff proof accepted. C++/Rust audits confirmed C++ has no semantic split between native owner bodies and generated-entry owner bodies for normal call return: both must resume at the caller owner post-call continuation, but Rust's generated-entry direct-call path still defaults to VM continuation until it can prove and enter the owner native post-call block. Rust now carries the artifact-derived owner-continuation site on generated-entry `Call`/`CallWithThis`/`Construct` handoffs, validates the static owner/opcode/destination/argument-count/resume/kind shape against the current registered CodeBlock before dispatch, and rejects forged site metadata without dispatch while leaving dynamic value-profile storage equality to the existing native/P9 proof path. This is deliberately proof-carrying only: generated direct calls still return through VM continuation unless they came from the D53 P9 native-exit route. Gates passed including `cargo fmt -- --check`, `cargo check --lib`, focused owner-continuation tests (10 passed), focused P9 tests (17 passed), focused value-profile tests (10 passed), `cargo test --lib -- --quiet` 2030 passed, `cargo build --release --example octane_probe`, and the comparable Richards loop with `fallbacks=0`, `baseline_generated_executions=307544`, `baseline_generated_executed_bytecodes=2876847`, `generated_direct_call_transactions=152531`, `generated_direct_call_rootless_generated_entries=143119`, and `generated_direct_call_rootless_native_entries=9281`. The next dependency is VM-side generated-handoff lookup of a matching retained P9 owner proof plus a native-loop entry point that can start directly at `P9OwnerPostCallReentry`.
- 2026-05-26: D55 generated-origin owner post-call reentry route accepted for proof-backed ordinary `Call`/`CallWithThis`. C++ audits confirmed Baseline generated/native callers share the same `CallLinkInfo::doneLocation` normal-return target and must run reset/profile/result-store/resume exactly once; exceptions, construct, tail calls, and ignored-result calls remain excluded. Rust now factors the emitted P6 native loop so it can start from either normal entry or a `P9OwnerPostCallReentry`, validates generated direct-call handoffs against D54 owner-continuation metadata, looks up a matching retained P9 owner proof from the caller's ready emitted-native owner artifact, and routes normal generated-origin direct-call returns into that owner native post-call block instead of `finish_ordinary_js_call_return`. Missing owner native proof keeps the old VM continuation; stale or forged retained proof rejects before callee dispatch. Focused tests cover generated-origin `Call` and `CallWithThis` preserving the cold VM sample count while native raw profile storage is updated, plus forged generated-owner proof rejection before direct-call transaction. Gates passed including `cargo fmt -- --check`, `cargo check --lib`, focused generated-origin/P9/rootless/owner-continuation tests, `cargo test --lib -- --quiet` 2032 passed, `cargo build --release --example octane_probe`, and the comparable Richards loop with `fallbacks=0`, unchanged hot generated-execution counters (`baseline_generated_executions=307544`, `baseline_generated_executed_bytecodes=2876847`, `generated_direct_call_transactions=152531`, `generated_direct_call_rootless_generated_entries=143119`, `generated_direct_call_rootless_native_entries=9281`). The route is correct but Richards still lacks ready owner-native proofs for its hot generated callers, so the next dependency is making owner-native continuity available on that surface rather than adding more proof metadata.
- 2026-05-26: D56 P15 supplemental owner-native install policy accepted. C++ audit confirmed Baseline machine code, call-link/property metadata, and `doneLocation` continuations coexist on the same `CodeBlock`; Rust audit found D55 was starved because P15 treated any valid generated artifact as `Eligible` and never tried to materialize the owner-native proof beside it. Rust now distinguishes missing-artifact auto install from supplemental native install, permits the supplemental path only for generated owners with ordinary `Call`/`CallWithThis` owner-continuation sites and no native artifact, preserves the existing generated artifact and generated entry decision, disables generated fallback replacement in the supplemental mode, and suppresses repeated supplemental retries after a same-owner failed supplemental attempt. Focused tests prove generated artifacts are preserved while a supplemental owner-native artifact is installed, generated-origin `Call`/`CallWithThis` can use D55 without manual native setup, and existing P16c cold/unvalidated callee behavior still protects callee entry semantics. Gates passed including `cargo fmt -- --check`, `cargo check --lib`, `cargo test --lib -- --quiet` 2034 passed, `cargo build --release --example octane_probe`, and the comparable Richards loop with `fallbacks=0`, `baseline_entry_auto_materializations=38`, `baseline_entry_artifacts=1`, `baseline_generated_code_artifacts=32`, `baseline_generated_executions=307544`, `baseline_generated_executed_bytecodes=2876847`, `generated_direct_call_transactions=152531`, `generated_direct_call_rootless_generated_entries=143119`, and `generated_direct_call_rootless_native_entries=9281`. Filtered Richards telemetry shows the new supplemental attempts fail `Lowering` for owners 57, 60, 81, 72, and 75 without replacing generated artifacts, so the next dependency is native-lowering support/diagnostics for the hot generated owner shapes rather than more P15 policy.
- 2026-05-26: D57 native-lowering failure detail telemetry accepted. C++ audit confirmed Baseline JIT emits broad fast paths with slow/runtime exits for bitwise, relational, equality, truthiness, and `ToNumber`, with int32 bitwise/relational the safest pure native families but equality and `ToNumber` requiring conservative exits. Rust now preserves `P6X86_64BaselineLoweringError` summaries on auto-materialization records as `native_detail`, exposes `baseline_native_lowering_failures` through VM/Octane summaries, and keeps supplemental failure policy unchanged. Focused tests prove unsupported supplemental lowering records bytecode index/opcode detail and preserves generated artifacts. Gates passed including `cargo fmt`, focused tests, `cargo test --lib` 2036 passed, `cargo check --example octane_probe`, and release `octane_probe` eval/probe checks. The comparable Richards loop still returns with `fallbacks=0`, `baseline_entry_auto_materializations=38`, `baseline_native_lowering_failures=35`, `baseline_entry_artifacts=1`, `baseline_generated_code_artifacts=32`, `baseline_generated_executions=307544`, `baseline_generated_executed_bytecodes=2876847`, `generated_direct_call_transactions=152531`, `generated_direct_call_rootless_generated_entries=143119`, and `generated_direct_call_rootless_native_entries=9281`; filtered detail identifies the D56 owners as `GetGlobalObjectProperty`/`GetByName` for 57 and 60, `Equal`/`GetByName` for 81 and 72, and `ToNumber`/`GetByName` for 75. Full scoring-harness Richards attempts were not recorded because 6/5 and 2/1 runs stayed CPU-bound for over 12 and 5 minutes and were interrupted. The next dependency is hot-owner native lowering/exit coverage for property handoff gaps first, while keeping equality and `ToNumber` on generated/VM exits until their C++ fast-path invariants are proved.
- 2026-05-26: D58 P10 property native-exit metadata widening accepted. C++ audit tied `GetByName` to `op_get_by_id` and `GetGlobalObjectProperty` to `op_get_from_scope` GlobalProperty: both preserve the frame/base or implicit global metadata, perform the VM/IC property path, then write the destination only after the continuation. Rust now admits `GetGlobalObjectProperty` into retained P10 property native exits with the two-operand global-property shape, validates and executes it through the existing VM property handoff/sidecar path, and derives supplemental native property-exit metadata from current CodeBlock IC metadata while keeping generated-code artifact install on cold unlinked IC metadata. Focused tests prove exact P10 global metadata retention, first-run VM property transaction behavior, second-run global load sidecar hits, and the cold/current metadata split for warmed `GetByName` and `GetGlobalObjectProperty` ICs. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --example octane_probe`, `cargo test --lib -- --quiet` 2040 passed, `cargo build --release --example octane_probe`, and the comparable Richards loop with `fallbacks=0`, `baseline_entry_auto_materializations=38`, `baseline_native_lowering_failures=27`, `baseline_entry_artifacts=2`, `baseline_generated_code_artifacts=31`, `baseline_generated_executions=307535`, `baseline_generated_executed_bytecodes=2876831`, `generated_direct_call_transactions=152531`, `generated_direct_call_rootless_generated_entries=143117`, and `generated_direct_call_rootless_native_entries=9283`. Filtered Richards detail no longer reports `GetGlobalObjectProperty` or `GetByName` lowering failures; owners 57 and 60 now fail later at `SemanticByteEmission`, while the remaining lowering details are `Equal`/`NotEqual`, `ToNumber`, `BitAndInt32`/`BitOrInt32`, and `LoadCallee`. The next shared dependency is semantic-emission failure detail/support for the newly admitted property-native owners, plus conservative C++-backed native lowering for the remaining arithmetic/equality families.
- 2026-05-26: D59 semantic-byte-emission detail and frame-argument native operand support accepted. C++ audits confirmed Baseline property/call slow paths resume after frame-slot operands and that Rust's generated/native frame window stores locals before arguments; Rust now preserves compact `SemanticByteEmission` detail on auto-materialization records, reports `baseline_native_semantic_byte_emission_failures` through VM/Octane summaries, threads CodeBlock frame-local capacity into P6 lowering/backend contracts, and emits frame-argument operands as positive displacements from the pinned native frame base instead of rejecting `FrameArgumentUnsupported`. Focused tests prove semantic detail mapping, split lowering-vs-semantic failure counts, backend frame-argument contract recording, semantic emission of frame-argument sources, and the CallWithThis integration path accepting either native callee materialization or generated fallback. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --example octane_probe`, `cargo test --lib -- --quiet` 2044 passed, `cargo build --release --example octane_probe`, and the comparable Richards loop with `fallbacks=0`, `baseline_entry_auto_materializations=36`, `baseline_native_lowering_failures=27`, `baseline_native_semantic_byte_emission_failures=0`, `baseline_entry_artifacts=8`, `baseline_generated_code_artifacts=25`, `baseline_generated_executions=307321`, `baseline_generated_executed_bytecodes=2876195`, `generated_direct_call_transactions=152505`, `generated_direct_call_rootless_generated_entries=143089`, and `generated_direct_call_rootless_native_entries=9293`. Filtered Richards detail shows prior `FrameArgumentUnsupported` owners 51, 63, 66, 54, 57, and 60 now install native entries; remaining blockers are lowering for `LoadCallee`, `Equal`/`NotEqual`, `BitAndInt32`/`BitOrInt32`, and `ToNumber`.
- 2026-05-26: D60 exact int32 `BitAndInt32`/`BitOrInt32` emitted-native lowering accepted. C++ audits tied the slice to Baseline/DFG/FTL int32 bitwise fast paths: guard both operands as int32, emit `and`/`or`, retag an int32 result, and side-exit/fallback without writing on non-int32 rather than implementing generic JS `ToInt32` or BigInt semantics. Rust now adds exact P6/P8a/P8b `BitAndOr` native opcode subsets and emitter kinds, lowers only `BitAndInt32`/`BitOrInt32` through a separate bitwise policy with no overflow or negative-zero exits, emits x86_64 `and`/`or` bytes after int32 payload extraction, supports frame-argument bitwise operands, and preserves generated artifacts while supplemental Richards-shaped bitand owners install native. `LoadCallee` remains deferred because Rust's current raw native frame base does not expose the C++ call-frame callee header slot. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --lib`, focused `bitand_or` tests, focused P6 callable/P15 tests, `cargo test --lib -- --quiet` 2047 passed, `cargo check --example octane_probe`, `cargo build --release --example octane_probe`, and the comparable Richards loop with `fallbacks=0`, `baseline_entry_auto_materializations=36`, `baseline_native_lowering_failures=23`, `baseline_native_semantic_byte_emission_failures=0`, `baseline_entry_artifacts=12`, `baseline_generated_code_artifacts=21`, `baseline_generated_executions=284277`, `baseline_generated_executed_bytecodes=2738675`, `generated_direct_call_transactions=152505`, `generated_direct_call_rootless_generated_entries=129483`, and `generated_direct_call_rootless_native_entries=9293`. Filtered Richards detail no longer reports `BitAndInt32`/`BitOrInt32`; remaining lowering blockers are `LoadCallee`, `Equal`/`NotEqual`, and `ToNumber`, plus one P8b validation-shape gap.
- 2026-05-26: D61 emitted-native `LoadCallee` ABI carrier accepted. C++ audit tied `GetCallee`/`LoadCallee` to a non-allocating call-frame callee-slot read (`CallFrameSlot::callee`, LLInt/JIT frame-slot loads); Rust keeps observable behavior but uses an explicit C-ABI JSValue carrier because the current emitted-native frame base points at local0 rather than a full JSC call-frame header. P6 callable entries now pin the active callee bits in `r9`, P9 owner post-call reentry gets its own fifth-argument callee seed so result/profile carriers stay intact, lowering/backend contracts model `CallFrameCalleeValue`, and VM native entry rejects missing active callees before dispatch while non-callee bodies keep the undefined carrier. Focused tests cover P6 lowering/selection/emission, executable-memory callee-bit ABI passing, native `LoadCallee` returning the active function cell, and missing-callee fallback. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --lib`, `cargo test load_callee --lib`, `cargo test callee_bits --lib`, focused P9 reentry tests, `cargo test --lib -- --quiet` 2052 passed, `cargo check --example octane_probe`, and `cargo build --release --example octane_probe`. The short direct Richards eval sanity check returned with `fallbacks=0`, but the benchmark-runner Richards 2/1 and single `Benchmark.runIteration()` probes both hit the 120s cap, so no comparable Richards counter set is recorded for this slice; remaining hot blockers are `Equal`/`NotEqual`, `ToNumber`, and the P8b validation-shape gap.
- 2026-05-26: D62 exact int32 loose `Equal`/`NotEqual` emitted-native lowering accepted. C++ audit tied the slice to Baseline `op_eq`/`op_neq`: load operands, side-exit unless both are int32, compare payloads, box a boolean, and store only after the guards; generic loose equality still belongs to `operationCompareEq`/`JSValue::equalSlowCaseInline` because it can convert, allocate, call JS, and throw. Rust now adds exact P6/P8a/P8b `BitAndOrEquality` native opcode subsets and emitter kinds, lowers `Equal`/`NotEqual` through an int32 equality policy, emits x86_64 `cmp` plus inverted internal branch around true materialization, supports `JumpIfFalse` over the produced boolean, and falls back at the precise bytecode for non-int32 operands without writing the destination. Focused tests cover subset admission, emitter-kind validation, lowering/backend/selection/bytes, P8b `NotEqual` to `JumpIfFalse`, callable VM matches for local and argument int32 equality, and non-int32 side-exit fallback. Gates passed including `cargo fmt -- --check`, `cargo check --lib`, focused `equal`/`bitand` tests, `cargo test --lib -- --quiet` 2057 passed, `cargo check --example octane_probe`, and `cargo build --release --example octane_probe`. The comparable Richards loop now records `fallbacks=0`, `baseline_entry_auto_materializations=36`, `baseline_native_lowering_failures=17`, `baseline_native_semantic_byte_emission_failures=0`, `baseline_entry_artifacts=18`, `baseline_generated_code_artifacts=15`, `baseline_generated_executions=283929`, `baseline_generated_executed_bytecodes=2737867`, `generated_direct_call_transactions=152505`, `generated_direct_call_rootless_generated_entries=129483`, and `generated_direct_call_rootless_native_entries=9293`. Filtered detail no longer reports `Equal`/`NotEqual`; remaining blockers are six runtime-helper branch targets, five `ToNumber`, three `GreaterThanInt32`, two `LessThanInt32`, and one P8b validation-shape gap.
- 2026-05-26: D63 exact int32 relational emitted-native lowering accepted. C++ audit tied `LessThanInt32`/`LessEqualInt32`/`GreaterThanInt32`/`GreaterEqualInt32` to signed int32 Baseline compare fast paths: guard both operands as int32, compare payloads with signed conditions, box a boolean, and store only after guards. Rust now adds exact P6/P8a/P8b `BitAndOrEqualityRelational` native subsets and emitter kinds, lowers the four relational ops through a relational side-exit policy, emits signed x86_64 `cmp` plus inverted false branches (`jge`/`jg`/`jle`/`jl`), supports `JumpIfFalse` over the produced boolean, and preserves generated baseline for mixed relational+loose-equality owners because generated already handles no-call nullish/same-cell equality while emitted-native equality is still exact int32-only. Focused tests cover subset admission, emitter-kind validation, lowering/backend/selection/bytes, P8b relational-to-`JumpIfFalse`, callable VM matches for local and argument int32 comparisons, non-int32 side-exit fallback, and the mixed equality generated-loop guard. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --lib`, focused `relational`/`equality`/`bitand`/side-exit tests, `cargo test --lib -- --quiet` 2062 passed, `cargo check --example octane_probe`, and `cargo build --release --example octane_probe`. The comparable Richards loop remains unchanged at `baseline_native_lowering_failures=17`, `baseline_entry_artifacts=18`, `baseline_generated_code_artifacts=15`, `baseline_generated_executions=283929`, and `generated_direct_call_rootless_native_entries=9293`; filtered detail still reports relational owners where mixed loose equality forces conservative generated fallback, plus runtime-helper branch targets and `ToNumber`. The next dependency is C++-backed native no-call loose-equality widening or another shared blocker that reduces those mixed-owner lowering failures without regressing generated equality behavior.
- 2026-05-26: D64 emitted-native no-call loose-equality widening accepted. C++ audit confirmed Baseline still inlines only int32 equality for generic `op_eq`/`op_neq`, but null/undefined immediates, boolean/boolean, and same-cell identity are no-call cases as long as object-to-primitive, strings, BigInt, doubles/NaN, boolean-number coercion, and masquerades-as-undefined stay on side exits until their invariants are modeled. Rust now adds separate P6/P8a/P8b `NoCallLooseEquality` native subsets, lowers `Equal`/`NotEqual` through a generated-baseline no-call loose-equality fast path for int32/int32, nullish equality/inequality, boolean/boolean, same-cell identity, and nullish-vs-non-nullish false, rejects unsupported equality operands before writing the destination, and lets mixed relational+loose-equality owners choose the relational no-call-equality subset instead of falling back to generated-only selection. Focused tests cover subset/effect contracts, emitter-kind validation, selection/bytes for full-value and tag checks, P8b equality-to-`JumpIfFalse`, mixed relational+loose-equality lowering, callable no-call cases (`null == undefined`, `object != null`, boolean inequality, same-cell equality), and precise side-exit behavior for unsupported equality operands. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --lib`, focused `no_call_loose_equality`/`equality`/`relational` tests, `cargo test --lib -- --quiet` 2068 passed, `cargo check --example octane_probe`, and `cargo build --release --example octane_probe`. A direct `runRichards(); runRichards();` eval returned with `fallbacks=0` and `baseline_native_lowering_failures=0`, but the benchmark-runner Richards 2/1 and bounded direct loop probes were still CPU-bound and manually stopped after several minutes, so no comparable D63 counter set is recorded for this slice. The next dependency remains reducing the remaining runner hot-path blockers, especially `ToNumber` and runtime-helper/native-entry branch shapes, with C++ evidence before widening more coercive equality cases.
- 2026-05-26: D65 emitted-native branch-to-runtime-helper opcode labels accepted. C++ audits confirmed Baseline/LLInt branches target bytecode opcode labels and may land on opcodes that later slow-path; Rust audit kept JS-call/property native-exit branch targets rejected until their continuation/rooting proofs are separate. Rust now allows P8 branch targets whose target instruction is a retained `RuntimeHelperNativeExit`, so a branch can land on the helper opcode label, return the existing helper payload, dispatch through the existing root/safepoint/no-GC helper transaction, and resume after the helper without recording arithmetic fallback telemetry. Focused coverage proves lowering accepts `Jump -> NewObject` helper targets and the emitted native path dispatches `NewObject` exactly at the target label before resuming to `Return`; JS-call/property branch targets remain rejected. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --lib`, focused P8/runtime-helper tests, `cargo test --lib -- --quiet` 2069 passed, `cargo check --example octane_probe`, and `cargo build --release --example octane_probe`. The direct `runRichards(); runRichards();` sanity probe remained `fallbacks=0` and `baseline_native_lowering_failures=0`. The next dependency remains primitive emitted-native `ToNumber` with C++-backed number/boolean/null/undefined fast paths and side exits for cell/unknown, not broad conversion.
- 2026-05-26: D66 primitive emitted-native `ToNumber` accepted. C++ audits confirmed `op_to_number` fast-paths existing numbers by bitwise JSValue pass-through, while non-number conversion belongs to the slow path; only booleans, null, and undefined are safe primitive no-call conversions, with `undefined` materializing JSC pure NaN bits and cells/unknown values side-exiting before any destination write so the slow path can still read the original operand. Rust now adds P6/P8 emitted-native primitive-ToNumber subsets, lowers `ToNumber` to a guarded `ToNumberPrimitive` machine op, records an explicit `UnsupportedToNumberOperand` side-exit reason mapped to `UnsupportedPrimitiveNumericCoercionOperand` telemetry, updates emitted-native callable metadata to the widened supported subset, and preserves generated `ToNumber`/`Void` behavior separately. Focused coverage proves plan admission, lowering/selection/bytes, JSC pure-NaN materialization, primitive callable execution for int32/double/true/false/null/undefined, precise side exit for unsupported values with destination unchanged until interpreter replay, and callable-kind metadata. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --lib`, focused primitive-ToNumber/code metadata/P6 callable tests, `cargo test --lib -- --quiet` 2073 passed, `cargo check --example octane_probe`, and `cargo build --release --example octane_probe`. The direct `runRichards(); runRichards();` sanity probe remained `fallbacks=0` and `baseline_native_lowering_failures=0`. The next dependency is reducing the remaining runner hot-path gap with measured telemetry, not broadening `ToNumber` into string/object/BigInt/Symbol conversion.
- 2026-05-26: D67 branch-to-retained native-exit opcode labels accepted. C++/JetStream audits clarified that default Richards is 120 measured iterations and each `Benchmark.runIteration()` performs 50 `runRichards()` simulations, so direct two-call sanity probes are not comparable to the runner workload; C++ Baseline/LLInt branch emission links jumps to bytecode opcode labels without excluding later slow/property/call opcodes. Rust now allows validated P8 branch targets to land on retained JS-call and property native-exit labels, reusing the existing payload return stubs and VM-owned continuation/rooting/handoff validation instead of rejecting them during lowering. Focused coverage proves `JumpIfFalse -> GetByName` and `JumpIfFalse -> Call` dispatch through existing P10/P9 retained native exits and resume at `Return` without arithmetic fallback telemetry, while existing P10/P9 cold-path tests still pass. Gates passed including `cargo fmt -- --check`, `cargo check --lib`, focused branch/P10/P9 tests, `cargo test --lib -- --quiet` 2075 passed, and `cargo build --release --example octane_probe`. The filtered Richards two-simulation loop now drops from 15 to 2 native lowering failures, with remaining detail limited to one P8b validation-shape gap and one `RightShiftInt32` unsupported opcode; the next dependencies are benchmark-summary telemetry parity for hidden route/rejection evidence, then C++-backed native support for the remaining measured opcode/shape gaps.
- 2026-05-26: D68 benchmark tiering-summary telemetry parity accepted. Octane benchmark reports now carry per-benchmark deltas for general rootless rejection counts, rootless unsupported-opcode histograms, preferred native-entry counts, baseline generated-execution summaries, and generated direct-call transaction summaries, and `octane_probe --tiering-summary` prints those details alongside the existing scalar native-entry rejection counts. Focused coverage proves the vector/detail deltas stay per-benchmark instead of cumulative suite totals. Gates passed including `cargo fmt -- --check`, focused `octane_tiering_summary_delta_preserves_detailed_rootless_telemetry`, `cargo check --lib`, `cargo test --lib -- --quiet` 2076 passed, `cargo build --release --example octane_probe`, and `git diff --check`; a short baseline smoke probe was stopped after it stayed CPU-bound without useful output. C++ bit-shift audit identified the next safe native opcode slice as Int32-only `RightShiftInt32` via arithmetic shift with `rhs & 31` and non-int32 side exits before destination writes; `UnsignedRightShiftInt32` remains separate until UInt32-to-Number widening or a high-bit side exit is modeled.
- 2026-05-26: D69 emitted-native `RightShiftInt32` accepted. C++ audit tied the slice to Baseline/DFG int32 arithmetic shift-right: require both operands to be int32, mask the count with `& 31`, emit signed `sar`, retag an int32, and side-exit before destination writes for non-int32 operands with no overflow or negative-zero exit. Rust now admits `RightShiftInt32` into the active exact emitted-native BitAndOr subset family, lowers/selects it through the int32 bitwise policy, emits `and ecx, 31` plus `sar r10d, cl`, and keeps `LeftShiftInt32`, `BitXorInt32`, `BitNot`, and `UnsignedRightShiftInt32` out of this slice. Gates passed including `cargo fmt -- --check`, focused right-shift/bitwise/side-exit tests, `cargo check --lib`, `cargo test --lib -- --quiet` 2079 passed, and `cargo build --release --example octane_probe`; direct probes for `(-8) >> 33` and `runRichards(); runRichards();` returned with `baseline_native_lowering_failures=0`.
- 2026-05-26: D70 P8b rootless retained truthiness side-exit admission accepted. Subagent/C++ audits tied `JumpIfFalse` unsupported truthiness to JSC's normal `jfalse` slow continuation (`valueIsFalseyGenerator`/LLInt `slow_path_jfalse`) rather than a native-entry-invalidating exit. Rust now allows retained side exits in rootless emitted-native entry proof only when every site is a well-formed `UnsupportedTruthinessOperand` at a valid `JumpIfFalse` bytecode with matching side-exit ordinal/payload and valid branch target; all arithmetic/non-int32/overflow and malformed side exits still reject as `RetainedSideExit`. The proof uses a non-throwing deferred-rooting mode so a taken truthiness side exit can publish nonlocal roots before interpreter fallback without broadening throw permission. Focused tests cover the allowlist metadata, direct-call hot-slot P8b native-entry admission, and the existing arithmetic retained-side-exit rejection; the `rootless` suite passes. The Richards benchmark-form rootless probe was interrupted after roughly two minutes without a summary, and a tiny source-level direct-call probe stayed at hot-slot misses, so the durable proof for this slice is the VM hot-slot test plus the C++ slow-continuation evidence.
- 2026-05-26: D71 retained side-exit rejection telemetry accepted. Subagent/C++ audits kept the D70 truthiness admission narrow: `toBoolean` is non-throwing, but `NonInt32Operand` and `UnsupportedToNumberOperand` can enter conversion/call/throw paths, while arithmetic `Overflow`/`NegativeZero` are only safe with deferred non-throwing rooting and still co-reside with unsafe retained exits in current tables. Rust now records native-entry rootless retained-side-exit rejection histograms by target code block, bytecode index, decoded opcode, reason, and count, carries them through Octane per-benchmark tiering-summary deltas, and prints them from `octane_probe`. The retained-side-exit allowlist now explicitly accepts only truthiness plus non-throwing int32 overflow/negative-zero shapes and rejects `NonInt32Operand`/`UnsupportedToNumberOperand` until value-kind or throw-capable side-exit contracts exist. Gates passed including `cargo fmt`, focused allowlist/direct-call/rootless-summary tests, `cargo check --lib`, `cargo test rootless --lib`, `cargo test --lib` 2079 passed, and `cargo build --release --example octane_probe`. The bounded Richards x5 direct loop remains `fallbacks=0`, `baseline_native_lowering_failures=2`, `generated_direct_call_rootless_native_entries=12`, and `generated_direct_call_rootless_native_entry_reject_retained_side_exit=3994`; the new histogram shows the real blocker is unsafe `NonInt32Operand` on `Equal` at packed bci 20 (`19950`) and `GreaterThanInt32` at packed bci 52 (`7980`) in target `CodeBlockId(CellId(72))`, with truthiness exits already classified and small `ToNumber`/`AddInt32` mixed tables explaining why broad admission is still unsafe.
- 2026-05-26: D72 emitted-native `BitXorInt32` accepted. C++ audits tied the slice to LLInt `bitOp(bitxor)` and `JITBitXorGenerator`: guard operands as int32, emit `xor32`, box/retag an int32 result, and leave generic ToInt32/BigInt/object conversion on the slow path. Rust now admits `BitXorInt32` into the existing exact emitted-native bitwise subset family, lowers/selects it through the int32 bitwise policy, emits x86_64 `xor` bytes after int32 payload extraction, and covers callable local/argument execution plus non-int32 side-exit metadata without widening `BitNot`, shifts, or generic bitwise conversion. A parallel C++ side-exit audit confirmed D71's conservative boundary: generic `NonInt32Operand` retained side exits for loose equality or relational ops can call/throw and still require value-kind/effect metadata before rootless admission. Gates passed including `cargo fmt -- --check`, focused `bitxor`/`bitand_or`/P6 callable tests, `cargo check --lib`, `cargo check --example octane_probe`, `cargo test --lib` 2080 passed, `cargo build --release --example octane_probe`, and `git diff --check`. The bounded Richards x5 direct loop remains `fallbacks=0` and drops to `baseline_native_lowering_failures=1`, `baseline_installs=31`, `baseline_generated_code_artifacts=2`, `baseline_generated_executions=274`, `generated_direct_call_transactions=25`, and `generated_direct_call_rootless_native_entry_reject_retained_side_exit=2`; the remaining native lowering failure is a P8b no-call-loose-equality/truthiness validation-shape gap with runtime-helper root maps, not another unsupported opcode.
- 2026-05-26: D73 P8b runtime-helper root-map shape and variable P9 call-site metadata accepted. C++ audits confirmed Baseline/LLInt branches target bytecode labels independent of opcode kind, so P8b no-call loose equality plus `JumpIfFalse` may share the same runtime-helper native-exit root-map validation shape as exact equality/relational P8b paths when the existing snapshot, safepoint, root-map, no-handler, and native-exit checks remain intact. Rust now accepts generator-produced helper root maps that are mirrored in both unlinked and linked CodeBlock side tables, while still rejecting arbitrary extra unlinked maps, and native lowering telemetry now preserves the actual validation shape. A follow-on Richards probe exposed a separate invented 16-site cap on P9 JS-call/construct native-exit metadata; C++ uses vector-like call metadata and Rust payloads already carry a u32 site index, so the cap now follows payload capacity instead of the unrelated 16 argument-register limit. Focused coverage proves P8b `NotEqualNoCallLoose -> JumpIfFalse -> NewObject` lowers through the runtime-helper native-exit shape with mirrored root maps, existing P8a branch-to-helper coverage still passes, and 20 `Construct` P9 sites derive searchable metadata. The bounded Richards x5 direct loop still returns with `fallbacks=0`; its native lowering blocker advances from the D72 validation-shape failure through Construct metadata and now reports a single unsupported `Throw` at packed bci 404, which remains deferred until a C++-backed exception/retained-exit contract exists.
- 2026-05-26: D74 retained `Throw` native-exit admission accepted. C++ audits confirmed Baseline/LLInt `Throw` is terminal exception control flow through `operationThrow`/`genericUnwind`, not a generic helper, so Rust keeps direct native `Throw` blocked and admits only a VM-owned retained runtime-helper native-exit shape for zero-handler code blocks. Rust now generates source root maps for `Throw`, derives an `ExceptionThrow` runtime boundary with `may_throw`/no-GC/root-map proof, lowers `Throw` only as `RuntimeHelperNativeExit`, and proves heap-thrown values publish exception roots while helper roots and no-GC depth are restored. Gates passed including `cargo fmt -- --check`, `cargo check --lib`, focused `throw` tests, `cargo test --lib` 2087 passed, `cargo build --release --example octane_probe`, and `git diff --check`; the bounded Richards x5 direct loop now returns with `fallbacks=0` and `baseline_native_lowering_failures=0`, leaving rootless native-entry retained side-exit rejections for unsafe `NonInt32Operand`/overflow shapes as the next blocker rather than `UnsupportedOpcode Throw`.
- 2026-05-26: D75 retained side-exit root transaction accepted. C++ audits confirmed `jfalse` truthiness and post-int32-guard arithmetic overflow are non-calling side exits, while broad `NonInt32Operand` replay for add/equality/relational/bitwise opcodes can call, allocate, or throw and therefore needs full root publication before interpreter replay. Rust now keeps deferred nonlocal roots alive across retained P6 side-exit fallback and nested completion draining, adds a throwing-capable side-exit rootless proof for validated replayable `NonInt32Operand` sites, and still rejects `UnsupportedToNumberOperand` until its conversion effects are modeled. Focused side-exit/rootless suites passed, full `cargo test --lib` passed 2087 tests, `cargo fmt -- --check`, `cargo check --lib`, and `cargo build --release --example octane_probe` passed. The bounded Richards x5 direct loop remains `fallbacks=0` and `baseline_native_lowering_failures=0`, with `generated_direct_call_rootless_native_entry_reject_retained_side_exit=0`; the next measured blocker is `generated_direct_call_rootless_native_entry_reject_retained_runtime_helper_exit=2`.
- 2026-05-26: D76 retained runtime-helper native-entry rootless admission accepted. C++ audits confirmed helper opcodes are real Baseline/LLInt bytecode labels/slow paths, `NewObject` can allocate/GC, `Throw` is exceptional/unwind control flow, and C++ publishes frame/root state before helper/native calls. Rust now admits retained P6 runtime-helper native exits in rootless emitted-native direct-call entries only when the retained table exactly matches the derived runtime-helper plan: snapshot, eligible helper family, bytecode index, opcode, safepoint, root map, root count, no-GC reentry, may-throw, and helper payload ordinal. Body validation now allows helper opcodes only through validated retained helper sites, and the proof uses `DeferredGeneratedExitRooting` so the existing helper transaction publishes deferred nonlocal roots before helper dispatch and syncs exception roots on throw. Focused runtime-helper/rootless suites passed, full `cargo test --lib` passed 2089 tests, `cargo fmt -- --check`, `cargo check --lib`, `cargo build --release --example octane_probe`, and `git diff --check` passed. The bounded Richards x5 direct loop remains `fallbacks=0` and `baseline_native_lowering_failures=0`; `generated_direct_call_rootless_native_entry_reject_retained_runtime_helper_exit` drops to 0 and `generated_direct_call_rootless_native_entries=2`. A benchmark-runner Richards 2/1 probe still timed out at 90s without a summary, so the next frontier is runner-throughput classification rather than another direct-loop native-entry rejection.
- 2026-05-26: D77 Richards runner-throughput classification accepted. JetStream/C++ audit confirmed local JS3 defaults are 120/4, Richards has no overrides, `Benchmark.runIteration()` calls `runRichards()` 50 times, and the smallest legal `2/1` scorer therefore runs 100 scheduler simulations; local C++ JSC completes one simulation at roughly 1.75 ms and one `runIteration()` body at roughly 6.78 ms. Rust `octane_probe` now exposes the existing Octane `DispatchConfig` as `--dispatch-steps N` for benchmark/suite runs, while leaving raw `--eval` probes unchanged; a zero-budget Richards run reports `DispatchStepLimitExceeded` through normal Octane failure/tiering output, but nonzero 1k/100k budgets still time out inside `Generated(Runner)`, proving the current budget is not a useful hot-runner interrupt. Raw Rust baseline probes separate scorer overhead from scheduler throughput: `runRichards()` x1 returns in about 1.34-1.36 s and x50 returns in about 73.0 s with `baseline_native_lowering_failures=0`, `generated_direct_call_transactions=0`, and the lone generated program owner falling back once at its first JS call before the scheduler body runs on the slow path. Gates passed including `cargo fmt -- --check`, `cargo check --example octane_probe`, focused `octane_execution_honors_explicit_dispatch_budget`, `cargo build --release --example octane_probe`, help/zero-budget probe checks, bounded raw Richards x1/x50 probes, and `git diff --check`. The next dependency is C++-backed generated JS-call continuation/direct-call engagement for the runner/program call into Richards, not more rootless native-entry admission.
- 2026-05-26: D78 `GetGlobalLexical` generated fallback resume accepted. C++ audit confirmed Baseline's first JS-call slow miss preserves the owner call continuation and returns to `doneLocation`; it is not expected to produce a direct-call transaction on the first hit. Rust audit then corrected the D77 blocker: the generated Program exited at `GetGlobalLexical` immediately before the `runRichards` call, so no generated JS-call handoff was created. Rust now admits `GetGlobalLexical` to the existing generated single-dispatch fallback transaction, preserving interpreter TDZ/read-only/binding semantics while resuming generated execution at the following bytecode; `octane_probe --tiering-summary` also reports `call_link_inline_cache_attachments` so plan/recheck/attachment gaps are visible. Focused regression proves a generated global-lexical read resumes into the following `Call`, records the cold call observation, and attaches monomorphic call-link metadata. Gates passed including `cargo fmt -- --check`, `git diff --check`, focused global-lexical/resume tests, focused `octane_execution_honors_explicit_dispatch_budget`, `cargo check --example octane_probe`, and `cargo build --release --example octane_probe`. Raw Richards probes now show the intended surface: x5 returns with `js_call_count=5`, `generated_direct_call_transactions=4`, `generated_direct_call_native_entries=3`, `generated_direct_call_nested_interpreter_fallbacks=1`, and `call_link_inline_cache_attachments=53`; x10 returns with `js_call_count=10`, `generated_direct_call_transactions=9`, and `generated_direct_call_native_entries=8`. The x50 raw loop still hit a 160s timeout without summary output, so the next dependency is scheduler-body throughput after the program call is hot, not call-link attachment starvation.
- 2026-05-26: D79 hot property-probe telemetry aggregation accepted. Parallel C++/Rust audits split the D78 surface: C++ points to JSC-like nested/polymorphic call and property IC consumption inside the Richards scheduler, while Rust telemetry proved the current immediate blocker was millions of retained property sidecar miss records rather than top-level call-link starvation. Rust now caps retained hot profile, generated direct-call transaction, generated property-load probe miss, guarded property-load miss, and property-store probe miss records while preserving aggregate miss/backedge counts and all terminal load-miss invalidation behavior; the VM also aggregates non-terminal post-cap property misses instead of constructing full tiering records and ordinals for each `StructureMismatch`. `octane_probe` now prints hidden property IC/probe/stub counters and retained-record counts. Gates passed including `cargo fmt -- --check`, `git diff --check`, focused telemetry-retention/property-sidecar tests, `cargo check --example octane_probe`, and `cargo build --release --example octane_probe`. Raw Richards x10 returns with unchanged call semantics but `record_ordinals` reduced from about 6.75M to 6.5k while preserving `generated_property_load_probe_misses=6068212` and `generated_property_store_probe_misses=684154`; x20 returns with `generated_property_load_probe_misses=28182397` and `generated_property_store_probe_misses=3153449`, confirming remaining time is actual property IC miss scanning/hit-rate, not retained telemetry. A shortcut that retired monomorphic-slot attachment rejections was tested and reverted after it regressed the probe; the next dependency is C++-backed polymorphic/structure-stub property IC behavior and candidate selection, not plan-retirement shortcuts.
- 2026-05-26: D80 generated property sidecar structure-guard prefilter accepted. Parallel C++/Rust audits confirmed Baseline JSC property ICs walk local `(structureID, key, access-case)` guards and call the C++ slow path only after the handler chain misses; Rust had been VM-probing every same-site/key candidate and letting the host rediscover `StructureMismatch`. Rust now exposes an optional `DispatchHost::generated_property_sidecar_base_structure` query, implements it for non-proxy Core objects, and uses it in generated own-data load and named replace/transition store sidecars to skip known-wrong structures without recording a VM probe miss. Guarded prototype loads and indexed plans stay on the existing host probe path because their miss reasons/lifecycles differ. Focused unit and VM coverage proves both compatibility with hosts that do not expose the query and the new Core path with two receiver shapes where only the matching candidate reaches the host. Gates passed including `cargo fmt -- --check`, `git diff --check`, focused sidecar-prefilter tests, existing load/store sidecar regression tests, `cargo check --example octane_probe`, and `cargo build --release --example octane_probe`. Bounded Richards x10 and x2 benchmark probes were stopped after staying CPU-bound without summary output, so the next dependency is to rerun bounded raw scheduler probes and then add C++-shaped polymorphic/structure-stub case selection/buffering rather than lifecycle shortcuts.
- 2026-05-26: D81 newest-first generated property sidecar candidate order accepted. C++ audits confirmed Baseline handler ICs prepend new property handlers and repatching IC cascade code emits cases in reverse order after the local inline guard, while LLInt remains monomorphic; Rust audit showed generated sidecar tables preserved insertion order and sidecars consumed candidates oldest-first. Rust now adds newest-first filtered iterators for own-data load and store mutation candidate tables and uses them only in generated property load/store sidecar dispatch, leaving table storage order, guarded prototype-load candidates, table builders, and VM attachment projection unchanged. Focused table/baseline/VM coverage proves storage remains stable, generated load/store sidecars probe the newest matching plan first, known-wrong structures are still skipped locally before host probing when the newer plan mismatches, and forced transition-store tests pass only when the test host reports the plan's guarded base structure. Gates passed including `cargo fmt -- --check`, `git diff --check`, focused newest-first and structure-prefilter tests, existing load/store sidecar regressions, full `cargo test --lib -- --quiet` with 2100 passed, `cargo check --example octane_probe`, and `cargo build --release --example octane_probe`. The prepared raw Richards x10 probe still returns quickly with `generated_property_load_probe_misses=0`, `generated_property_store_probe_misses=0`, `property_inline_cache_attachments=63`, and no direct-call transactions on that prepared raw surface; the scorer-body throughput blocker observed in D80 remains separate. The next shared dependency is the C++ per-site property IC evolution gate: countdown/cooldown/buffering plus duplicate suppression keyed by structure or `(structure,key)`, before deeper case replacement or megamorphic folding.
- 2026-05-26: D82 generated property IC evolution buffering accepted. C++ audits traced `PropertyInlineCache::shouldRepatch`, repatch buffering defaults, structure/key duplicate suppression, and cooldown growth; Rust audit placed the gate at observation-to-plan publication before generated sidecars see candidates. Rust now tracks per-site load/store evolution state keyed by owner, bytecode snapshot, slot, bytecode index, and access family, buffers named duplicates by `StructureId`, buffers indexed duplicates by `(StructureId, CacheKey)`, suppresses duplicate plan publication during the buffering window, enters exponential cooldown after the repatch threshold, and resets buffered structures after accepted IC clears. Guarded prototype/negative loads, full code regeneration, `canReplace`, binary switch stubs, final/megamorphic folding, and executable patch lifecycle remain deferred. C++'s initial `countdown = 1` is also deferred as a separate migration because the current Rust observation point and broad VM tests model the first cacheable handoff as the first publishable plan; D82 still records cooldown decisions after the threshold so the state shape is ready for that follow-up. `octane_probe --tiering-summary` and benchmark deltas now expose evolution records, admitted plans, buffered structures, duplicate suppressions, and cooldown skips. Gates passed including `cargo fmt -- --check`, `git diff --check`, focused evolution/generated-property/Octane-summary tests, full `cargo test --lib -- --quiet` with 2103 passed, `cargo check --example octane_probe`, and `cargo build --release --example octane_probe`. The prepared raw Richards x10 probe returns with `generated_property_load_probe_misses=0`, `generated_property_store_probe_misses=0`, `property_inline_cache_evolution_records=63`, `property_inline_cache_evolution_admitted=63`, `property_inline_cache_evolution_buffered=63`, duplicate/cooldown skips at 0, and `property_inline_cache_attachments=63`; the next shared dependency is the deferred initial countdown and then C++-backed case replacement/megamorphic behavior.
- 2026-05-26: D83 C++ initial property IC countdown accepted. C++ audit confirmed Baseline `HandlerPropertyInlineCache` starts at `countdown = 1`, so the first qualifying optimized slow-path observation only consumes the countdown and cannot repatch, while LLInt remains outside this `PropertyInlineCache` countdown path; Rust audit kept the gate at the observation-to-plan publication boundary. Rust now initializes per-site property IC evolution state with countdown 1, records first-sighting skips as `SkippedInitialCountdown`, and updates generated/load/store/native/rootless/resume tests to warm through real VM observations instead of forcing plans. Gates passed including focused property IC/native/generated/rootless/resume suites, full `cargo test --lib -- --quiet` with 2103 passed, `cargo fmt -- --check`, `git diff --check`, `cargo check --example octane_probe`, and `cargo build --release --example octane_probe`. The prepared raw Richards x10 probe now returns with the same 63 evolution records but `property_inline_cache_evolution_admitted=0`, `property_load_access_case_plans=0`, `property_store_access_case_plans=0`, and `property_inline_cache_attachments=0`, proving this one-sighting surface now matches the C++ initial-countdown rule; the next shared dependency is C++-backed replacement/case growth and megamorphic behavior after second sightings.
- 2026-05-27: D84 Baseline handler `canReplace` projection accepted. C++ audit confirmed HandlerIC compiles immediately after countdown, suppresses a new case when an already-listed case can replace it, prepends non-replaced handlers for newest-first probing, and leaves FTL full-regeneration pruning/binary-switch behavior separate. Rust now applies a conservative VM projection helper to own-load plan tables, store plan tables, and generated store mutation candidate tables: same owner/site/key/kind/base/holder/global/dependency/effect/stub shape suppresses a new replaceable case while unrelated structures still accumulate, and stores also require the same transition target. Bytecode IC state remains monomorphic metadata-only; max-8 final/gave-up and megamorphic folding remain deferred. Gates passed including `cargo fmt -- --check`, focused load/store/candidate projection tests, focused evolution and generated sidecar newest-first tests, `cargo check --lib`, full `cargo test --lib -- --quiet` with 2106 passed, `cargo build --release --example octane_probe`, focused load/store eval probes with two admitted/attached cases and zero generated property probe misses, and the prepared raw Richards x10 probe still returning with `property_inline_cache_evolution_records=63`, `property_inline_cache_evolution_admitted=0`, no property plans, and no property attachments on the one-sighting surface. The next shared dependency is second-sighting Octane surfaces plus C++ max-8 final/gave-up and megamorphic promotion behavior.
- 2026-05-27: D85 Baseline handler `MadeNoChanges` and max-8 final/gave-up accepted. C++ audits confirmed `Options::maxAccessVariantListSize = 8`, HandlerIC keeps the eighth generated case and then switches future misses to the gave-up slow operation, while megamorphic folding replaces the handler chain only when a real megamorphic cache/slow op is generated. Rust now suppresses publication when an active/attached same-site case can replace the new load/store case before it reaches plan tables, counts only non-replaced admitted cases toward the per-site access-case cap, admits the eighth case, marks the site terminal as `GaveUp`, and records later observations as `SkippedGaveUp`; it does not fake megamorphic behavior without a Rust megamorphic cache model. Octane/probe summaries expose final-gave-up transitions and gave-up skips. Gates passed including focused property evolution/projection tests, generated get/store sidecar/probe tests, `cargo check --lib`, `cargo check --example octane_probe`, full `cargo test --lib -- --quiet` with 2109 passed, and `cargo build --release --example octane_probe`. A synthetic polymorphic load probe reports `property_load_access_case_plans=8`, `property_inline_cache_evolution_admitted=8`, `property_inline_cache_evolution_final_gave_up=1`, and `property_inline_cache_evolution_gave_up_skips=1`; prepared raw Richards x10 remains the C++-shaped one-sighting surface with `property_inline_cache_evolution_records=63`, `admitted=0`, no property plans, and no terminal events. The next shared dependency is a C++-backed megamorphic-cache/slow-op model plus second-sighting Octane surfaces that actually reach the max/fold boundary.
- 2026-05-27: D86 load-only GetById megamorphic metadata cache accepted. C++ audit kept the slice to HandlerIC `GetById`/`GetByIdWithThis` load folding and required exact key exclusions (`length`, `name`, `prototype`, `__proto__`, and array-index names) plus a real cache/slow-op boundary rather than relabeling D85 `GaveUp`. Rust now threads a runtime-proven `can_use_get_by_id_megamorphic` fact from property-load observations, folds only eligible data-only own `GetByName` load sites at the eighth accepted case into terminal `MegamorphicLoad`, records later hits as `SkippedMegamorphicLoad`, suppresses the old finite handler plans/attachments for that site, and publishes VM-owned megamorphic load cache records that generated sidecars can consume through the existing validated structure/key/offset probe path. Ineligible indexed loads and stores still use D85 final `GaveUp`, and the C++ direct-mapped primary/secondary `MegamorphicCache` tables, epoch checks, distinct `LoadMegamorphic` access-case kind, store/has/by-val support, and real slow-op replacement remain deferred. Gates passed including focused key-gate and property-evolution tests, `cargo check --lib`, `cargo check --example octane_probe`, full `cargo test --lib -- --quiet` with 2111 passed, `cargo fmt -- --check`, `cargo build --release --example octane_probe`, and `git diff --check`. The next shared dependency is replacing this metadata cache with C++-shaped VM-wide megamorphic cache tables/epochs and finding second-sighting Octane surfaces that actually populate them.
- 2026-05-27: D87 load-only megamorphic primary/secondary cache table accepted. C++ audit tied the shape to `MegamorphicCache`: load primary 2048, secondary 512, `uint16_t` epoch starting at 1 with 0 invalid, `UINT16_MAX` offset cap, primary-first lookup, current-epoch primary displacement into the secondary table, stale-entry discard, and exact `StructureID`/UID matching before any hit is consumed. Rust now replaces the D86 per-record source with a VM-owned load cache substrate under tiering integration: megamorphic sites are registered separately from telemetry, current own-data load hits insert into primary/secondary tables with epoch filtering and offset validation, generated property sidecar projection reads current table entries, and `octane_probe` reports current megamorphic cache entry count and epoch. Rust uses atom-slot hashing as a stable substitute for C++ `UniquedStringImpl` pointer hashing while retaining exact key equality, and still routes projected hits through the existing validated structure/key/offset probe path. Direct C++-shaped primary/secondary lookup at probe time, production GC/watchpoint epoch aging, holder/miss/prototype entries, a distinct `LoadMegamorphic` access-case kind, store/has/by-val tables, and moving mutable cache state out of tiering telemetry remain deferred. Gates passed including `cargo check --lib`, `cargo check --example octane_probe`, full `cargo test --lib -- --quiet` with 2113 passed, `cargo fmt -- --check`, `cargo build --release --example octane_probe`, and `git diff --check`. The next shared dependency is direct `(baseStructure, key)` megamorphic lookup in generated/native sidecars so sites stop widening by property key through projected plan tables.
- 2026-05-27: D88 direct generated-side megamorphic lookup accepted. C++ audit confirmed generated `LoadMegamorphic` probes primary first, goes directly slow when primary structure/key matches but epoch is stale, checks secondary only after a primary structure/key mismatch, and decodes holder null/own/prototype entries after epoch validation. Rust now removes D87 key-wide megamorphic projection from finite load access-case tables, exposes an immutable `GeneratedPropertyLoadMegamorphicCandidateTable` snapshot with owner, epoch, sites, and primary/secondary entries, and wires P10/native-exit plus generated execution sidecars to query the active base `StructureId` and perform direct `(slot, bytecode index, key, structure)` lookup before finite property-load candidates. Hits still synthesize a data-only own-load plan and pass through the existing validated host probe, while missing active base-structure data, stale epochs, wrong sites, and structure misses fall back without widened key scans. Focused tests cover same-primary stale blocking secondary, secondary after primary mismatch, finite table suppression for megamorphic sites, direct sidecar hits, missing structure fallback, and unrelated megamorphic sites avoiding host structure queries. Deferred gaps remain holder null/miss/prototype entries, real `LoadMegamorphic` access-case kind, production GC/watchpoint epoch aging, store/has/by-val tables, and moving mutable cache ownership out of tiering telemetry. Gates passed including focused D88 tests, `cargo check --lib`, `cargo check --example octane_probe`, `cargo fmt -- --check`, `git diff --check`, full `cargo test --lib -- --quiet` with 2118 passed, and `cargo build --release --example octane_probe`. The next shared dependency is filling the C++ slow-op population/holder semantics rather than broadening the current own-data-only hit shape.
- 2026-05-27: D89 terminal-null megamorphic load miss entries accepted. C++ audit confirmed `LoadEntry` encodes own data with the sentinel holder, prototype hits with the holder object, and absence with `holder == nullptr`, `offset == 0`, exact UID/structure/epoch validation, and direct `undefined` return; slow-op miss population is valid only after a cacheable terminal-null prototype-chain proof, not for arbitrary undefined slow-path results. Rust now splits generated load megamorphic entries into typed `OwnData { offset }` and `Missing` variants, lets direct generated-side lookup return `Missing` after the same site/key/structure/epoch gates, and has generated property sidecars write `undefined` without host probing or destination root sync. Miss observations can feed the existing property IC evolution gate and populate VM-owned megamorphic miss entries only for named `GetById` terminal-null, depth-0, one-entry chains whose chain object matches the base object; prototype-chain misses remain on the guarded negative path until production epoch/prototype invalidation is modeled. Focused tests cover direct missing lookup, generated sidecar undefined writes without host probes, own-plus-missing terminal cache population, and prototype-chain miss suppression. Gates passed including `cargo check --lib`, `cargo check --example octane_probe`, `cargo fmt -- --check`, `git diff --check`, full `cargo test --lib -- --quiet` with 2122 passed, and `cargo build --release --example octane_probe`. The next shared dependency is holder-object prototype entries and production invalidation/aging before widening miss caching beyond terminal-null own-base absence.
- 2026-05-27: D90 guarded prototype-holder megamorphic load entries accepted. C++ audit confirmed prototype `LoadMegamorphic` entries store the actual `slotBase` holder object plus a `uint16_t` offset, while generated lookup still trusts only base `StructureID`, UID, and epoch before loading from the holder; structure-chain invalidation globally bumps the megamorphic cache epoch, and GC aging is mandatory in C++ because holders are raw `JSCell*` values. Rust now adds a typed `PrototypeData { holder, offset }` megamorphic entry and lookup result, a generated holder-probe host boundary that revalidates holder existence, named key, data property, and offset, and generated sidecar handling that writes/protects the loaded value like other property hits. Population is intentionally narrower than C++ for now: only an already-terminal megamorphic load site can receive a prototype-holder entry, and only after an accepted guarded prototype-chain watchpoint materialization proves the holder chain; accepted guard invalidation ages the VM load megamorphic cache epoch so stale holder entries stop matching. Focused tests cover direct prototype lookup, generated sidecar holder dispatch without own/guarded probing, interpreter holder hits and stale metadata misses, terminal-plus-materialized prototype insertion, non-terminal/rejected materialization suppression, and watchpoint invalidation aging. Gates passed including `cargo check --lib`, `cargo check --example octane_probe`, `cargo fmt -- --check`, `git diff --check`, full `cargo test --lib -- --quiet` with 2129 passed, and `cargo build --release --example octane_probe`. Remaining gaps are production GC-aging hooks for the Rust megamorphic cache, string-base normalization, dictionary/override predicates beyond current observation facts, and broader C++ slow-op population not dependent on guarded materialization.
- 2026-05-27: D91 load megamorphic cache collection-aging hook accepted. C++ audit tied cache aging to `Heap::finalize()` after the actual Eden/full collection scope is known: Eden bumps the shared epoch, full bumps the epoch and clears entries, and wrap to invalid epoch resets to 1. Rust now corrects full-aging semantics to keep the incremented epoch unless wrap occurs, records load-megamorphic aging events for collection completion and structure-chain invalidation, ignores unresolved `CollectionKind::Any` requests because they are not completed scopes, and exposes a narrow VM-owned collection-completion hook for the future collector driver without coupling `gc::Heap` back to tiering. Focused tests cover Eden aging, full clearing, epoch wrap reset, VM hook dispatch, and rejected watchpoint dispatch avoiding aging. Gates passed including `cargo fmt -- --check`, `cargo check --lib`, `cargo check --example octane_probe`, `git diff --check`, full `cargo test --lib -- --quiet` with 2132 passed, and `cargo build --release --example octane_probe`. Remaining gaps are wiring this hook from a real collector completion path once implemented, sharing the epoch across future store/has megamorphic caches, string-base normalization, dictionary/override predicates, and broader C++ slow-op population.
- 2026-05-27: D92 string-primitive GetById megamorphic normalization accepted. C++ audits confirmed generated `LoadMegamorphic` normalizes primitive string bases to `String.prototype` before lookup, while slow-op insertion records even own `String.prototype` data hits as holder/prototype entries keyed by `String.prototype` structure because the raw base is still the primitive string; DFG/FTL `GetByIdWithThisMegamorphic` remains excluded pending a separate proof. Rust now records eligible primitive-string `GetByName` own data hits on `String.prototype` with explicit `StringPrototype` base normalization, keeps excluded names/indices opaque, normalizes generated sidecar base-structure queries for primitive strings, suppresses finite own-load plans for normalized observations, and populates VM load megamorphic `PrototypeData { holder: String.prototype, offset }` entries after the existing terminal evolution gate. Prototype-above-string hits and misses remain deferred. Focused tests cover normalized observation draining, excluded `length`, finite-plan suppression, generated sidecar structure normalization, and terminal holder-entry population. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --lib`, `cargo check --example octane_probe`, full `cargo test --lib -- --quiet` with 2136 passed, and `cargo build --release --example octane_probe`. Remaining gaps are direct slow-op population for prototype-above-string/misses, dictionary/override predicates, production collector wiring for cache aging, and future store/has megamorphic caches.
- 2026-05-27: D93 immediate-prototype load megamorphic population accepted. C++ audits confirmed `getByIdMegamorphic` directly inserts prototype data hits from the slow operation when the key is megamorphic-eligible, the hit is a cacheable data slot within offset bounds, the base is non-dictionary/mono-proto for prototype hits, and the entry is keyed by base structure with the real holder pointer; C++ relies on global structure-chain epoch invalidation for deeper chains. Rust now feeds terminal megamorphic evolution for descriptor-proven immediate prototype data hits and records VM load megamorphic `PrototypeData { holder, offset }` entries directly from the observation, while retaining D90 guarded-materialization population and deferring deeper direct prototype chains because current generated holder probes do not validate intermediate absence/prototype links. Rust's ordinary object model has one prototype pointer and no dictionary mode for this slice; base own-property/prototype-link mutations change base structure, and holder mutation is revalidated by the existing holder probe. Focused tests cover direct immediate-prototype insertion, deeper-chain direct deferral, existing guarded materialization behavior, and prototype miss suppression. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --lib`, `cargo check --example octane_probe`, focused `prototype_holder` tests, full `cargo test --lib -- --quiet` with 2138 passed, and `cargo build --release --example octane_probe`. Remaining gaps are global structure-chain invalidation for arbitrary/deeper slow-op holder and miss population, dictionary/override predicates, production collector wiring for cache aging, and future store/has megamorphic caches.
- 2026-05-27: D94 global structure-chain load-megamorphic invalidation accepted. C++ audits confirmed `VM::invalidateStructureChainIntegrity` globally bumps the megamorphic cache epoch for prototype-relevant add/remove/change/prototype mutations, while C++ population still requires cacheable mono-proto/non-dictionary chain proofs and uses epoch validation rather than per-entry chain checks in generated code. Rust now records a separate `StructureChainInvalidationEvent` for every shape/prototype transition independently of structure watchpoint listeners, drains those events at VM property-IC safepoints before sidecar reuse, ages the VM load-megamorphic epoch exactly once per event, and makes generated megamorphic sidecars refuse stale snapshot use while an invalidation is pending in the same generated run. Guarded-watchpoint invalidation still retires guarded cases but no longer directly ages the megamorphic cache. With the global epoch boundary in place, descriptor-proven deeper prototype hits and prototype-chain terminal misses can now populate direct VM load-megamorphic `PrototypeData` and `Missing` entries after the existing terminal evolution gate. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --lib`, focused structure-chain/megamorphic/prototype-holder tests, full `cargo test --lib -- --quiet` with 2143 passed, `cargo check --example octane_probe`, and `cargo build --release --example octane_probe`. Remaining gaps are C++ dictionary/override/impure-structure predicates, production collector wiring for cache aging, string prototype-above-string/miss normalization, and future store/has megamorphic caches.
- 2026-05-27: D95 primitive-string prototype-chain megamorphic normalization accepted. C++ audits confirmed both generated `LoadMegamorphic` and slow `GetById` insertion normalize primitive string bases to `String.prototype`, then key entries by the current `String.prototype` structure for data hits on `String.prototype`, data hits above it, and terminal misses; excluded names, indexed names, `length`, accessors, impure/dictionary structures, and non-mono-proto absence still stay off this path. Rust now reuses the existing recorded lookup starting at `String.prototype` for eligible primitive string `GetByName` misses and data hits above `String.prototype`, marks those records with `StringPrototype` base normalization, preserves the normalized chain for rooting/descriptor drain, and allows normalized string prototype-chain holder/miss observations to populate VM load-megamorphic `PrototypeData`/`Missing` entries after the terminal evolution gate. Accessors and excluded/indexed string properties remain opaque. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --lib`, focused string/prototype-chain/megamorphic tests, full `cargo test --lib -- --quiet` with 2147 passed, `cargo check --example octane_probe`, and `cargo build --release --example octane_probe`. Remaining gaps are C++ dictionary/override/impure-structure predicates, production collector wiring for cache aging, future store/has megamorphic caches, and broader Octane second-sighting/perf measurement after the widened load cache surface.
- 2026-05-27: D96 VM collection-finalization megamorphic aging boundary accepted. C++ audits confirmed `Heap::finalize()` ages `VM::megamorphicCache()` after sweep/finalization using the completed collection scope, while `VM::invalidateStructureChainIntegrity()` separately bumps the epoch for prototype-chain mutations; C++ ages load/store/has caches together, but Rust still has only the load cache. Rust now replaces the cache-specific VM test hook with a generic `finalize_completed_collection` VM boundary that leaves the heap collection phase and records load-megamorphic aging from the completed Eden/full kind, keeping cache mechanics in tiering integration and avoiding per-request GC callback ownership. Focused tests cover Eden completion through the generic VM boundary and full completion from `GcPhase::End` back to `NotRunning`. Bounded probes showed the reduced Richards benchmark-eval path still returns without exercising the real runner hot loop, while the smallest legal scored Richards run still hit external timeouts, so the benchmark blocker remains runner throughput/coverage rather than a semantic regression from D95. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --lib`, focused collection-finalization tests, full `cargo test --lib -- --quiet` with 2148 passed, `cargo check --example octane_probe`, and `cargo build --release --example octane_probe`. Remaining gaps are wiring this boundary from a real collector driver once collection execution exists, sharing epoch aging across future store/has megamorphic caches, C++ dictionary/override/impure-structure predicates, and broader Octane runner throughput work.
- 2026-05-27: D97 load-megamorphic cacheability choke point accepted. C++ audits confirmed `LoadMegamorphic` creation and slow-op population only admit cacheable named `GetById` load/miss cases that cannot call JS, reject accessors/custom slots, global-proxy and non-load cases, require C++ key exclusions, and additionally depend on dictionary/override/impure-structure predicates not yet represented in Rust's active `CoreObjectCell` model. Rust now centralizes the common descriptor gate used by own-load, normalized string, prototype-holder, miss, and guard-materialized prototype megamorphic population paths: the descriptor must be runtime-proven GetById-megamorphic eligible, be a property load, be cacheable, avoid JS calls, and carry a supported named string key before shape-specific checks can run. Focused regression proves an already-terminal megamorphic site does not accept `Disallowed`, opaque-tainted, JS-calling, or getter observations into the VM load-megamorphic cache. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --lib`, focused megamorphic-cache tests, full `cargo test --lib -- --quiet` with 2149 passed, `cargo check --example octane_probe`, and `cargo build --release --example octane_probe`. Remaining gaps are threading active dictionary/method-table cacheability facts into descriptor formation once those object-model paths exist, sharing the choke point with future store/has megamorphic caches, and broader Octane runner throughput work.
- 2026-05-27: D98 store megamorphic cache substrate accepted. C++ audits confirmed store megamorphic is a separate `MegamorphicCache` table populated from `put_by_id` slow operations, with primary/secondary lookup keyed by bytecode slot, key, old structure, current epoch, and replace-vs-transition store modes rather than widened finite access-case plans. Rust now adds a VM-owned store megamorphic table, site registry, epoch aging alongside the load cache, direct generated-side store lookup, store evolution folding at the eighth eligible named `PutById` case, generated sidecar probing/commit through the existing host-owned store boundary, and `octane_probe` counters for generated/skipped store megamorphic events and current store-cache entries. The first slice is deliberately narrow: named string property stores only, own data replace plus created-property transition plans, slow-path fallback with write-barrier evidence, no indexed/by-val stores, and transition capacity/reallocation fidelity still deferred to the host commit boundary. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --lib`, focused generated-side/VM/baseline/store-summary tests, full `cargo test --lib -- --quiet` with 2152 passed, `cargo check --example octane_probe`, `cargo build --release --example octane_probe`, and a bounded synthetic store probe showing `property_inline_cache_evolution_generated_megamorphic_store=1`, `property_inline_cache_evolution_megamorphic_store_skips=15`, `property_store_megamorphic_cache_records=16`, and `property_store_megamorphic_cache_current_entries=12`. Remaining gaps are C++ exact key exclusions where Rust lacks string text at the cache-key boundary, shared load/store/has epoch ownership, megamorphic store miss/rejection telemetry, `PutByValue`/element stores, has/in megamorphic support, and Octane runner second-sighting/perf measurement.
- 2026-05-27: D99 store-megamorphic `PutById` key predicate accepted. C++ audits confirmed `canUseMegamorphicPutById` rejects array-index names and `__proto__` while allowing ordinary names including `length`, `name`, and `prototype`; `in`/has megamorphic instead follows the stricter GetById exclusions, and current Rust `in` support is parsed-only with no core opcode/interpreter/IC path yet. Rust now carries a runtime-proven `can_use_put_by_id_megamorphic` fact on property-store observations, computes it from the observed source key text at drain time, gates store megamorphic folding on that fact, and keeps excluded store names on the finite-handler path until they naturally reach `GaveUp` rather than entering the VM store megamorphic cache. Focused coverage proves the C++ store-name predicate and that an otherwise eligible polymorphic store site with an excluded key does not generate store megamorphic records while the D98 eligible-store fold still works. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --lib`, focused store-key and store-fold tests, full `cargo test --lib -- --quiet` with 2154 passed, `cargo check --example octane_probe`, and `cargo build --release --example octane_probe`. The next shared dependency is the audited C++ `MegamorphicCache` ownership model: one VM-owned cache with load/store/has tables and one shared epoch/aging record before adding the has-cache substrate or `in` bytecode plumbing.
- 2026-05-27: D100 shared megamorphic cache epoch accepted. C++ audits confirmed `VM::megamorphicCache()` owns separate load/store/has tables with one `uint16_t` epoch, ages that epoch once for Eden/full collection and structure-chain invalidation, clears all tables on full collection or epoch wrap, and has generated entries compare their per-entry epoch against the shared table epoch. Rust now moves epoch ownership out of the load/store cache structs into `VmTieringIntegration`, keeps per-entry epochs, passes the shared epoch into load/store insert/count/projection APIs, exposes neutral shared epoch/aging aliases while preserving existing load/store telemetry names, and records store entry counts on the existing aging record. Focused coverage proves load and store entries stale under the shared epoch, collection completion and structure-chain invalidation age once and make both cache kinds stale, and full collection clears both load/store arrays while generated-side lookup stays unchanged. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --lib`, focused megamorphic epoch/aging tests, full `cargo test --lib -- --quiet` with 2155 passed, `cargo check --example octane_probe`, and `cargo build --release --example octane_probe`. Remaining gaps are adding the has table substrate on this shared epoch, neutralizing load-named aging telemetry in public summaries if needed, and delaying full `in` bytecode/IC integration until core opcode/interpreter plumbing exists.
- 2026-05-27: D101 dormant has/in megamorphic cache substrate accepted. C++ audits confirmed `MegamorphicCache` has entries use primary/secondary sizes 512/128, share the VM `uint16_t` epoch, store `(uid, structureID, epoch, result)`, move only current-epoch displaced primary entries into secondary, and generated `hasMegamorphicProperty` returns the cached boolean after the same primary-stale-terminal rule as load/store. `canUseMegamorphicInById` follows the strict GetById exclusions: array-index names, `length`, `name`, `prototype`, and `__proto__`. Rust now adds the third VM-owned has table under the D100 shared epoch, has hit/miss insertion and record/site/projection APIs, generated-side has candidate snapshots returning `Hit(bool)`, shared aging counts/clearing for load/store/has together, `octane_probe` has-cache counters, and an explicit Rust predicate alias showing `in` uses the GetById exclusion set. This is intentionally dormant: parser support for `in` exists, but bytecode lowering, core opcodes, interpreter dispatch, IC evolution folding, and generated sidecar execution remain deferred. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --lib`, `cargo check --example octane_probe`, focused has-megamorphic/generated/aging/key-predicate tests, full `cargo test --lib -- --quiet` with 2161 passed, and `cargo build --release --example octane_probe`. Remaining gaps are real `InById`/`InByVal` bytecode and interpreter semantics, non-object TypeError behavior, by-val `ToPropertyKey`, slow-path has observation formation, IC evolution promotion to `InMegamorphic`/`IndexedMegamorphicIn`, generated has sidecar execution, symbol UID policy, and C++ dictionary/override/static-class-table cacheability predicates before activation.
- 2026-05-27: D102 core `in` bytecode/interpreter slice accepted. C++ audits tied the slice to `NodesCodegen`/`BytecodeGenerator` lowering where non-index string literals emit `op_in_by_id(dst, base, identifier)` after evaluating the base, while generic `lhs in rhs` emits `op_in_by_val(dst, base, property)` after evaluating key then base; LLInt/CommonSlowPaths require the RHS object check before by-val `ToPropertyKey` and route through `hasProperty`/proxy `has` without invoking ordinary getters. Rust now adds explicit `CoreOpcode::InById` and `CoreOpcode::InByVal`, lowers `AstBinaryOperator::In` to the C++ operand/evaluation shapes, dispatches by-id through the shared `has_property_value_with_completion` path, dispatches by-val through a string-hint `ToPropertyKey` path that preserves symbols and object-key coercion, and converts invalid RHS values into catchable `TypeError` throws instead of internal failures. This remains interpreter/core only: no inline-cache metadata derivation, has-megamorphic promotion, generated sidecar execution, `ArrayProfile`, `op_enumerator_in_by_val`, private `#x in obj`, or baseline/JIT admission yet. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --lib`, `cargo check --example octane_probe`, focused `in` lowering/execution/coercion tests, full `cargo test --lib -- --quiet` with 2167 passed, and `cargo build --release --example octane_probe`. Remaining gaps are generated/IC has-cache activation, exact C++ invalid-in error wording/source rewriting, by-val uint32 `ArrayProfile` observation, symbol UID policy for has caches, and broader `allow_in`/for-in grammar fidelity.
- 2026-05-27: D103 dormant `in` property-IC metadata and tier-safety accepted. C++ audits confirmed LLInt `op_in_by_id`/`op_in_by_val` remain slow-path-only, while Baseline JIT allocates side-car `BaselineUnlinkedPropertyInlineCache` records and `JITInById`/`JITInByVal` generators before executable repatching; `in` attachment taxonomy is boolean `InHit`/`InMiss`/`InMegamorphic` plus indexed/proxy cases, not load-value behavior. Rust now derives unlinked `PropertyInlineCache` entries for `InById` and `InByVal` with exact result/base/key operand shapes, `InById`/`InByVal` access/cache kinds, no get/put metadata, and linked-code-block authority. Baseline planning now classifies both opcodes as property accesses while keeping them out of generated property handoff admission, generated property handoff metadata derivation ignores the dormant `in` IC sites, and forged baseline property handoff execution rejects `InById`/`InByVal` as unsupported. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --lib`, `cargo check --example octane_probe`, focused `in`/property-handoff tests, full `cargo test --lib -- --quiet` with 2171 passed, and `cargo build --release --example octane_probe`. Remaining gaps are `PropertyLookupMode::HasProperty` observation formation, by-val `ArrayProfile`, IC evolution to `InMegamorphic`/`IndexedMegamorphicIn`, generated has sidecar execution, proxy/call-link status, symbol UID policy, and C++ dictionary/override/static-class-table cacheability predicates before activation.
- 2026-05-27: D104 interpreter `HasProperty` observation slice accepted. C++ audits confirmed `JSObject::hasProperty` is a `PropertySlot(..., HasProperty)` lookup that returns boolean presence without performing value gets, LLInt `in` remains slow-path-only, optimizing `operationInBy*Optimize` feeds `repatchInBy` from `HasProperty` slot results, and proxy/indexed paths require separate proxy call-link and `ArrayProfile`/indexed-in treatment. Rust now records `PropertyLookupMode::HasProperty` for interpreter `InById` and `InByVal` after successful by-val `ToPropertyKey`, records ordinary own/prototype/accessor/miss facts without invoking getters, marks indexed/typed-array presence as uncacheable `IndexedIn`, records proxy `has` as opaque/tainted/may-call, and keeps existing property-load drains rejecting these records so no load IC can accidentally consume `in` observations. This remains interpreter-local evidence only: no VM-visible has observation descriptor, no has-megamorphic promotion, no `InHit`/`InMiss` access-case planning, no generated has sidecar execution, and no `ArrayProfile` plumbing. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --lib`, `cargo check --example octane_probe`, focused `has_property`/`in_by` tests, full `cargo test --lib -- --quiet` with 2176 passed, and `cargo build --release --example octane_probe`. Remaining gaps are root-safe VM has observation descriptors, C++-faithful `InHit`/`InMiss`/proxy/indexed case taxonomy, by-val uint32 `ArrayProfile`, symbol UID policy for has caches, and dictionary/override/static-class-table cacheability predicates before activation.
- 2026-05-27: D105 root-safe dormant `HasProperty` observation descriptor accepted. C++/Rust audits confirmed `in` slow paths must carry boolean `PropertySlot::HasProperty` facts separately from loads, with root-safe base/holder/chain identity and no generated IC, CodeBlock mutation, proxy call-link, or has-megamorphic promotion yet. Rust now adds a JIT-facing `PropertyHasObservationDescriptor`, `HasProperty` IC schema cases for `InHit`/`InMiss`/proxy/indexed/megamorphic, interpreter `drain_property_has_observation` for `InById`/`InByVal`, conservative by-val key representation (`Dynamic` when not losslessly represented), root-safety tainting, and VM tiering records that only validate and store has observations without touching inline-cache evolution or the has megamorphic table. Focused coverage proves named hit/miss, by-val dynamic key, indexed `in`, proxy opacity, descriptor result/case validation, load drains still ignoring has records, and dormant VM recording. Gates passed including `cargo fmt`, `git diff --check`, `cargo check --lib`, `cargo check --example octane_probe`, focused `has_property`/`in_by`/`property_has` tests, full `cargo test --lib -- --quiet` with 2182 passed, and `cargo run --release --example octane_probe` which honestly still fail-fast stops at the current TypeScript parser gap. Remaining gaps are precise C++ indexed/proxy `AccessCase` taxonomy, by-val uint32 `ArrayProfile`, symbol UID policy, dictionary/override/static-class-table cacheability predicates, proxy call-link status, IC evolution to `InMegamorphic`/`IndexedMegamorphicIn`, and generated has sidecar execution.
- 2026-05-27: D106 precise dormant `in` AccessCase taxonomy accepted. C++ audits confirmed `in` uses named `InHit`/`InMiss`/`InMegamorphic`, proxy `ProxyObjectIn`/`IndexedProxyObjectIn`, and distinct indexed hit/miss/typed-array cases; proxy cases may call JS, typed-array `*In` cases can carry either boolean result, and `InByStatus` still only treats simple named hit/miss as DFG-simple. Rust now adds the C++-named dormant `in` variants, restricts the `HasProperty` schema to those precise cases instead of generic `ProxyObject`/`Megamorphic`/`IndexedIn`, validates boolean polarity without rejecting typed-array false results, and maps current interpreter evidence conservatively: named hit/miss, array indexed hit to `IndexedArrayStorageInHit`, `Uint8Array` hit to `IndexedTypedArrayUint8In`, ordinary numeric miss to `IndexedNoIndexingInMiss`, and proxy by-id/by-val to `ProxyObjectIn`/`IndexedProxyObjectIn`. Interpreter-private indexed records are precise only where Rust has object-kind evidence; unknown indexed shapes remain unclassified. This remains metadata-only: no has-megamorphic insertion, generated has sidecar execution, proxy call-link status, watchpoints, or DFG `InByStatus` activation. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --lib`, `cargo check --example octane_probe`, focused `has_property`/`in_by`/`property_has` tests, full `cargo test --lib -- --quiet` with 2185 passed, and `cargo run --release --example octane_probe` which still honestly fail-fast stops at the TypeScript parser gap. Remaining gaps are by-val uint32 `ArrayProfile`, exact indexed array/string/arguments/resizable typed-array predicates, typed-array out-of-range C++ stop-prototype semantics, symbol/private-name megamorphic policy, dictionary/override/static-class-table cacheability predicates, proxy call-link status, IC evolution to `InMegamorphic`/`IndexedMegamorphicIn`, and generated has sidecar execution.
- 2026-05-27: D107 dormant `InByVal` `ArrayProfile` slice accepted. C++ audits confirmed `op_in_by_val` owns one `ArrayProfile`, LLInt slow path passes it into `CommonSlowPaths::opInByVal`, only `JSValue::getUInt32` indexed keys call `observeIndexedRead`, that observation records last seen structure plus out-of-bounds, and `MayBeLargeTypedArray` is not set by this opcode path. Rust now allocates one linked dormant `ArrayProfile` per decoded `InByVal`, exposes owner-authorized indexed-read mutation on linked code blocks, and records interpreter `InByVal` uint32 observations with the C++ ordering before `ToPropertyKey`; typed-array out-of-bounds sets only the `out_of_bounds` flag and leaves large/resizable typed-array, generated indexed ICs, `repatchArrayInByVal`, megamorphic `InByVal`, and DFG `ArrayMode` consumption dormant. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --lib`, `cargo check --example octane_probe`, focused `array_profile`/`in_by_val`/`has_property`/`property_has` tests, full `cargo test --lib -- --quiet` with 2188 passed, and `cargo run --release --example octane_probe` which still honestly fail-fast stops at the TypeScript parser gap. Remaining gaps are exact indexed array/string/arguments/resizable typed-array predicates, C++ typed-array/string OOB fast-path behavior, proxy call-link status, `repatchArrayInByVal`/IC evolution to indexed and megamorphic `in`, DFG `InByStatus`/`ArrayMode` activation, symbol/private-name megamorphic policy, dictionary/override/static-class-table cacheability predicates, and generated has sidecar execution.
- 2026-05-27: D108 narrow named has-IC megamorphic evolution accepted. C++ audits confirmed `operationInByIdOptimize`/named `operationInByValOptimize` attach ordinary `InHit`/`InMiss` cases through `repatchInBy`, fold only after the shared 8-case access-variant threshold when every case is named hit/miss and the key passes `canUseMegamorphicInById`, and populate `initAsHasHit/Miss` only from the megamorphic slow helper after cacheability, override, dictionary, and mono-proto checks. Rust now gives has observations their own `VmPropertyInlineCacheEvolutionKind::Has`, terminal `MegamorphicHas`, generated/skipped has decisions, and probe/Octane summary counters. Eligible named `InById` `InHit`/`InMiss` descriptors enter the shared countdown/buffering/cooldown evolution path and populate the VM has megamorphic cache only after the terminal has-megamorphic state; dynamic keys, indexed `InByVal`, proxy/may-call, and disallowed-cacheability descriptors remain dormant with no evolution or cache entries, and generated has sidecar execution remains off. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --lib`, `cargo check --example octane_probe`, focused `property_has`, `property_inline_cache_evolution`, and Octane summary-delta tests, full `cargo test --lib -- --quiet` with 2190 passed, and `cargo run --release --example octane_probe` which still honestly fail-fast stops at the TypeScript parser gap. Remaining gaps are exact C++ dictionary/override/static-class-table and mono-proto predicates before generated activation, named `InByVal` megamorphic folding, indexed/proxy megamorphic `in`, proxy call-link status, symbol/private-name policy, and generated has sidecar execution.
- 2026-05-27: D109 named `InByVal` has-megamorphic key proof accepted. C++ audits confirmed named `operationInByValOptimize` only attaches when the original runtime key is a cacheable identifier cell, not merely when `ToPropertyKey` yields matching text; uint32 keys remain indexed, array-index names stay out of the named path, symbols are finite-cacheable in C++ but the generated `inByValMegamorphic` helper only consumes strings. Rust now carries an optional root-safe `CacheKey` on property lookup records, marks identifier-produced string values as atom-backed, uses that atom identity to admit only eligible named string `InByVal` has observations, and deliberately avoids inferring named cache identity from converted booleans/numbers or non-atom heap strings. Eligible named `InByVal` `InHit`/`InMiss` descriptors now share the D108 has evolution and VM has-cache population path with `InById`; indexed/proxy/dynamic/disallowed cases, symbols/private names, generated has sidecar execution, and C++ dictionary/override/static-class-table predicates remain dormant. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --lib`, `cargo check --example octane_probe`, focused `in_by_val`, `property_has`, and `property_inline_cache_evolution` tests, the by-id cache-key regression test, full `cargo test --lib -- --quiet` with 2193 passed, and `cargo run --release --example octane_probe` which still honestly fail-fast stops at the TypeScript parser gap in `typescript:./Octane/typescript-compiler.js` at span `11175..11177`.
- 2026-05-27: D110 narrow generated named has sidecar accepted. C++ audits confirmed Baseline generated `InById` can answer terminal `InMegamorphic` hits by loading the cached boolean from `hasMegamorphicProperty`, while `InByVal` generated megamorphic execution first proves the runtime property is an atom-backed non-rope string and indexed `InByVal` uses separate `IndexedMegamorphicIn` machinery. Rust now derives generated property handoff metadata for `InById`/`InByVal`, drains generated has handoffs into the D108/D109 has observation path, passes generated has-megamorphic tables through VM sidecars, and executes only `InById` named has-megamorphic hits through an object-only base-structure host hook. `InByVal`, primitive bases, proxies, unrelated sites, stale entries, misses, unsupported keys, and pending structure-chain invalidations still fall through to the existing property handoff rather than inventing behavior. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --lib`, `cargo check --example octane_probe`, focused generated-handoff/property-has/property-inline-cache tests, full `cargo test --lib -- --quiet` with 2200 passed, and `cargo run --release --example octane_probe`, which still fail-fast stops at the known TypeScript parser gap in `typescript:./Octane/typescript-compiler.js` at span `11175..11177`.
- 2026-05-27: D111 atom-backed named `InByVal` generated has sidecar accepted. C++ audits confirmed `operationInByValOptimize` and generated `IndexedMegamorphicIn` only use the named has-megamorphic path after proving an object base plus cacheable string cell, non-index name, atom/non-rope string identity, and `canUseMegamorphicInById`; indexed keys, symbols, dynamic coercions, proxies, and primitive RHS/base stay on fallback paths. Rust now adds a host runtime-key proof hook that accepts only atom-backed named strings with matching identifier text, lets generated `InByVal` has sidecars consume the has-megamorphic table only when the runtime key register matches the handoff metadata and table site, keeps stale/miss/no-site/pending-invalidation paths dormant, and admits `InById`/`InByVal` retained P10 property native exits through VM install/validation. Gates passed including focused generated has sidecar, property-has, generated-handoff, P10 emitter, and P10 retained VM tests; `cargo fmt -- --check`; `git diff --check`; `cargo check --lib`; `cargo check --example octane_probe`; full `cargo test --lib -- --quiet` with 2210 passed; and `cargo run --release --example octane_probe`, which still fail-fast stops at the TypeScript parser gap in `typescript:./Octane/typescript-compiler.js` at span `11175..11177`.
- 2026-05-27: D112 TypeScript shared-correctness breadth slice accepted. C++ audits tied the parser/runtime fixes to JSC lexer string-escape cooking, `var` declarations without initializers producing no assignment node, `typeof` unresolved names using `DoNotThrowIfNotFound`, relational operators flowing through `jsLess/jsLessEq`, String.prototype case methods, global `parseFloat`, and `ToNumber` for strings/Date objects. Rust now parses and lowers for-in, sequence expressions, labels plus named break/continue, JSC-shaped string escapes, `var` no-initializer no-op, unresolved `typeof`, widened primitive relational compares, `toLowerCase`/`toUpperCase`/locale-case string methods, string/Date `ToNumber`, and global `parseFloat`. Focused gates passed for the new parser/lowering/runtime cases plus `cargo check --lib`; `cargo run --release --example octane_probe -- --benchmark typescript --progress` now loads all TypeScript sources and runs the generated runner until the next honest shared boundary: Yarr/RegExp depth rejects `^(\\/\\/\\/\\s*<amd-dependency\\s+path=)('|\")(.+?)\\2\\s*(static=('|\")(.+?)\\2\\s*)*\\/>` with `gim` flags, surfaced as `Invalid regular expression pattern`. Remaining gaps are full JSC Yarr parse/execute semantics, String regex methods (`match`/`replace`/`split`) on that substrate, and global `NaN`/`Infinity` standard properties before broader TypeScript correctness.
- 2026-05-27: D113 TypeScript AMD-dependency RegExp slice accepted. C++ audits confirmed JSC Yarr accepts the pattern under `gim`, counts six captures, treats `\2` as a numeric backreference, backtracks lazy `.+?` against the following quote, preserves repeated-capture last-match/undefined semantics, and lets unmatched backreferences match empty. Rust now fixes parse planning so quantifiers bind to the whole parenthesized atom and quantified backreferences are legal, adds a shared `yarr` executor subset for literals, dot, classes, built-ins, groups, alternation, greedy/lazy quantifiers, captures, numeric backreferences, and multiline anchors, and routes `RegExp.exec`/`test` through that shared path with capture arrays and global/sticky `lastIndex` updates. Gates passed including `cargo test --lib simple_yarr_exec -- --quiet` 4 passed, `cargo test --lib vm_regexp_ -- --quiet` 6 passed, `cargo test --lib yarr -- --quiet` 34 passed, `cargo test --lib regexp -- --quiet` 11 passed, `cargo check --lib`, and a direct `octane_probe --eval` AMD-pattern smoke returning the true branch; a debug TypeScript `--benchmark-eval '42;'` smoke hit the 60s external cap without re-reporting the prior invalid-regex error. Remaining gaps are full JSC Yarr bytecode/JIT and Unicode/UTF-16 semantics, String regex methods on the shared substrate, global `NaN`/`Infinity`, and the next honest TypeScript runtime frontier.
- 2026-05-27: D114 TypeScript String prototype and scanner-throughput slice accepted. C++ audits tied the breadth to `StringPrototype.cpp` installing `replace`/`split` and routing RegExp search through the Yarr/RegExp substrate, plus TypeScript's scanner/parser calling `String.prototype.charCodeAt`, `substring`, `substr`, `split`, `replace`, and `lastIndexOf` in `typescript-compiler.js`. Rust now adds borrowed UTF-16-code-unit helpers for string indexing, avoids cloning string payloads for string primitive prototype lookups, adds partial JSC-shaped `lastIndexOf`/`substr`/`split`/`replace` methods including RegExp replacement through the shared simple Yarr executor, fixes `split(undefined)`, and adds an interpreter intrinsic path for native `String.prototype.charCodeAt` calls. Gates passed including `cargo fmt`, `git diff --check`, `cargo check --lib`, focused string indexing and TypeScript-helper VM tests, `cargo test --lib regexp -- --quiet` 11 passed, `cargo test --lib yarr -- --quiet` 34 passed, and `cargo build --release --example octane_probe`. Release TypeScript source-load plus benchmark-eval `42` now completes cleanly, direct String/RegExp evals return true, and a cached full `compiler_input.charCodeAt` sweep completes under a 120s cap; however parse-only `compiler.parser.parse(...)` still times out at 240s and full `addUnit` still times out, so the honest next frontier is interpreter/JIT throughput and property/call IC execution for TypeScript scanner/parser loops, with remaining standard-library gaps including full function replacers, `match`/`search`, full RegExp `@@replace`/`@@split`, global `NaN`/`Infinity`, and full Yarr semantics.
- 2026-05-27: D115 generated-baseline `GetLength` property handoff accepted. C++ audits confirmed public string `.length` lowers to dedicated `OpGetLength`, while `s.charCodeAt(i)` remains ordinary `GetById` plus call until a proven native `CharCodeAtIntrinsic` path is available. Rust now derives `GetLength` property IC metadata, admits it through generated property handoff planning, execution validation, sidecar probing, observation draining, and rootless generated direct-call property exits, preserving slow-path VM semantics for accessors and non-string bases. Gates passed including `cargo fmt -- --check`, `cargo test --lib property_handoff -- --quiet` 50 passed, `cargo test --lib vm_generated_direct_call_rootless_property -- --quiet` 4 passed, `cargo check --lib`, and `cargo build --release --example octane_probe`; the scanner-shaped release probe moved from `fallbacks=2000` and `generated_direct_call_rootless_reject_unsupported_body_opcode=1998` on `GetLength` to `fallbacks=0`, `generated_direct_call_rootless_generated_entries=999`, and zero unsupported opcodes. TypeScript source-load plus benchmark-eval `42` still completes, and the full cached `compiler_input.charCodeAt` sweep completes under 120s with zero unsupported opcodes, but remains dominated by ordinary `charCodeAt` call observations; parser/addUnit throughput is still the next frontier.
- 2026-05-27: D116 typed `StringCharCodeAt` native-intrinsic call path accepted. C++ audits confirmed `String.prototype.charCodeAt` is installed as an ordinary data-property `JSFunction` carrying `CharCodeAtIntrinsic`, lookup remains normal `GetById`, call linking guards the actual callee identity, and the native thunk only specializes exact string receiver plus one int32 in-bounds index before falling back to the full native implementation. Rust now records a stable `NativeIntrinsic::StringCharCodeAt` on native call observations, exposes a generated native-intrinsic dispatch hook parallel to JS call-link descriptors, and lets generated/baseline unsupported `CallWithThis` sites consume it only when the runtime callee is the native `StringCharCodeAt`, the receiver is a string primitive, and the single argument is an in-bounds int32. Overwrites, accessors, proxies, non-string receivers, missing/extra/non-int32/out-of-range arguments, and plain `Call` continue through ordinary VM/native-call fallback. Gates passed including `cargo fmt`, `cargo check --lib`, `git diff --check`, focused `char_code_at` tests, native-target call-observation regression, string indexing VM test, and `cargo build --release --example octane_probe`. The scanner-shaped release probe stays at `fallbacks=0` and drops to `call_observations=34`; the cached TypeScript `compiler_input.charCodeAt` sweep completes in roughly 20s with `call_observations=41` instead of D115's 1,243,631 ordinary call observations. Prototype replacement still returns the replacement result before restoring the native. Full TypeScript benchmark execution still times out at 120s, so the next frontier remains parser/addUnit throughput beyond the scanner `charCodeAt` bottleneck.
- 2026-05-27: D117 typed `StringSubstring` native-intrinsic call path accepted. C++ audits tied the slice to `StringPrototypeSubstringIntrinsic`, `stringProtoFuncSubstring`/`extractSubstringOffsets` clamp-and-swap behavior, and `jsSubstring`/`tryJSSubstringImpl` as the source of truth for substring materialization; Rust deliberately keeps the existing owned-string allocation for now rather than claiming JSC substring-sharing. Rust now records `NativeIntrinsic::StringSubstring`, carries a second generated native-call argument, specializes generated/baseline `CallWithThis` only for the actual native `StringSubstring` callee with primitive string receiver and int32/undefined arguments, and adds a direct interpreter native-call path that reuses the full existing coercing substring semantics without building the generic argument vector. Non-native callees, replacement prototype functions, non-string receivers, non-int32 generated arguments, and extra generated arguments fall back. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --lib`, focused generated substring/native-observation/charCodeAt/string-indexing tests, full `cargo test --lib -- --quiet` with 2248 passed, and `cargo build --release --example octane_probe`. The scanner-shaped `compiler_input.substring(i, i + 16)` release probe keeps the same return value while dropping `call_observations` from 77,765 to 41 and generated executions from 233,265 to 155,541. Prototype replacement returns the replacement result before restoring the native; `compiler_input.substring(0, 2000)` parse completes under 60s, but full `compiler_input` parse still times out at 90s and full TypeScript still times out at 120s inside `Benchmark.runIteration()`. The next frontier remains parser/addUnit throughput, likely after measuring remaining scanner/parser hot calls and then deciding between shared substring backing, parser-loop IC coverage, or additional string/RegExp native paths.
- 2026-05-27: D118 string backing and string-search intrinsic slice accepted. Parallel audits tied the shared next dependency to C++ `JSString::tryJSSubstringImpl`/`jsSubstring` and `StringImpl::createSubstringSharingImpl`, with JSC copying very small strings but sharing longer substring backing, plus C++ `StringPrototypeIndexOfIntrinsic`/`StringPrototypeLastIndexOfIntrinsic` lowering by exact native callee identity. Rust now gives `CoreStringStore` O(1) payload-to-index lookup, adds safe ASCII substring cells that fold substring-of-substring offsets into the original base, keeps tiny substrings and non-ASCII boundary slices on the existing flat allocation path, and extends typed native intrinsics/direct no-Vec native calls to `StringIndexOf` and `StringLastIndexOf` for exact native callees with primitive string receivers/search strings and int32/undefined positions. Coercive search values, non-string receivers, prototype replacement, extra generated arguments, and non-int32 generated positions still fall back to full native semantics. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --lib`, focused core-string-store/generated-search/native-observation/generated-substring/string-substring VM tests, full `cargo test --lib -- --quiet` with 2254 passed, and `cargo build --release --example octane_probe`. Release probes: `compiler_input.substring(i, i + 16)` still returns the D117 value with `call_observations=41` and now runs in about 29s after the small-string threshold; indexOf/lastIndexOf sweeps complete in about 8.2s each with `call_observations=40`; prototype replacement for both search methods returns the replacement result before restoring the native. `compiler_input.substring(0, 2000)` parse remains about 49.5s, `substring(0, 5000)` parse still times out at 90s, and full TypeScript still times out at 120s in the runner, so the next frontier is not more scanner string-call overhead alone: measure parser-loop tiering/allocation and then choose between parser-loop generated coverage, `ArrayPush`/object allocation paths, or deeper RegExp/string method fidelity.
- 2026-05-27: D119 VM-owned nested parser-entry admission accepted. C++ audits tied the priority to LLInt/Baseline tier checks at function entry and loop hints: JS callees are entered and tiered from their own `CodeBlock`, while `CallLinkInfo`/IC metadata controls direct-call fast paths, not whether a cold JS function may use the VM-owned entry path. Rust now splits VM-owned registered-target validation from call-link direct-dispatch validation for ordinary calls, constructs, and function-value calls; it records accepted/rejected VM-owned target telemetry, requires the registered target to be live, snapshot-matching, specialization-matching, executable-owned, and installed on its `FunctionExecutable`, and keeps unregistered/stale/missing-install targets on the direct interpreter fallback. Source-session function call/construct blocks are now root-map stamped before both VM registry insertion and host publication so the host request and registered `CodeBlock` have identical snapshot provenance. Gates passed including `cargo fmt -- --check`, `cargo check --lib`, focused VM-owned call/construct/function-value/source-session snapshot tests, full `cargo test --lib -- --quiet` with 2255 passed, and `cargo build --release --example octane_probe`. The 1k TypeScript parser-prefix probe now admits nested parser callees (`entry_decisions=1399`, `launch_descriptors=236`, `vm_owned_call_target_accepted=1393`, `vm_owned_call_target_rejected=0`, `call_observations=1075`) where D118 stayed at six entry decisions and disallowed all cold parser callees; however the broader 2k prefix regresses to a 90s timeout because generated direct-call/native-entry coverage is still zero and unsupported generated bodies fall back thousands of times. The next dependency is therefore parser-call performance after admission, not more admission policy: either broaden generated/native coverage for the accepted parser bodies, attach call/direct-entry metadata, or address the `ArrayPush`/allocation hot path after confirming the top fallback owners.
- 2026-05-27: D120 closure/capture generated-runtime-helper exits accepted. Parallel C++/Rust audits found the post-D119 TypeScript parser prefix was not yet blocked by `ArrayPush` or rootless direct-call proof: the hot generated parser bodies exited first on closure environment bytecodes (`LoadCapture=3086`, `NewClosureCell=845`) before generated call-link metadata could be consumed. Rust now emits precise helper root maps for `LoadCapture`, `NewClosureCell`, `GetClosureCell`, and `PutClosureCell`; derives runtime-helper boundary proofs for the closure/capture family; admits them into retained x86_64 runtime-helper native exits; and validates their operand shapes without adding unsafe no-heap P6 semantics for heap reads/allocation/write-barriers. Focused root-map and runtime-helper derivation tests pass, along with `cargo check --lib` and `cargo build --release --example octane_probe`. The 1k TypeScript parser-prefix probe now drops `fallbacks` from 3934 to 234, raises `baseline_generated_executions` from 4570 to 19497, raises `baseline_generated_executed_bytecodes` from 8020 to 47885, and exposes call-link consumption (`generated_direct_call_transactions=885`, `generated_direct_call_native_entries=811`, `generated_direct_call_generated_entries=74`) with VM-owned target validation still clean (`accepted=1393`, `rejected=0`). The next honest blockers are generated `ArrayAppend`/truthiness/strict-equality/number fallback sites and rootless native-entry retained side exits; the 2k prefix still exceeded a 180s cap, so throughput parity is not claimed.
- 2026-05-27: D121 generated `ArrayAppend` runtime-helper native exit accepted. C++ audits confirmed there is no standalone C++ JSC `ArrayAppend` bytecode; the Rust opcode is a local helper abstraction for plain array literal append only and must not absorb spread/rest/elision behavior. Rust now gives `ArrayAppend(array, value)` a source/source helper root map, runtime-helper boundary descriptor, x86_64 retained native-exit eligibility, no-JS-call heap-helper classification, and operand-shape validation while leaving actual append semantics in the VM helper path with allocation/write-barrier behavior. A narrow rootless `ToNumber` side-exit admission was explicitly rejected and reverted after the same parser-prefix probe hit the 150s timeout, so D121 does not widen retained side-exit admission. Gates passed including `cargo fmt -- --check`, `git diff --check`, focused root-map/runtime-helper tests, `cargo check --lib`, full `cargo test --lib -- --quiet` with 2259 passed, and `cargo build --release --example octane_probe`. On the same-command 1k TypeScript parser-prefix rebaseline, `fallbacks` moves from 233 to 232, `baseline_generated_executions` from 19482 to 21466, and `baseline_generated_executed_bytecodes` from 47863 to 51796, with `generated_direct_call_transactions=885`, native/generated entries unchanged at `811/74`, rootless native entries still `127`, and VM-owned targets still clean (`accepted=1393`, `rejected=0`). The remaining blockers are truthiness and strict-equality fallback sites plus retained side exits dominated by `AddInt32` non-int32/overflow and `ToNumber` unsupported operands.
- 2026-05-27: D122 primitive strict-equality emitted-native fast path accepted. C++ audits tied the slice to LLInt `strictEqOp` and `JSValue::strictEqual`: side-exit before raw equality for doubles/unknowns, accept raw equal non-doubles including same-cell identity, side-exit raw-unequal cell-involved cases, and materialize primitive `undefined`/`null`/boolean/int32 strict equality without calls. Rust now lowers `StrictEqual`/`StrictNotEqual` in the primitive-boolean native subset, adds a dedicated `UnsupportedStrictEqualityOperand` side-exit reason, emits x86_64 tag guards and raw JSValue comparison, maps callable side exits back to generated fallback telemetry, and keeps rootless retained native entry rejection for this side-exit reason. Focused emitter/VM/rootless tests cover primitive completion, same-cell identity, double/unknown/cell side exits, and rootless rejection. Gates passed including `cargo fmt -- --check`, `cargo check --lib`, full `cargo test --lib -- --quiet` with 2262 passed, `cargo build --release --example octane_probe`, and the 1k TypeScript parser-prefix probe. The parser-prefix counters stayed at the D121 surface (`fallbacks=232`, `baseline_generated_executions=21466`, `baseline_generated_executed_bytecodes=51796`, `baseline_native_lowering_failures=104`, direct-call native/generated entries `811/74`, rootless native entries `127`, VM-owned accepted/rejected `1393/0`), so activation did not perturb the current Octane slice. Remaining retained side exits are still dominated by `AddInt32` non-int32/overflow and `ToNumber` unsupported operands, with broader truthiness/number semantics still deferred.
- 2026-05-27: D123 arithmetic side-exit single-dispatch slow path accepted. C++ audits kept the next numeric slice honest: `op_to_number` fast paths are number-only while non-number conversion belongs on slow/runtime paths, and Baseline arithmetic slow paths execute the exiting operation before continuing rather than replaying the entire remainder as an interpreter fallback. Rust now handles emitted-native `AddInt32`/`SubInt32`/`MulInt32` side exits for non-int32, overflow, and negative zero by synchronizing the existing deferred roots, single-dispatching exactly the side-exited bytecode, and resuming from the returned next bytecode through the existing generated/interpreter resume control; unsupported side-exit families still use the old precise-PC interpreter fallback. A broader generated-first ownership policy for `ToNumber` plus numeric bodies was measured and rejected locally because the 1k TypeScript prefix hit the 150s timeout, so it is not retained. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --lib`, focused callable side-exit tests, full `cargo test --lib -- --quiet` with 2262 passed, `cargo build --release --example octane_probe`, and the 1k TypeScript parser-prefix probe. The parser-prefix counters remain at the D122 surface (`fallbacks=232`, `baseline_generated_executions=21466`, `baseline_generated_executed_bytecodes=51796`, `baseline_native_lowering_failures=104`, direct-call native/generated entries `811/74`, rootless native entries `127`, VM-owned accepted/rejected `1393/0`), so this is a baseline slow-path structure improvement rather than a rootless-admission win; retained rootless native blockers are still `AddInt32` non-int32/overflow plus `ToNumber` unsupported operands.
- 2026-05-27: D124 rootless `ToNumber` retained-side-exit admission rejected. Parallel C++ and Rust audits confirmed C++ Baseline/LLInt `op_to_number` fast paths are number-only and the slow path executes `argument.toNumber(globalObject)`, writes the destination, checks exceptions, and resumes at the next bytecode; strings, objects, Symbols, and BigInts are not safe emitted-native fast-path cases without exact conversion/rooting/throw behavior. A narrow Rust admission attempt allowed static `ToNumber` retained side exits into rootless emitted-native entries with deferred throwing-side-exit rooting and passed focused proof/rooting tests, but the same 1k TypeScript parser-prefix probe that completes at D123 timed out under the 150s cap. The policy was reverted: `UnsupportedToNumberOperand` remains a rootless native-entry rejection, and the next numeric dependency should target either exact generated `ToNumber` slow-path continuation cost or C++-backed number-number arithmetic fast paths rather than static rootless admission alone.
- 2026-05-27: D125 emitted-native number-number arithmetic fast path accepted. C++ audits tied the slice to LLInt/Baseline arithmetic shape: generic `op_add`/`op_sub`/`op_mul` try checked int32 first, then accept already-number operands through int32-to-double/double unboxing and IEEE `addd`/`subd`/`muld`, while strings, objects, BigInts, and other non-number conversions remain on the slow path; int32 overflow and int32 `mul` negative-zero still side-exit. Rust now lowers emitted-native `AddInt32`/`SubInt32`/`MulInt32` through an `Int32OrNumberArithmetic` machine instruction that keeps the checked int32 result path, guards tagged int32/double operands, masks the Rust tagged-double low byte before SSE use, boxes double results with the canonical double tag, and leaves non-number/overflow/negative-zero retained exits intact. Focused VM coverage proves mixed/double arithmetic returns from the emitted callable entry without host dispatch, including double negative zero. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --lib`, focused emitter/VM arithmetic tests, full `cargo test --lib -- --quiet` with 2263 passed, `cargo build --release --example octane_probe`, and the 1k TypeScript `compiler.addUnit(compiler_input.substring(0, 1000), ...)` parser-prefix probe. Parser-prefix counters stay at the D123/D124 surface (`fallbacks=232`, `baseline_generated_executions=21466`, `baseline_generated_executed_bytecodes=51796`, `baseline_native_lowering_failures=104`, direct-call native/generated entries `811/74`, rootless native entries `127`, VM-owned accepted/rejected `1393/0`) because rootless native-entry admission still rejects retained arithmetic and `ToNumber` side exits statically; the next numeric dependency is exact `ToNumber` slow-path continuation or reducing retained side-exit admission cost, not another arithmetic-only emitted fast path.
- 2026-05-27: D126 `ToNumber` side-exit single-dispatch scaffold accepted. C++ audits confirmed Baseline/LLInt `op_to_number` exits through the shared slow path, computes `argument.toNumber(globalObject)`, checks exceptions before publishing the destination, writes/profile-updates only on success, and resumes compiled/LLInt continuation rather than replaying the rest of the interpreter. Rust now admits only the rooted retained-side-exit dispatcher pair `UnsupportedToNumberOperand` plus `ToNumber` into the existing single-bytecode side-exit path while keeping rootless emitted-native admission rejected for `UnsupportedToNumberOperand`. Focused VM tests cover rootless rejection, unknown-cell fallback telemetry, and string `ToNumber` side-exit correctness; the string case proves today's P6 native continuation still dispatches the exact `ToNumber` bytecode once and then falls back from the next bytecode because arbitrary-bytecode native continuation/reentry labels are not implemented yet. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --lib`, focused `ToNumber` side-exit/rootless tests, full `cargo test --lib -- --quiet` with 2264 passed, `cargo build --release --example octane_probe`, and the corrected 1k TypeScript parser-prefix probe (`var compiler = createCompiler(); compiler.addUnit(compiler_input.substring(0, 1000), "compiler_input.ts");`). Parser-prefix counters remain at the D125 surface (`fallbacks=232`, `baseline_generated_executions=21466`, `baseline_generated_executed_bytecodes=51796`, `baseline_native_lowering_failures=104`, direct-call native/generated entries `811/74`, rootless native entries `127`, VM-owned accepted/rejected `1393/0`), so the next shared dependency is generic P6 native continuation for side-exit resumes before reconsidering rootless `ToNumber` admission.
- 2026-05-27: D127 P6 retained side-exit native continuation accepted. C++ audits confirmed Baseline slow paths execute the exiting bytecode once, check exceptions before normal continuation, and resume the compiled slow-case continuation (`fastPathResumePoint`) instead of falling through the interpreter; Rust audits identified side-exit-owned resume metadata as the smallest safe native reentry boundary. Rust now records each eligible retained arithmetic/`ToNumber` side exit's resume bytecode plus callable reentry offset, emits a callable reentry prologue for those resume targets, retains the metadata in VM side-exit tables, and after a successful `SingleDispatchOutcome::Continue(Some(next))` reenters the active P6 native invocation loop when the snapshot, bytecode, and entry offset all validate. Missing, mismatched, throwing, rootless, or unsupported metadata keeps the old interpreter/generated fallback path. Focused coverage proves `ToNumber` string side exits dispatch exactly `ToNumber` once then finish the following native `AddInt32`/`Return`, arithmetic overflow/negative-zero resumes natively while generic non-int32 arithmetic still completes through interpreter, and emitted side-exit stubs point at explicit callable reentry prologues rather than arbitrary labels. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --lib`, focused emitter/VM/rootless side-exit tests, full `cargo test --lib -- --quiet` with 2264 passed, `cargo build --release --example octane_probe`, and the corrected 1k TypeScript parser-prefix probe. Parser-prefix headline counters remain unchanged from D126 (`entry_decisions=1424`, `fallbacks=232`, `baseline_generated_executions=21466`, `baseline_generated_executed_bytecodes=51796`, `baseline_native_lowering_failures=104`, direct-call native/generated entries `811/74`, rootless native entries `127`, VM-owned accepted/rejected `1393/0`); rootless retained-side-exit admission is still rejected, so the next dependency is reducing retained side-exit/rootless cost or widening only C++-proven slow paths that can use this continuation boundary.
- 2026-05-27: D128 rootless `ToNumber` retained-side-exit admission rejected after D127 reentry. C++ audits still support the semantic shape only with deferred roots synchronized before the `op_to_number` slow path and exceptions treated as terminal, while the Rust audit confirmed D127 resume metadata can identify the next native continuation. A narrow candidate admitted `UnsupportedToNumberOperand` plus `ToNumber` into rootless native entry only when the side-exit carried a valid resume bytecode and reentry offset, and focused tests proved the simple string conversion case could dispatch exactly `ToNumber`, publish deferred caller roots, and resume native `Return`. The comparable 1k TypeScript parser-prefix probe nevertheless timed out at the 150s cap with no tiering summary, so the policy was reverted: rootless native entry again rejects `UnsupportedToNumberOperand`, while the rooted single-dispatch/native-continuation path from D126/D127 remains. Gates after revert passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --lib`, focused rootless/ToNumber/native-continuation tests, full `cargo test --lib -- --quiet` with 2264 passed, `cargo build --release --example octane_probe`, and the corrected 1k TypeScript parser-prefix probe. The post-revert counters match D127 (`entry_decisions=1424`, `fallbacks=232`, `baseline_generated_executions=21466`, `baseline_generated_executed_bytecodes=51796`, `baseline_native_lowering_failures=104`, direct-call native/generated entries `811/74`, rootless native entries `127`, VM-owned accepted/rejected `1393/0`), with retained rootless blockers still showing hot `ToNumber` unsupported operands alongside `AddInt32` non-int32/overflow. The next dependency should reduce the cost or frequency of those retained exits rather than statically admitting `ToNumber` rootless reentry.
- 2026-05-27: D129 Octane dependency map and CodeBlock dump telemetry accepted. The rewrite now keeps a compact Mermaid dependency/status tree in `docs/003-octane-dependency-map.md`, marking major completed, WIP, and pending engine surfaces without moving volatile evidence out of `progress.md`. Rust adds a read-only VM diagnostic accessor and `octane_probe --dump-code-block CELL_ID` for eval and benchmark-eval runs, dumping decoded opcodes, operands, source provenance, executable metadata, lifecycle, link context, frame shape, and source notes for a live `CodeBlock`. Parallel C++ and Rust audits agreed not to retry static rootless `ToNumber`: C++ `op_to_number` remains number-fast-path plus rooted slow conversion, while the Rust counters show the current TypeScript parser-prefix blocker is the scanner `nextChar` body shape rather than a standalone conversion policy. The new dump mode confirms hot `CodeBlockId(CellId(2498))` is the TypeScript scanner `nextChar` pattern `GetByName -> ToNumber -> AddInt32(+1) -> PutByName` for `pos++` and `col++`, followed by `peekChar`; this is now represented in the Mermaid map as the WIP scanner property-increment path under opcode-specific side-exit cost reduction. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --lib`, `cargo check --example octane_probe`, full `cargo test --lib -- --quiet` with 2264 passed, `cargo build --release --example octane_probe`, and the 1k TypeScript parser-prefix probe with `--dump-code-block 2498`, whose counters still match D127/D128 (`entry_decisions=1424`, `fallbacks=232`, direct-call native/generated entries `811/74`, rootless native entries `127`, VM-owned accepted/rejected `1393/0`).
- 2026-05-27: D130 generated numeric property-increment sidecar accepted. C++ audit confirmed JSC does not have a fused LLInt/Baseline property-increment opcode: property updates execute as separate get, `ToNumeric`/`Inc`, and put bytecodes with independent get/put ICs, and a Rust fused path is only acceptable as a guarded fast path that preserves those bytecode effects and exits at the correct boundary. Rust now recognizes the current Rust-lowered numeric shape `GetByName -> ToNumber -> optional Move -> LoadInt32(1) -> AddInt32 -> PutByName`, verifies the same base/key and matching `PutByName` handoff site, executes the existing load sidecar first, fuses only already-number loaded values, writes the skipped bytecode registers in bytecode order, rereads `AddInt32` operands so register aliasing matches actual execution, reuses the existing store sidecar and host commit on a hit, and hands off directly at `PutByName` on a store miss after materializing the numeric prefix. Non-number values, accessors, proxies, missing/wrong structures, string/Date/object conversion, BigInt, and generic put behavior stay on the existing bytecode or property handoff paths. Focused tests cover successful postfix-style numeric fusion, store-miss resume at `PutByName`, and register-aliasing order. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --lib`, `cargo check --example octane_probe`, focused property-increment/mixed-sidecar tests, full `cargo test --lib -- --quiet` with 2267 passed, `cargo build --release --example octane_probe`, and the 1k TypeScript parser-prefix probe. The probe remains a non-regression rather than a rootless-native win: counters still match D129 (`entry_decisions=1424`, `fallbacks=232`, generated direct-call native/generated entries `811/74`, rootless native entries `127`, native-entry retained-side-exit rejections `318`) because the hot `nextChar` calls still route through P10 native property exits, which do not yet build the combined load/store increment sidecar. The next dependency is the P10 native-exit combined increment probe, then only after that a conservative rootless admission proof for the skipped `ToNumber`/`AddInt32` retained exits; separately, the bytecompiler still needs eventual C++ `ToNumeric`/`Inc` update lowering for full semantic fidelity.
- 2026-05-27: D131 P10 native property-increment sidecar accepted. The P10 retained `GetByName` native-exit path now builds store sidecar tables alongside the existing load, guarded-load, and megamorphic-load tables, invokes the generated numeric load/increment/store probe for the current Rust-lowered `GetByName -> ToNumber -> optional Move -> LoadInt32(1) -> AddInt32 -> PutByName` shape, and passes current property-handoff metadata so the downstream `PutByName` site is validated against the active native-exit plan. Store hits resume after `PutByName`; store probe misses and mutation rejections are recorded through the P13 telemetry path, and store misses dispatch the downstream `PutByName` handoff directly instead of replaying the original `GetByName`. Focused P10 tests cover store-hit resume-after-put and store-miss no-get-replay behavior. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --lib`, `cargo check --example octane_probe`, focused P10 and generated increment tests, full `cargo test --lib -- --quiet` with 2269 passed, `cargo build --release --example octane_probe`, and the 1k TypeScript parser-prefix probe. The probe remains a non-regression with D130 counters unchanged (`entry_decisions=1424`, `fallbacks=232`, native/generated direct-call entries `811/74`, rootless native entries `127`, retained side-exit rejections `318`); the Mermaid dependency map now marks the P10 increment sidecar done while rootless admission and C++ `ToNumeric`/`Inc` lowering remain pending.
- 2026-05-27: D132 proven property-increment rootless admission accepted. C++ audits kept the boundary narrow: C++ JSC lowers property increments as `GetById`/`ToNumeric`/`Inc`/`PutById`, so a Rust fused rootless path is only valid when the load/store IC evidence proves an own-data replace, the loaded value is int32, `+1` cannot overflow, and every miss resumes at the exact bytecode with roots synchronized before conversion or store slow paths. Rust now gives the P10 increment sidecar a rootless numeric mode that refuses doubles, non-numbers, BigInts, objects, strings, and int32 overflow before the skipped numeric chain; rootless emitted-native admission accepts `UnsupportedToNumberOperand` only when the retained side exit is covered by the exact `GetByName -> ToNumber -> optional Move -> LoadInt32(1) -> AddInt32 -> PutByName` shape and the matching `PutByName` store readiness is an accepted `DataOnlyReplace` for an int32 stored value. Generic `UnsupportedToNumberOperand` remains rejected. Focused tests cover the narrow proof, rejection without the paired put exit, rootless native sidecar hit admission, and store-miss rooting before `PutByName` without replaying `GetByName`. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --lib`, focused rootless increment/P11/generated increment tests, `cargo test --lib increment -- --nocapture`, full `cargo test --lib -- --quiet` with 2272 passed, `cargo build --release --example octane_probe`, and the 1k TypeScript parser-prefix probe. The probe is still a non-regression rather than an Octane win: headline counters remain at D131 (`entry_decisions=1424`, `fallbacks=232`, native/generated direct-call entries `811/74`, rootless native entries `127`, native-entry retained-side-exit rejections `318`), and the hot scanner owners still report retained `ToNumber`/`AddInt32` side exits, so the next scanner dependency is hot store-readiness/value-kind coverage before this admission can activate in the benchmark.
- 2026-05-27: D133 producer-derived Int32 proof for rootless property increments accepted. Parallel C++ and Rust audits clarified that C++ `PutById`/`AccessCase::Replace` store IC metadata proves only own-data replace shape (structure/key/offset/no-call), not stored value kind; the Int32 proof for a rootless fused increment must come from the producer chain and checked arithmetic exits. Rust now keeps the same owner/snapshot/bci/slot/key `DataOnlyReplace` store-readiness requirement for the downstream `PutByName`, but no longer treats `VmGeneratedPropertyStoreMutationReadinessRecord::stored_value_kind` as store-IC proof during rootless increment admission. The P10 rootless increment sidecar still runs in `Int32NoOverflow` mode and refuses doubles, non-numbers, and overflow before committing the store. Focused coverage now seeds a production-style replace-store readiness record from a Double-valued store observation and proves the rootless increment sidecar still admits only because the producer chain stores an Int32 at runtime. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --lib`, focused rootless/increment tests, full `cargo test --lib -- --quiet` with 2272 passed, and `cargo build --release --example octane_probe`. The 1k TypeScript parser-prefix probe remains unchanged from D132 (`entry_decisions=1424`, `fallbacks=232`, rootless native entries `127`, native-entry retained-side-exit rejections `318`), so this fixes the proof model but the hot scanner still needs production store-readiness coverage for bcis 28/52 before the admission can activate.
- 2026-05-27: D134 hot scanner store-readiness coverage accepted. C++ audits confirmed `PutById`/`AccessCase::Replace` creation is shape/property evidence only; write-barrier execution is separate, and immediate Int32/number stores either need no value barrier or are handled by conservative runtime paths. Rust now retains per-execution interpreter property-store observations, drains matching generated property `PutByName`/`PutByValue` sites at the VM safepoint for owners with generated/retained property handoff metadata, and lets the existing store-plan, install-recheck, readiness, and metadata-only attachment pipeline decide whether a `DataOnlyReplace` store is usable. Store barrier readiness now distinguishes actual cell/transition barrier evidence from non-cell no-barrier proof: Int32/double/boolean/null/undefined replace/indexed stores may carry `NotRequired(NullOrNonCellTarget)` with zero observed barriers, while cell stores and transitions still require observed runtime barrier evidence. Focused tests cover production P10 increment store-plan/readiness attachment without manual seeding, store-miss replay at the downstream `PutByName`, non-cell no-barrier store plans/rechecks/candidates, and cell-store missing-barrier rejection. Gates passed including `cargo fmt -- --check`, `git diff --check`, `cargo check --lib`, focused `property_store`, `increment`, and `rootless` suites, full `cargo test --lib -- --quiet` with 2275 passed, `cargo build --release --example octane_probe`, and the corrected 1k TypeScript parser-prefix probe. The probe still has `entry_decisions=1424`, `fallbacks=232`, and direct-call native/generated entries `811/74`, but rootless native entries rise from `127` to `427`, `generated_direct_call_rootless_reject_retained_side_exit` drops to `0`, and native-entry retained-side-exit rejections drop from `318` to `18`; the hot scanner `CodeBlockId(CellId(2498))` now shows native-entry direct-call transactions (`210` and `112`) instead of the prior `ToNumber`/`AddInt32` retained-side-exit rejection wall. Remaining native-entry retained side exits are on other owners, notably `CodeBlockId(CellId(2456))`, plus loop-backedge exit proof.

## Open High-Risk Gaps

- Full GC integration, handles, rooting, barriers, finalization, and weak/ephemeron behavior.
- Host API compatibility and embedding lifecycle.
- Module loading/evaluation, jobs, microtasks, and async ordering.
- Standard library breadth and conformance beyond current VM tests.
- RegExp/Yarr depth, Wasm execution, tooling integration, and optimizing tiers.
